# Generated by linkml-typedb-generator v0.1.0
# Schema: oscal
# https://linkml.io

define

  # Attribute types
  attribute remarks, value string;
  attribute uuid, value string;
  attribute id, value string;
  attribute -class, value string;
  attribute title, value string;
  attribute published, value datetime;
  attribute last-modified, value datetime;
  attribute version, value string;
  attribute oscal-version, value string;
  attribute scheme, value string;
  attribute identifier, value string;
  attribute short-name, value string;
  attribute description, value string;
  attribute email-addresses, value string;
  attribute urls, value string;
  attribute location-uuids, value string;
  attribute member-of-organizations, value string;
  attribute role-id, value string;
  attribute party-uuids, value string;
  attribute date, value datetime;
  attribute system, value string;
  attribute number, value string;
  attribute addr-lines, value string;
  attribute city, value string;
  attribute postal-code, value string;
  attribute country, value string;
  attribute algorithm, value string;
  attribute ns, value string;
  attribute group-attr, value string;
  attribute href, value string;
  attribute rel, value string;
  attribute resource-fragment, value string;
  attribute media-type, value string;
  attribute text, value string;
  attribute filename, value string;
  attribute prose, value string;
  attribute depends-on, value string;
  attribute label-attr, value string;
  attribute usage, value string;
  attribute values, value string;
  attribute expression, value string;
  attribute choice, value string;
  attribute pattern, value string;
  attribute with-ids, value string;
  attribute as-is, value boolean;
  attribute param-id, value string;
  attribute control-id, value string;
  attribute by-name, value string;
  attribute by-class, value string;
  attribute by-id, value string;
  attribute by-ns, value string;
  attribute statement-ids, value string;
  attribute objective-id, value string;
  attribute subject-uuid, value string;
  attribute task-uuid, value string;
  attribute component-uuid, value string;
  attribute start, value datetime;
  attribute end, value datetime;
  attribute period, value integer;
  attribute activity-uuid, value string;
  attribute purpose, value string;
  attribute role-ids, value string;
  attribute functions-performed, value string;
  attribute identifier-type, value string;
  attribute actor-uuid, value string;
  attribute subject-placeholder-uuid, value string;
  attribute methods, value string;
  attribute types, value string;
  attribute collected, value datetime;
  attribute expires, value datetime;
  attribute implementation-statement-uuid, value string;
  attribute target-id, value string;
  attribute reason, value string;
  attribute observation-uuid, value string;
  attribute risk-uuid, value string;
  attribute statement, value string;
  attribute deadline, value datetime;
  attribute implementation-uuid, value string;
  attribute lifecycle, value string;
  attribute status-change, value string;
  attribute party-uuid, value string;
  attribute response-uuid, value string;
  attribute system-name, value string;
  attribute system-name-short, value string;
  attribute date-authorized, value string;
  attribute security-sensitivity-level, value string;
  attribute information-type-ids, value string;
  attribute base, value string;
  attribute selected, value string;
  attribute adjustment-justification, value string;
  attribute security-objective-confidentiality, value string;
  attribute security-objective-integrity, value string;
  attribute security-objective-availability, value string;
  attribute caption, value string;
  attribute statement-id, value string;
  attribute provided-uuid, value string;
  attribute responsibility-uuid, value string;
  attribute source, value string;
  attribute mapping-description, value string;
  attribute relationship, value string;
  attribute id-ref, value string;
  attribute percentage, value double;
  attribute generation-method, value string;
  attribute target-coverage, value double;
  attribute finding-uuid, value string;
  # Enum: partytypeenum (permitted values: person, organization)
  attribute type-attr, value string;
  # Enum: metadatapropnameenum (permitted values: keywords, resolution-tool, source-profile-uuid)
  attribute name, value string;
  # Enum: parametercardinalityenum (permitted values: one, one-or-more)
  attribute how-many, value string;
  # Enum: withchildcontrolsenum (permitted values: yes, no)
  attribute with-child-controls, value string;
  # Enum: combinationmethodenum (permitted values: use-first, merge, keep)
  attribute method, value string;
  # Enum: byitemnameenum (permitted values: param, prop, link, part, mapping, map)
  attribute by-item-name, value string;
  # Enum: additionpositionenum (permitted values: before, after, starting, ending)
  attribute position, value string;
  # Enum: insertorderenum (permitted values: keep, ascending, descending)
  attribute order, value string;
  # Enum: timingunitenum (permitted values: seconds, minutes, hours, days, months, years)
  attribute unit, value string;
  # Enum: componentstateenum (permitted values: under-development, operational, disposition, other)
  attribute state, value string;
  # Enum: transportenum (permitted values: TCP, UDP)
  attribute transport, value string;
  # Enum: privacydesignationenum (permitted values: yes, no)
  attribute value-attr, value string;
  # Enum: matchingrationaleenum (permitted values: syntactic, semantic, functional)
  attribute matching-rationale, value string;
  # Enum: mappingstatusenum (permitted values: complete, not-complete, draft, deprecated, superseded)
  attribute status, value string;
  # Enum: qualifiersubjectenum (permitted values: source, target, both)
  attribute subject, value string;
  # Enum: qualifierpredicateenum (permitted values: has-requirement, has-incompatibility)
  attribute predicate, value string;
  # Enum: qualifiercategoryenum (permitted values: restricted, addressable, blocked)
  attribute category, value string;

  # Entity types
  entity haspropsandlinks,
      plays props:haspropsandlinks,
      plays links-attr:haspropsandlinks;
  
  entity oscalcommon,
      owns remarks;
  
  entity hasresponsibleroles,
      plays responsible-roles:hasresponsibleroles;
  
  entity hasresponsibleparties,
      plays responsible-parties:hasresponsibleparties;
  
  entity oscaldocument;
  
  entity catalogdocument, sub oscaldocument,
      plays catalog-rel:catalogdocument;
  
  entity catalog,
      owns uuid @card(1..1),
      plays catalog-rel:catalog,
      plays metadata-rel:catalog,
      plays back-matter:catalog,
      plays params:catalog,
      plays controls:catalog,
      plays groups:catalog;
  
  entity group,
      owns id,
      owns -class,
      owns title @card(1..1),
      plays groups:group,
      plays params:group,
      plays parts:group,
      plays controls:group;
  
  entity control,
      owns id @card(1..1),
      owns -class,
      owns title @card(1..1),
      plays controls:control,
      plays params:control,
      plays parts:control;
  
  entity metadata,
      owns title @card(1..1),
      owns published,
      owns last-modified @card(1..1),
      owns version @card(1..1),
      owns oscal-version @card(1..1),
      plays metadata-rel:metadata,
      plays document-ids:metadata,
      plays revisions:metadata,
      plays roles:metadata,
      plays locations:metadata,
      plays parties:metadata,
      plays actions:metadata;
  
  entity revision,
      owns title,
      owns published,
      owns last-modified,
      owns version @card(1..1),
      owns oscal-version,
      plays revisions:revision;
  
  entity documentid,
      owns scheme,
      owns identifier @card(1..1),
      plays document-ids:documentid;
  
  entity role-attr,
      owns id @card(1..1),
      owns title @card(1..1),
      owns short-name,
      owns description,
      plays roles:role-attr;
  
  entity location,
      owns uuid @card(1..1),
      owns title,
      owns email-addresses @card(0..),
      owns urls @card(0..),
      plays locations:location,
      plays telephone-numbers:location,
      plays address-rel:location;
  
  entity party,
      owns uuid @card(1..1),
      owns type-attr @card(1..1),
      owns name,
      owns short-name,
      owns email-addresses @card(0..),
      owns location-uuids @card(0..),
      owns member-of-organizations @card(0..),
      plays parties:party,
      plays telephone-numbers:party,
      plays external-ids:party,
      plays addresses:party;
  
  entity partyexternalid,
      owns scheme @card(1..1),
      owns id @card(1..1);
  
  entity responsibleparty,
      owns role-id @card(1..1),
      owns party-uuids @card(0..),
      plays responsible-parties:responsibleparty;
  
  entity responsiblerole,
      owns role-id @card(1..1),
      owns party-uuids @card(0..),
      plays responsible-roles:responsiblerole;
  
  entity action,
      owns uuid @card(1..1),
      owns type-attr @card(1..1),
      owns date,
      owns system @card(1..1),
      plays actions:action;
  
  entity telephonenumber,
      owns type-attr,
      owns number @card(1..1),
      plays telephone-numbers:telephonenumber;
  
  entity address,
      owns type-attr,
      owns addr-lines @card(0..),
      owns city,
      owns state,
      owns postal-code,
      owns country,
      plays address-rel:address,
      plays addresses:address;
  
  entity hash,
      owns value-attr @card(1..1),
      owns algorithm @card(1..1),
      plays hashes:hash;
  
  entity property,
      owns name @card(1..1),
      owns uuid,
      owns ns,
      owns value-attr @card(1..1),
      owns -class,
      owns remarks,
      owns group-attr,
      plays props:property;
  
  entity metadataproperty, sub property;
  
  entity revisionproperty, sub property;
  
  entity locationproperty, sub property;
  
  entity partyproperty, sub property;
  
  entity resourceproperty, sub property,
      plays props:resourceproperty;
  
  entity partproperty, sub property;
  
  entity parameterproperty, sub property;
  
  entity metadatapartyexternalid, sub partyexternalid,
      plays external-ids:metadatapartyexternalid;
  
  entity link,
      owns href @card(1..1),
      owns rel,
      owns resource-fragment,
      owns media-type,
      owns text,
      plays links-attr:link;
  
  entity backmatter,
      plays back-matter:backmatter,
      plays resources:backmatter;
  
  entity resource,
      owns uuid @card(1..1),
      owns title,
      owns description,
      owns remarks,
      plays resources:resource,
      plays props:resource,
      plays document-ids:resource,
      plays citation-rel:resource,
      plays rlinks:resource,
      plays base64:resource;
  
  entity citation,
      owns text @card(1..1),
      plays citation-rel:citation;
  
  entity resourcelink,
      owns href @card(1..1),
      owns media-type,
      plays rlinks:resourcelink,
      plays hashes:resourcelink;
  
  entity base64resource,
      owns media-type,
      owns value-attr @card(1..1),
      owns filename,
      plays base64:base64resource;
  
  entity part,
      owns id,
      owns name @card(1..1),
      owns ns,
      owns -class,
      owns title,
      owns prose,
      plays parts:part;
  
  entity parameter,
      owns id @card(1..1),
      owns -class,
      owns depends-on,
      owns label-attr,
      owns usage,
      owns values @card(0..),
      plays params:parameter,
      plays constraints:parameter,
      plays guidelines:parameter,
      plays select-attr:parameter;
  
  entity parameterconstraint,
      owns description,
      plays constraints:parameterconstraint,
      plays tests:parameterconstraint;
  
  entity constrainttest,
      owns remarks,
      owns expression @card(1..1),
      plays tests:constrainttest;
  
  entity parameterguideline,
      owns prose @card(1..1),
      plays guidelines:parameterguideline;
  
  entity parameterselection,
      owns how-many,
      owns choice @card(0..),
      plays select-attr:parameterselection;
  
  entity includeall,
      plays include-all:includeall;
  
  entity controlmatching,
      owns remarks,
      owns pattern,
      plays matching:controlmatching;
  
  entity selectcontrolbyid,
      owns with-child-controls,
      owns with-ids @card(0..),
      plays matching:selectcontrolbyid,
      plays include-controls:selectcontrolbyid,
      plays exclude-controls:selectcontrolbyid,
      plays unmapped-controls:selectcontrolbyid;
  
  entity profiledocument, sub oscaldocument,
      plays profile-rel:profiledocument;
  
  entity profile,
      owns uuid @card(1..1),
      plays profile-rel:profile,
      plays metadata-rel:profile,
      plays imports:profile,
      plays merge:profile,
      plays modify:profile,
      plays back-matter:profile;
  
  entity profileimport,
      owns href @card(1..1),
      plays imports:profileimport,
      plays include-all:profileimport,
      plays include-controls:profileimport,
      plays exclude-controls:profileimport;
  
  entity profilemerge,
      owns as-is,
      plays merge:profilemerge,
      plays combine:profilemerge,
      plays flat:profilemerge,
      plays custom:profilemerge;
  
  entity combinationrule,
      owns method,
      plays combine:combinationrule;
  
  entity mergeflat,
      plays flat:mergeflat;
  
  entity mergecustom,
      plays custom:mergecustom,
      plays groups:mergecustom,
      plays insert-controls:mergecustom;
  
  entity profilegroup,
      owns id,
      owns -class,
      owns title @card(1..1),
      plays groups:profilegroup,
      plays params:profilegroup,
      plays parts:profilegroup,
      plays insert-controls:profilegroup;
  
  entity profilemodify,
      plays modify:profilemodify,
      plays set-parameters:profilemodify,
      plays alters:profilemodify;
  
  entity parametersetting,
      owns param-id @card(1..1),
      owns -class,
      owns depends-on,
      owns label-attr,
      owns usage,
      owns values @card(0..),
      plays set-parameters:parametersetting,
      plays constraints:parametersetting,
      plays guidelines:parametersetting,
      plays select-attr:parametersetting;
  
  entity alteration,
      owns control-id @card(1..1),
      plays alters:alteration,
      plays removes:alteration,
      plays adds:alteration;
  
  entity removal,
      owns by-name,
      owns by-class,
      owns by-id,
      owns by-item-name,
      owns by-ns,
      owns remarks,
      plays removes:removal;
  
  entity addition,
      owns position,
      owns by-id,
      owns title,
      plays adds:addition,
      plays params:addition,
      plays props:addition,
      plays links-attr:addition,
      plays parts:addition;
  
  entity profilealterationproperty, sub property,
      plays props:profilealterationproperty;
  
  entity insertcontrols,
      owns order,
      plays insert-controls:insertcontrols,
      plays include-all:insertcontrols,
      plays include-controls:insertcontrols,
      plays exclude-controls:insertcontrols;
  
  entity assessmentplandocument, sub oscaldocument,
      plays assessment-plan:assessmentplandocument;
  
  entity assessmentplan,
      owns uuid @card(1..1),
      plays assessment-plan:assessmentplan,
      plays metadata-rel:assessmentplan,
      plays import-ssp:assessmentplan,
      plays local-definitions:assessmentplan,
      plays terms-and-conditions:assessmentplan,
      plays assessment-subjects:assessmentplan,
      plays assessment-assets:assessmentplan,
      plays reviewed-controls:assessmentplan,
      plays tasks:assessmentplan,
      plays back-matter:assessmentplan;
  
  entity importssp,
      owns href @card(1..1),
      owns remarks,
      plays import-ssp:importssp;
  
  entity localdefinitions,
      owns remarks,
      plays local-definitions:localdefinitions,
      plays components:localdefinitions,
      plays inventory-items:localdefinitions,
      plays users:localdefinitions,
      plays objectives-and-methods:localdefinitions,
      plays activities:localdefinitions;
  
  entity termsandconditions,
      plays terms-and-conditions:termsandconditions,
      plays parts:termsandconditions;
  
  entity reviewedcontrols,
      owns description,
      plays reviewed-controls:reviewedcontrols,
      plays control-selections:reviewedcontrols,
      plays control-objective-selections:reviewedcontrols,
      plays related-controls:reviewedcontrols;
  
  entity controlselection,
      owns description,
      plays control-selections:controlselection,
      plays include-all:controlselection,
      plays include-controls:controlselection,
      plays exclude-controls:controlselection;
  
  entity controlobjectiveselection,
      owns description,
      plays control-objective-selections:controlobjectiveselection,
      plays include-all:controlobjectiveselection,
      plays include-objectives:controlobjectiveselection,
      plays exclude-objectives:controlobjectiveselection;
  
  entity assessmentselectcontrolbyid,
      owns control-id @card(1..1),
      owns statement-ids @card(0..),
      plays include-controls:assessmentselectcontrolbyid,
      plays exclude-controls:assessmentselectcontrolbyid;
  
  entity selectobjectivebyid,
      owns objective-id @card(1..1),
      owns remarks,
      plays include-objectives:selectobjectivebyid,
      plays exclude-objectives:selectobjectivebyid;
  
  entity assessmentsubject,
      owns type-attr @card(1..1),
      owns description,
      plays assessment-subjects:assessmentsubject,
      plays include-all:assessmentsubject,
      plays include-subjects:assessmentsubject,
      plays exclude-subjects:assessmentsubject,
      plays subjects:assessmentsubject;
  
  entity selectsubjectbyid,
      owns subject-uuid @card(1..1),
      owns type-attr @card(1..1),
      plays include-subjects:selectsubjectbyid,
      plays exclude-subjects:selectsubjectbyid;
  
  entity subjectreference,
      owns subject-uuid @card(1..1),
      owns type-attr @card(1..1),
      owns title,
      plays subjects:subjectreference;
  
  entity assessmentsubjectplaceholder,
      owns uuid @card(1..1),
      owns description,
      plays sources:assessmentsubjectplaceholder;
  
  entity assessmentsubjectsource,
      owns task-uuid @card(1..1),
      owns remarks,
      plays sources:assessmentsubjectsource;
  
  entity assessmentassets,
      plays assessment-assets:assessmentassets,
      plays components:assessmentassets,
      plays assessment-platforms:assessmentassets;
  
  entity assessmentplatform,
      owns uuid @card(1..1),
      owns title,
      plays assessment-platforms:assessmentplatform,
      plays uses-components:assessmentplatform;
  
  entity usescomponent,
      owns component-uuid @card(1..1),
      plays uses-components:usescomponent;
  
  entity localobjective,
      owns control-id @card(1..1),
      owns description,
      plays objectives-and-methods:localobjective,
      plays parts:localobjective;
  
  entity assessmentmethod,
      owns uuid @card(1..1),
      owns description,
      plays part-rel:assessmentmethod;
  
  entity activity,
      owns uuid @card(1..1),
      owns title,
      owns description @card(1..1),
      plays activities:activity,
      plays steps:activity,
      plays related-controls:activity;
  
  entity step,
      owns uuid @card(1..1),
      owns title,
      owns description @card(1..1),
      plays steps:step,
      plays reviewed-controls:step;
  
  entity task,
      owns uuid @card(1..1),
      owns type-attr @card(1..1),
      owns title @card(1..1),
      owns description,
      plays tasks:task,
      plays timing:task,
      plays dependencies:task,
      plays associated-activities:task,
      plays subjects:task;
  
  entity eventtiming,
      plays timing:eventtiming,
      plays on-date:eventtiming,
      plays within-date-range:eventtiming,
      plays at-frequency:eventtiming;
  
  entity ondatecondition,
      owns date @card(1..1),
      owns remarks,
      plays on-date:ondatecondition;
  
  entity withindaterange,
      owns start @card(1..1),
      owns end @card(1..1),
      owns remarks,
      plays within-date-range:withindaterange;
  
  entity atfrequency,
      owns period @card(1..1),
      owns unit @card(1..1),
      owns remarks,
      plays at-frequency:atfrequency;
  
  entity taskdependency,
      owns task-uuid @card(1..1),
      owns remarks,
      plays dependencies:taskdependency;
  
  entity associatedactivity,
      owns activity-uuid @card(1..1),
      plays associated-activities:associatedactivity,
      plays subjects:associatedactivity;
  
  entity assessmentpart,
      owns uuid,
      owns name @card(1..1),
      owns ns,
      owns -class,
      owns title,
      owns prose,
      plays part-rel:assessmentpart,
      plays parts:assessmentpart;
  
  entity termsandconditionspart, sub assessmentpart,
      plays parts:termsandconditionspart;
  
  entity controlpart,
      owns id,
      owns name @card(1..1),
      owns ns,
      owns -class,
      owns title,
      owns prose,
      plays parts:controlpart;
  
  entity setparameter,
      owns param-id @card(1..1),
      owns values @card(0..),
      owns remarks,
      plays set-parameters:setparameter;
  
  entity systemcomponent,
      owns uuid @card(1..1),
      owns type-attr @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns purpose,
      plays components:systemcomponent,
      plays protocols:systemcomponent,
      plays status-rel:systemcomponent;
  
  entity componentstatus,
      owns state @card(1..1),
      owns remarks,
      plays status-rel:componentstatus;
  
  entity protocol,
      owns uuid,
      owns name,
      owns title,
      plays protocols:protocol,
      plays port-ranges:protocol;
  
  entity portrange,
      owns start,
      owns end,
      owns transport,
      owns remarks,
      plays port-ranges:portrange;
  
  entity implementationstatus,
      owns state @card(1..1),
      owns remarks,
      plays implementation-status:implementationstatus;
  
  entity systemuser,
      owns uuid @card(1..1),
      owns title,
      owns short-name,
      owns description,
      owns role-ids @card(0..),
      plays users:systemuser,
      plays authorized-privileges:systemuser;
  
  entity authorizedprivilege,
      owns title @card(1..1),
      owns description,
      owns functions-performed @card(0..),
      plays authorized-privileges:authorizedprivilege;
  
  entity inventoryitem,
      owns uuid @card(1..1),
      owns description @card(1..1),
      plays inventory-items:inventoryitem,
      plays implemented-components:inventoryitem;
  
  entity implementedcomponent,
      owns component-uuid @card(1..1),
      plays implemented-components:implementedcomponent;
  
  entity systemid,
      owns id @card(1..1),
      owns identifier-type,
      plays system-ids:systemid,
      plays system-id:systemid;
  
  entity implementationcommonproperty, sub property;
  
  entity implementationcommonlink, sub link;
  
  entity implementationresponsiblerole, sub responsiblerole;
  
  entity implementationresponsibleparty, sub responsibleparty;
  
  entity origin,
      plays actors:origin,
      plays related-tasks:origin,
      plays origins:origin,
      plays origin-rel:origin;
  
  entity originactor,
      owns type-attr @card(1..1),
      owns actor-uuid @card(1..1),
      owns role-id,
      plays actors:originactor;
  
  entity relatedtask,
      owns task-uuid @card(1..1),
      plays related-tasks:relatedtask,
      plays subjects:relatedtask,
      plays identified-subject:relatedtask;
  
  entity identifiedsubject,
      owns subject-placeholder-uuid @card(1..1),
      plays identified-subject:identifiedsubject,
      plays subjects:identifiedsubject;
  
  entity observation,
      owns uuid @card(1..1),
      owns title,
      owns description @card(1..1),
      owns methods @card(0..),
      owns types @card(0..),
      owns collected @card(1..1),
      owns expires,
      plays origins:observation,
      plays subjects:observation,
      plays relevant-evidence:observation,
      plays observations:observation;
  
  entity relevantevidence,
      owns href,
      owns description @card(1..1),
      plays relevant-evidence:relevantevidence;
  
  entity finding,
      owns uuid @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns implementation-statement-uuid,
      plays target:finding,
      plays origins:finding,
      plays related-observations:finding,
      plays related-risks:finding,
      plays findings:finding;
  
  entity findingtarget,
      owns type-attr @card(1..1),
      owns target-id @card(1..1),
      owns title,
      owns description,
      plays target:findingtarget,
      plays implementation-status:findingtarget,
      plays status-rel:findingtarget;
  
  entity objectivestatus,
      owns state @card(1..1),
      owns reason,
      owns remarks,
      plays status-rel:objectivestatus;
  
  entity relatedobservation,
      owns observation-uuid @card(1..1),
      owns remarks,
      plays related-observations:relatedobservation;
  
  entity associatedrisk,
      owns risk-uuid @card(1..1),
      owns remarks,
      plays related-risks:associatedrisk;
  
  entity risk,
      owns uuid @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns statement @card(1..1),
      owns deadline,
      owns status @card(1..1),
      plays origins:risk,
      plays threat-ids:risk,
      plays characterizations:risk,
      plays mitigating-factors:risk,
      plays remediations:risk,
      plays risk-log:risk,
      plays related-observations:risk,
      plays risks:risk;
  
  entity threatid,
      owns href,
      owns system @card(1..1),
      owns id @card(1..1),
      plays threat-ids:threatid;
  
  entity characterization,
      plays characterizations:characterization,
      plays origin-rel:characterization,
      plays facets:characterization;
  
  entity facet,
      owns name @card(1..1),
      owns value-attr @card(1..1),
      owns system @card(1..1),
      plays facets:facet;
  
  entity mitigatingfactor,
      owns uuid @card(1..1),
      owns description @card(1..1),
      owns implementation-uuid,
      plays mitigating-factors:mitigatingfactor,
      plays subjects:mitigatingfactor;
  
  entity response,
      owns uuid @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns lifecycle @card(1..1),
      plays remediations:response,
      plays origins:response,
      plays required-assets:response,
      plays tasks:response;
  
  entity requiredasset,
      owns uuid @card(1..1),
      owns title,
      owns description @card(1..1),
      plays required-assets:requiredasset,
      plays subjects:requiredasset;
  
  entity risklog,
      plays risk-log:risklog,
      plays entries:risklog;
  
  entity risklogentry,
      owns uuid @card(1..1),
      owns title,
      owns description,
      owns start @card(1..1),
      owns end,
      owns status-change,
      plays entries:risklogentry,
      plays logged-by:risklogentry,
      plays related-responses:risklogentry;
  
  entity loggedby,
      owns party-uuid @card(1..1),
      owns role-id,
      owns remarks,
      plays logged-by:loggedby;
  
  entity riskresponsereference,
      owns response-uuid @card(1..1),
      plays related-responses:riskresponsereference,
      plays related-tasks:riskresponsereference;
  
  entity sspdocument, sub oscaldocument,
      plays system-security-plan:sspdocument;
  
  entity systemsecurityplan,
      owns uuid @card(1..1),
      plays system-security-plan:systemsecurityplan,
      plays metadata-rel:systemsecurityplan,
      plays import-profile:systemsecurityplan,
      plays system-characteristics:systemsecurityplan,
      plays system-implementation:systemsecurityplan,
      plays control-implementation:systemsecurityplan,
      plays back-matter:systemsecurityplan;
  
  entity importprofile,
      owns href @card(1..1),
      owns remarks,
      plays import-profile:importprofile;
  
  entity systemcharacteristics,
      owns system-name @card(1..1),
      owns system-name-short,
      owns description @card(1..1),
      owns date-authorized,
      owns security-sensitivity-level,
      owns remarks,
      plays system-characteristics:systemcharacteristics,
      plays system-ids:systemcharacteristics,
      plays system-information:systemcharacteristics,
      plays security-impact-level:systemcharacteristics,
      plays system-status:systemcharacteristics,
      plays authorization-boundary:systemcharacteristics,
      plays network-architecture:systemcharacteristics,
      plays data-flow:systemcharacteristics,
      plays responsible-parties:systemcharacteristics;
  
  entity systeminformation,
      plays system-information:systeminformation,
      plays props:systeminformation,
      plays links-attr:systeminformation,
      plays information-types:systeminformation;
  
  entity informationtype,
      owns uuid,
      owns title @card(1..1),
      owns description @card(1..1),
      plays information-types:informationtype,
      plays categorizations:informationtype,
      plays props:informationtype,
      plays links-attr:informationtype,
      plays confidentiality-impact:informationtype,
      plays integrity-impact:informationtype,
      plays availability-impact:informationtype;
  
  entity informationtypecategorization,
      plays categorizations:informationtypecategorization;
  
  entity impactlevel,
      owns base @card(1..1),
      owns selected,
      owns adjustment-justification,
      plays confidentiality-impact:impactlevel,
      plays integrity-impact:impactlevel,
      plays availability-impact:impactlevel,
      plays props:impactlevel,
      plays links-attr:impactlevel;
  
  entity securityimpactlevel,
      owns security-objective-confidentiality @card(1..1),
      owns security-objective-integrity @card(1..1),
      owns security-objective-availability @card(1..1),
      plays security-impact-level:securityimpactlevel;
  
  entity systemstatus,
      owns remarks,
      plays system-status:systemstatus;
  
  entity authorizationboundary,
      owns description @card(1..1),
      owns remarks,
      plays authorization-boundary:authorizationboundary,
      plays props:authorizationboundary,
      plays links-attr:authorizationboundary,
      plays diagrams:authorizationboundary;
  
  entity diagram,
      owns uuid @card(1..1),
      owns description,
      owns caption,
      owns remarks,
      plays diagrams:diagram,
      plays props:diagram,
      plays links-attr:diagram;
  
  entity networkarchitecture,
      owns description @card(1..1),
      owns remarks,
      plays network-architecture:networkarchitecture,
      plays props:networkarchitecture,
      plays links-attr:networkarchitecture,
      plays diagrams:networkarchitecture;
  
  entity dataflow,
      owns description @card(1..1),
      owns remarks,
      plays data-flow:dataflow,
      plays props:dataflow,
      plays links-attr:dataflow,
      plays diagrams:dataflow;
  
  entity systemimplementation,
      owns remarks,
      plays system-implementation:systemimplementation,
      plays props:systemimplementation,
      plays links-attr:systemimplementation,
      plays leveraged-authorizations:systemimplementation,
      plays users:systemimplementation,
      plays components:systemimplementation,
      plays inventory-items:systemimplementation;
  
  entity leveragedauthorization,
      owns uuid @card(1..1),
      owns title @card(1..1),
      owns party-uuid @card(1..1),
      owns date-authorized @card(1..1),
      owns remarks,
      plays leveraged-authorizations:leveragedauthorization,
      plays props:leveragedauthorization,
      plays links-attr:leveragedauthorization;
  
  entity sspcontrolimplementation,
      owns description @card(1..1),
      plays control-implementation:sspcontrolimplementation,
      plays set-parameters:sspcontrolimplementation,
      plays implemented-requirements:sspcontrolimplementation;
  
  entity sspimplementedrequirement,
      owns uuid @card(1..1),
      owns control-id @card(1..1),
      owns remarks,
      plays implemented-requirements:sspimplementedrequirement,
      plays props:sspimplementedrequirement,
      plays links-attr:sspimplementedrequirement,
      plays set-parameters:sspimplementedrequirement,
      plays responsible-roles:sspimplementedrequirement,
      plays statements:sspimplementedrequirement,
      plays by-components:sspimplementedrequirement;
  
  entity sspstatement,
      owns statement-id @card(1..1),
      owns uuid @card(1..1),
      owns remarks,
      plays statements:sspstatement,
      plays props:sspstatement,
      plays links-attr:sspstatement,
      plays responsible-roles:sspstatement,
      plays by-components:sspstatement;
  
  entity bycomponent,
      owns component-uuid @card(1..1),
      owns uuid @card(1..1),
      owns description @card(1..1),
      owns remarks,
      plays by-components:bycomponent,
      plays props:bycomponent,
      plays links-attr:bycomponent,
      plays set-parameters:bycomponent,
      plays implementation-status:bycomponent,
      plays export-rel:bycomponent,
      plays inherited:bycomponent,
      plays satisfied:bycomponent,
      plays responsible-roles:bycomponent;
  
  entity export,
      owns description,
      plays export-rel:export,
      plays props:export,
      plays links-attr:export,
      plays provided:export,
      plays responsibilities:export;
  
  entity providedcontrolimplementation,
      owns uuid @card(1..1),
      owns description @card(1..1),
      owns remarks,
      plays provided:providedcontrolimplementation,
      plays props:providedcontrolimplementation,
      plays links-attr:providedcontrolimplementation,
      plays responsible-roles:providedcontrolimplementation;
  
  entity controlresponsibility,
      owns uuid @card(1..1),
      owns provided-uuid,
      owns description @card(1..1),
      owns remarks,
      plays responsibilities:controlresponsibility,
      plays props:controlresponsibility,
      plays links-attr:controlresponsibility,
      plays responsible-roles:controlresponsibility;
  
  entity inheritedcontrolimplementation,
      owns uuid @card(1..1),
      owns provided-uuid,
      owns description @card(1..1),
      owns remarks,
      plays inherited:inheritedcontrolimplementation,
      plays props:inheritedcontrolimplementation,
      plays links-attr:inheritedcontrolimplementation,
      plays responsible-roles:inheritedcontrolimplementation;
  
  entity satisfiedcontrolimplementation,
      owns uuid @card(1..1),
      owns responsibility-uuid,
      owns description @card(1..1),
      owns remarks,
      plays satisfied:satisfiedcontrolimplementation,
      plays props:satisfiedcontrolimplementation,
      plays links-attr:satisfiedcontrolimplementation,
      plays responsible-roles:satisfiedcontrolimplementation;
  
  entity sspsystemcharacteristicsprop, sub property;
  
  entity sspsysteminformationprop, sub property,
      plays props:sspsysteminformationprop;
  
  entity sspcontroloriginationprop, sub property,
      plays props:sspcontroloriginationprop;
  
  entity sspallowsauthenticatedscanprop, sub property;
  
  entity sspsysteminformationlink, sub link,
      plays links-attr:sspsysteminformationlink;
  
  entity sspdiagramlink, sub link,
      plays links-attr:sspdiagramlink;
  
  entity sspleveragedauthorizationlink, sub link,
      plays links-attr:sspleveragedauthorizationlink;
  
  entity sspbycomponentlink, sub link,
      plays links-attr:sspbycomponentlink;
  
  entity sspsystemcharacteristicsresponsibleparty, sub responsibleparty,
      plays responsible-parties:sspsystemcharacteristicsresponsibleparty;
  
  entity sspimplementedrequirementresponsiblerole, sub responsiblerole,
      plays responsible-roles:sspimplementedrequirementresponsiblerole;
  
  entity sspbycomponentresponsiblerole, sub responsiblerole,
      plays responsible-roles:sspbycomponentresponsiblerole;
  
  entity sspsystemcomponent, sub systemcomponent,
      plays components:sspsystemcomponent;
  
  entity sspinventoryitem, sub inventoryitem,
      plays inventory-items:sspinventoryitem;
  
  entity assessmentresultsdocument, sub oscaldocument,
      plays assessment-results:assessmentresultsdocument;
  
  entity assessmentresults,
      owns uuid @card(1..1),
      plays assessment-results:assessmentresults,
      plays metadata-rel:assessmentresults,
      plays import-ap:assessmentresults,
      plays local-definitions:assessmentresults,
      plays results:assessmentresults,
      plays back-matter:assessmentresults;
  
  entity importassessmentplan,
      owns href @card(1..1),
      owns remarks,
      plays import-ap:importassessmentplan;
  
  entity assessmentresultslocaldefinitions,
      owns remarks,
      plays local-definitions:assessmentresultslocaldefinitions,
      plays objectives-and-methods:assessmentresultslocaldefinitions,
      plays activities:assessmentresultslocaldefinitions;
  
  entity result,
      owns uuid @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns start @card(1..1),
      owns end,
      plays results:result,
      plays local-definitions:result,
      plays reviewed-controls:result,
      plays attestations:result,
      plays assessment-log:result,
      plays observations:result,
      plays risks:result,
      plays findings:result;
  
  entity resultlocaldefinitions,
      plays local-definitions:resultlocaldefinitions,
      plays components:resultlocaldefinitions,
      plays inventory-items:resultlocaldefinitions,
      plays users:resultlocaldefinitions,
      plays assessment-assets:resultlocaldefinitions,
      plays tasks:resultlocaldefinitions;
  
  entity attestation,
      plays attestations:attestation,
      plays parts:attestation;
  
  entity assessmentlog,
      plays assessment-log:assessmentlog,
      plays entries:assessmentlog;
  
  entity assessmentlogentry,
      owns uuid @card(1..1),
      owns title,
      owns description,
      owns start @card(1..1),
      owns end,
      plays entries:assessmentlogentry,
      plays logged-by:assessmentlogentry,
      plays related-tasks:assessmentlogentry;
  
  entity componentdefinitiondocument, sub oscaldocument,
      plays component-definition:componentdefinitiondocument;
  
  entity componentdefinition,
      owns uuid @card(1..1),
      plays component-definition:componentdefinition,
      plays metadata-rel:componentdefinition,
      plays import-component-definitions:componentdefinition,
      plays components:componentdefinition,
      plays capabilities:componentdefinition,
      plays back-matter:componentdefinition;
  
  entity importcomponentdefinition,
      owns href @card(1..1),
      owns remarks,
      plays import-component-definitions:importcomponentdefinition;
  
  entity definedcomponent,
      owns uuid @card(1..1),
      owns type-attr @card(1..1),
      owns title @card(1..1),
      owns description @card(1..1),
      owns purpose,
      plays components:definedcomponent,
      plays protocols:definedcomponent,
      plays control-implementations:definedcomponent;
  
  entity capability,
      owns uuid @card(1..1),
      owns name @card(1..1),
      owns description @card(1..1),
      plays capabilities:capability,
      plays incorporates-components:capability,
      plays control-implementations:capability;
  
  entity incorporatescomponent,
      owns component-uuid @card(1..1),
      owns description @card(1..1),
      plays incorporates-components:incorporatescomponent;
  
  entity controlimplementationset,
      owns uuid @card(1..1),
      owns source @card(1..1),
      owns description @card(1..1),
      plays control-implementations:controlimplementationset,
      plays set-parameters:controlimplementationset,
      plays implemented-requirements:controlimplementationset;
  
  entity implementedrequirement,
      owns uuid @card(1..1),
      owns control-id @card(1..1),
      owns description @card(1..1),
      owns remarks,
      plays implemented-requirements:implementedrequirement,
      plays set-parameters:implementedrequirement,
      plays statements:implementedrequirement;
  
  entity implementedcontrolstatement,
      owns statement-id @card(1..1),
      owns uuid @card(1..1),
      owns description @card(1..1),
      owns remarks,
      plays statements:implementedcontrolstatement;
  
  entity mappingcollectiondocument, sub oscaldocument,
      plays mapping-collection:mappingcollectiondocument;
  
  entity mappingcollection,
      owns uuid @card(1..1),
      plays mapping-collection:mappingcollection,
      plays metadata-rel:mappingcollection,
      plays provenance:mappingcollection,
      plays mappings:mappingcollection,
      plays back-matter:mappingcollection;
  
  entity mappingprovenance,
      owns method @card(1..1),
      owns matching-rationale @card(1..1),
      owns status @card(1..1),
      owns mapping-description @card(1..1),
      plays provenance:mappingprovenance,
      plays confidence-score:mappingprovenance,
      plays coverage-rel:mappingprovenance;
  
  entity mapping,
      owns uuid @card(1..1),
      owns method,
      owns matching-rationale,
      owns status,
      owns mapping-description,
      plays mappings:mapping,
      plays source-resource:mapping,
      plays target-resource:mapping,
      plays maps:mapping,
      plays source-gap-summary:mapping,
      plays target-gap-summary:mapping,
      plays confidence-score:mapping,
      plays coverage-rel:mapping;
  
  entity map,
      owns uuid @card(1..1),
      owns ns,
      owns matching-rationale,
      owns relationship @card(1..1),
      plays maps:map,
      plays sources:map,
      plays targets:map,
      plays qualifiers:map,
      plays confidence-score:map,
      plays coverage-rel:map;
  
  entity mappingitem,
      owns type-attr @card(1..1),
      owns id-ref @card(1..1),
      plays sources:mappingitem,
      plays targets:mappingitem;
  
  entity mappingresourcereference,
      owns ns,
      owns type-attr @card(1..1),
      owns href @card(1..1),
      plays source-resource:mappingresourcereference,
      plays target-resource:mappingresourcereference;
  
  entity qualifieritem,
      owns subject @card(1..1),
      owns predicate @card(1..1),
      owns category @card(1..1),
      owns description @card(1..1),
      owns remarks,
      plays qualifiers:qualifieritem;
  
  entity gapsummary,
      owns uuid @card(1..1),
      plays source-gap-summary:gapsummary,
      plays target-gap-summary:gapsummary,
      plays unmapped-controls:gapsummary;
  
  entity confidencescore,
      owns category,
      owns percentage,
      plays confidence-score:confidencescore;
  
  entity coverage,
      owns generation-method,
      owns target-coverage @card(1..1),
      plays coverage-rel:coverage;
  
  entity poamdocument, sub oscaldocument,
      plays plan-of-action-and-milestones:poamdocument;
  
  entity planofactionandmilestones,
      owns uuid @card(1..1),
      plays plan-of-action-and-milestones:planofactionandmilestones,
      plays metadata-rel:planofactionandmilestones,
      plays import-ssp:planofactionandmilestones,
      plays system-id:planofactionandmilestones,
      plays local-definitions:planofactionandmilestones,
      plays observations:planofactionandmilestones,
      plays risks:planofactionandmilestones,
      plays findings:planofactionandmilestones,
      plays poam-items:planofactionandmilestones,
      plays back-matter:planofactionandmilestones;
  
  entity poamlocaldefinitions,
      owns remarks,
      plays local-definitions:poamlocaldefinitions,
      plays components:poamlocaldefinitions,
      plays inventory-items:poamlocaldefinitions,
      plays assessment-assets:poamlocaldefinitions;
  
  entity poamitem,
      owns uuid,
      owns title @card(1..1),
      owns description @card(1..1),
      plays poam-items:poamitem,
      plays origins:poamitem,
      plays related-findings:poamitem,
      plays related-observations:poamitem,
      plays related-risks:poamitem;
  
  entity relatedfinding,
      owns finding-uuid @card(1..1),
      owns remarks,
      plays related-findings:relatedfinding;
  

  # Relations (from object-ranged slots)
  relation props,
      relates haspropsandlinks,
      relates property,
      relates resource,
      relates resourceproperty,
      relates addition,
      relates profilealterationproperty,
      relates systeminformation,
      relates sspsysteminformationprop,
      relates informationtype,
      relates impactlevel,
      relates authorizationboundary,
      relates diagram,
      relates networkarchitecture,
      relates dataflow,
      relates systemimplementation,
      relates leveragedauthorization,
      relates sspimplementedrequirement,
      relates sspcontroloriginationprop,
      relates sspstatement,
      relates bycomponent,
      relates export,
      relates providedcontrolimplementation,
      relates controlresponsibility,
      relates inheritedcontrolimplementation,
      relates satisfiedcontrolimplementation;
  
  relation links-attr,
      relates haspropsandlinks,
      relates link,
      relates addition,
      relates systeminformation,
      relates sspsysteminformationlink,
      relates informationtype,
      relates impactlevel,
      relates authorizationboundary,
      relates diagram,
      relates sspdiagramlink,
      relates networkarchitecture,
      relates dataflow,
      relates systemimplementation,
      relates leveragedauthorization,
      relates sspleveragedauthorizationlink,
      relates sspimplementedrequirement,
      relates sspstatement,
      relates bycomponent,
      relates sspbycomponentlink,
      relates export,
      relates providedcontrolimplementation,
      relates controlresponsibility,
      relates inheritedcontrolimplementation,
      relates satisfiedcontrolimplementation;
  
  relation responsible-roles,
      relates hasresponsibleroles,
      relates responsiblerole,
      relates sspimplementedrequirement,
      relates sspimplementedrequirementresponsiblerole,
      relates sspstatement,
      relates bycomponent,
      relates sspbycomponentresponsiblerole,
      relates providedcontrolimplementation,
      relates controlresponsibility,
      relates inheritedcontrolimplementation,
      relates satisfiedcontrolimplementation;
  
  relation responsible-parties,
      relates hasresponsibleparties,
      relates responsibleparty,
      relates systemcharacteristics,
      relates sspsystemcharacteristicsresponsibleparty;
  
  relation catalog-rel,
      relates catalogdocument,
      relates catalog;
  
  relation metadata-rel,
      relates catalog,
      relates metadata,
      relates profile,
      relates assessmentplan,
      relates systemsecurityplan,
      relates assessmentresults,
      relates componentdefinition,
      relates mappingcollection,
      relates planofactionandmilestones;
  
  relation back-matter,
      relates catalog,
      relates backmatter,
      relates profile,
      relates assessmentplan,
      relates systemsecurityplan,
      relates assessmentresults,
      relates componentdefinition,
      relates mappingcollection,
      relates planofactionandmilestones;
  
  relation params,
      relates catalog,
      relates parameter,
      relates group,
      relates control,
      relates profilegroup,
      relates addition;
  
  relation controls,
      relates catalog,
      relates control,
      relates group;
  
  relation groups,
      relates catalog,
      relates group,
      relates mergecustom,
      relates profilegroup;
  
  relation parts,
      relates group,
      relates part,
      relates control,
      relates profilegroup,
      relates addition,
      relates termsandconditions,
      relates termsandconditionspart,
      relates localobjective,
      relates controlpart,
      relates assessmentpart,
      relates attestation;
  
  relation document-ids,
      relates metadata,
      relates documentid,
      relates resource;
  
  relation revisions,
      relates metadata,
      relates revision;
  
  relation roles,
      relates metadata,
      relates role-attr;
  
  relation locations,
      relates metadata,
      relates location;
  
  relation parties,
      relates metadata,
      relates party;
  
  relation actions,
      relates metadata,
      relates action;
  
  relation telephone-numbers,
      relates location,
      relates telephonenumber,
      relates party;
  
  relation address-rel,
      relates location,
      relates address;
  
  relation external-ids,
      relates party,
      relates metadatapartyexternalid;
  
  relation addresses,
      relates party,
      relates address;
  
  relation resources,
      relates backmatter,
      relates resource;
  
  relation citation-rel,
      relates resource,
      relates citation;
  
  relation rlinks,
      relates resource,
      relates resourcelink;
  
  relation base64,
      relates resource,
      relates base64resource;
  
  relation hashes,
      relates resourcelink,
      relates hash;
  
  relation constraints,
      relates parameter,
      relates parameterconstraint,
      relates parametersetting;
  
  relation guidelines,
      relates parameter,
      relates parameterguideline,
      relates parametersetting;
  
  relation select-attr,
      relates parameter,
      relates parameterselection,
      relates parametersetting;
  
  relation tests,
      relates parameterconstraint,
      relates constrainttest;
  
  relation matching,
      relates selectcontrolbyid,
      relates controlmatching;
  
  relation profile-rel,
      relates profiledocument,
      relates profile;
  
  relation imports,
      relates profile,
      relates profileimport;
  
  relation merge,
      relates profile,
      relates profilemerge;
  
  relation modify,
      relates profile,
      relates profilemodify;
  
  relation include-all,
      relates profileimport,
      relates includeall,
      relates insertcontrols,
      relates controlselection,
      relates controlobjectiveselection,
      relates assessmentsubject;
  
  relation include-controls,
      relates profileimport,
      relates selectcontrolbyid,
      relates insertcontrols,
      relates controlselection,
      relates assessmentselectcontrolbyid;
  
  relation exclude-controls,
      relates profileimport,
      relates selectcontrolbyid,
      relates insertcontrols,
      relates controlselection,
      relates assessmentselectcontrolbyid;
  
  relation combine,
      relates profilemerge,
      relates combinationrule;
  
  relation flat,
      relates profilemerge,
      relates mergeflat;
  
  relation custom,
      relates profilemerge,
      relates mergecustom;
  
  relation insert-controls,
      relates mergecustom,
      relates insertcontrols,
      relates profilegroup;
  
  relation set-parameters,
      relates profilemodify,
      relates parametersetting,
      relates sspcontrolimplementation,
      relates setparameter,
      relates sspimplementedrequirement,
      relates bycomponent,
      relates controlimplementationset,
      relates implementedrequirement;
  
  relation alters,
      relates profilemodify,
      relates alteration;
  
  relation removes,
      relates alteration,
      relates removal;
  
  relation adds,
      relates alteration,
      relates addition;
  
  relation assessment-plan,
      relates assessmentplandocument,
      relates assessmentplan;
  
  relation import-ssp,
      relates assessmentplan,
      relates importssp,
      relates planofactionandmilestones;
  
  relation local-definitions,
      relates assessmentplan,
      relates localdefinitions,
      relates assessmentresults,
      relates assessmentresultslocaldefinitions,
      relates result,
      relates resultlocaldefinitions,
      relates planofactionandmilestones,
      relates poamlocaldefinitions;
  
  relation terms-and-conditions,
      relates assessmentplan,
      relates termsandconditions;
  
  relation assessment-subjects,
      relates assessmentplan,
      relates assessmentsubject;
  
  relation assessment-assets,
      relates assessmentplan,
      relates assessmentassets,
      relates resultlocaldefinitions,
      relates poamlocaldefinitions;
  
  relation reviewed-controls,
      relates assessmentplan,
      relates reviewedcontrols,
      relates step,
      relates result;
  
  relation tasks,
      relates assessmentplan,
      relates task,
      relates response,
      relates resultlocaldefinitions;
  
  relation components,
      relates localdefinitions,
      relates systemcomponent,
      relates assessmentassets,
      relates systemimplementation,
      relates sspsystemcomponent,
      relates resultlocaldefinitions,
      relates componentdefinition,
      relates definedcomponent,
      relates poamlocaldefinitions;
  
  relation inventory-items,
      relates localdefinitions,
      relates inventoryitem,
      relates systemimplementation,
      relates sspinventoryitem,
      relates resultlocaldefinitions,
      relates poamlocaldefinitions;
  
  relation users,
      relates localdefinitions,
      relates systemuser,
      relates systemimplementation,
      relates resultlocaldefinitions;
  
  relation objectives-and-methods,
      relates localdefinitions,
      relates localobjective,
      relates assessmentresultslocaldefinitions;
  
  relation activities,
      relates localdefinitions,
      relates activity,
      relates assessmentresultslocaldefinitions;
  
  relation control-selections,
      relates reviewedcontrols,
      relates controlselection;
  
  relation control-objective-selections,
      relates reviewedcontrols,
      relates controlobjectiveselection;
  
  relation include-objectives,
      relates controlobjectiveselection,
      relates selectobjectivebyid;
  
  relation exclude-objectives,
      relates controlobjectiveselection,
      relates selectobjectivebyid;
  
  relation include-subjects,
      relates assessmentsubject,
      relates selectsubjectbyid;
  
  relation exclude-subjects,
      relates assessmentsubject,
      relates selectsubjectbyid;
  
  relation sources,
      relates assessmentsubjectplaceholder,
      relates assessmentsubjectsource,
      relates map,
      relates mappingitem;
  
  relation assessment-platforms,
      relates assessmentassets,
      relates assessmentplatform;
  
  relation uses-components,
      relates assessmentplatform,
      relates usescomponent;
  
  relation part-rel,
      relates assessmentmethod,
      relates assessmentpart;
  
  relation steps,
      relates activity,
      relates step;
  
  relation related-controls,
      relates activity,
      relates reviewedcontrols;
  
  relation timing,
      relates task,
      relates eventtiming;
  
  relation dependencies,
      relates task,
      relates taskdependency;
  
  relation associated-activities,
      relates task,
      relates associatedactivity;
  
  relation subjects,
      relates task,
      relates assessmentsubject,
      relates associatedactivity,
      relates relatedtask,
      relates identifiedsubject,
      relates observation,
      relates subjectreference,
      relates mitigatingfactor,
      relates requiredasset;
  
  relation on-date,
      relates eventtiming,
      relates ondatecondition;
  
  relation within-date-range,
      relates eventtiming,
      relates withindaterange;
  
  relation at-frequency,
      relates eventtiming,
      relates atfrequency;
  
  relation protocols,
      relates systemcomponent,
      relates protocol,
      relates definedcomponent;
  
  relation status-rel,
      relates systemcomponent,
      relates componentstatus,
      relates findingtarget,
      relates objectivestatus;
  
  relation port-ranges,
      relates protocol,
      relates portrange;
  
  relation authorized-privileges,
      relates systemuser,
      relates authorizedprivilege;
  
  relation implemented-components,
      relates inventoryitem,
      relates implementedcomponent;
  
  relation actors,
      relates origin,
      relates originactor;
  
  relation related-tasks,
      relates origin,
      relates relatedtask,
      relates riskresponsereference,
      relates assessmentlogentry;
  
  relation identified-subject,
      relates relatedtask,
      relates identifiedsubject;
  
  relation origins,
      relates observation,
      relates origin,
      relates finding,
      relates risk,
      relates response,
      relates poamitem;
  
  relation relevant-evidence,
      relates observation,
      relates relevantevidence;
  
  relation target,
      relates finding,
      relates findingtarget;
  
  relation related-observations,
      relates finding,
      relates relatedobservation,
      relates risk,
      relates poamitem;
  
  relation related-risks,
      relates finding,
      relates associatedrisk,
      relates poamitem;
  
  relation implementation-status,
      relates findingtarget,
      relates implementationstatus,
      relates bycomponent;
  
  relation threat-ids,
      relates risk,
      relates threatid;
  
  relation characterizations,
      relates risk,
      relates characterization;
  
  relation mitigating-factors,
      relates risk,
      relates mitigatingfactor;
  
  relation remediations,
      relates risk,
      relates response;
  
  relation risk-log,
      relates risk,
      relates risklog;
  
  relation origin-rel,
      relates characterization,
      relates origin;
  
  relation facets,
      relates characterization,
      relates facet;
  
  relation required-assets,
      relates response,
      relates requiredasset;
  
  relation entries,
      relates risklog,
      relates risklogentry,
      relates assessmentlog,
      relates assessmentlogentry;
  
  relation logged-by,
      relates risklogentry,
      relates loggedby,
      relates assessmentlogentry;
  
  relation related-responses,
      relates risklogentry,
      relates riskresponsereference;
  
  relation system-security-plan,
      relates sspdocument,
      relates systemsecurityplan;
  
  relation import-profile,
      relates systemsecurityplan,
      relates importprofile;
  
  relation system-characteristics,
      relates systemsecurityplan,
      relates systemcharacteristics;
  
  relation system-implementation,
      relates systemsecurityplan,
      relates systemimplementation;
  
  relation control-implementation,
      relates systemsecurityplan,
      relates sspcontrolimplementation;
  
  relation system-ids,
      relates systemcharacteristics,
      relates systemid;
  
  relation system-information,
      relates systemcharacteristics,
      relates systeminformation;
  
  relation security-impact-level,
      relates systemcharacteristics,
      relates securityimpactlevel;
  
  relation system-status,
      relates systemcharacteristics,
      relates systemstatus;
  
  relation authorization-boundary,
      relates systemcharacteristics,
      relates authorizationboundary;
  
  relation network-architecture,
      relates systemcharacteristics,
      relates networkarchitecture;
  
  relation data-flow,
      relates systemcharacteristics,
      relates dataflow;
  
  relation information-types,
      relates systeminformation,
      relates informationtype;
  
  relation categorizations,
      relates informationtype,
      relates informationtypecategorization;
  
  relation confidentiality-impact,
      relates informationtype,
      relates impactlevel;
  
  relation integrity-impact,
      relates informationtype,
      relates impactlevel;
  
  relation availability-impact,
      relates informationtype,
      relates impactlevel;
  
  relation diagrams,
      relates authorizationboundary,
      relates diagram,
      relates networkarchitecture,
      relates dataflow;
  
  relation leveraged-authorizations,
      relates systemimplementation,
      relates leveragedauthorization;
  
  relation implemented-requirements,
      relates sspcontrolimplementation,
      relates sspimplementedrequirement,
      relates controlimplementationset,
      relates implementedrequirement;
  
  relation statements,
      relates sspimplementedrequirement,
      relates sspstatement,
      relates implementedrequirement,
      relates implementedcontrolstatement;
  
  relation by-components,
      relates sspimplementedrequirement,
      relates bycomponent,
      relates sspstatement;
  
  relation export-rel,
      relates bycomponent,
      relates export;
  
  relation inherited,
      relates bycomponent,
      relates inheritedcontrolimplementation;
  
  relation satisfied,
      relates bycomponent,
      relates satisfiedcontrolimplementation;
  
  relation provided,
      relates export,
      relates providedcontrolimplementation;
  
  relation responsibilities,
      relates export,
      relates controlresponsibility;
  
  relation assessment-results,
      relates assessmentresultsdocument,
      relates assessmentresults;
  
  relation import-ap,
      relates assessmentresults,
      relates importassessmentplan;
  
  relation results,
      relates assessmentresults,
      relates result;
  
  relation attestations,
      relates result,
      relates attestation;
  
  relation assessment-log,
      relates result,
      relates assessmentlog;
  
  relation observations,
      relates result,
      relates observation,
      relates planofactionandmilestones;
  
  relation risks,
      relates result,
      relates risk,
      relates planofactionandmilestones;
  
  relation findings,
      relates result,
      relates finding,
      relates planofactionandmilestones;
  
  relation component-definition,
      relates componentdefinitiondocument,
      relates componentdefinition;
  
  relation import-component-definitions,
      relates componentdefinition,
      relates importcomponentdefinition;
  
  relation capabilities,
      relates componentdefinition,
      relates capability;
  
  relation control-implementations,
      relates definedcomponent,
      relates controlimplementationset,
      relates capability;
  
  relation incorporates-components,
      relates capability,
      relates incorporatescomponent;
  
  relation mapping-collection,
      relates mappingcollectiondocument,
      relates mappingcollection;
  
  relation provenance,
      relates mappingcollection,
      relates mappingprovenance;
  
  relation mappings,
      relates mappingcollection,
      relates mapping;
  
  relation confidence-score,
      relates mappingprovenance,
      relates confidencescore,
      relates mapping,
      relates map;
  
  relation coverage-rel,
      relates mappingprovenance,
      relates coverage,
      relates mapping,
      relates map;
  
  relation source-resource,
      relates mapping,
      relates mappingresourcereference;
  
  relation target-resource,
      relates mapping,
      relates mappingresourcereference;
  
  relation maps,
      relates mapping,
      relates map;
  
  relation source-gap-summary,
      relates mapping,
      relates gapsummary;
  
  relation target-gap-summary,
      relates mapping,
      relates gapsummary;
  
  relation targets,
      relates map,
      relates mappingitem;
  
  relation qualifiers,
      relates map,
      relates qualifieritem;
  
  relation unmapped-controls,
      relates gapsummary,
      relates selectcontrolbyid;
  
  relation plan-of-action-and-milestones,
      relates poamdocument,
      relates planofactionandmilestones;
  
  relation system-id,
      relates planofactionandmilestones,
      relates systemid;
  
  relation poam-items,
      relates planofactionandmilestones,
      relates poamitem;
  
  relation related-findings,
      relates poamitem,
      relates relatedfinding;
  

