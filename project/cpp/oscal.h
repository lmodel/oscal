// Code generated by linkml gen-cpp-header. DO NOT EDIT.
#pragma once

#include <cstdint>
#include <cstring>
#include <optional>
#include <string>
#include <vector>


namespace oscal {

enum class PartyTypeEnum {
    PERSON,
    ORGANIZATION
};

/// Convert PartyTypeEnum to its string representation.
inline const char* to_string(PartyTypeEnum value) {
    switch (value) {
        case PartyTypeEnum::PERSON: return "person";
        case PartyTypeEnum::ORGANIZATION: return "organization";
    }
    return "unknown";
}

/// Parse a string into PartyTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PartyTypeEnum& out) {
    if (std::strcmp(str, "person") == 0) { out = PartyTypeEnum::PERSON; return true; }
    if (std::strcmp(str, "organization") == 0) { out = PartyTypeEnum::ORGANIZATION; return true; }
    return false;
}

/// Curated hash algorithm values. Other values are permitted (OSCAL allow-other="yes").
enum class HashAlgorithmEnum {
    SHA-224,
    SHA-256,
    SHA-384,
    SHA-512,
    SHA3-224,
    SHA3-256,
    SHA3-384,
    SHA3-512
};

/// Convert HashAlgorithmEnum to its string representation.
inline const char* to_string(HashAlgorithmEnum value) {
    switch (value) {
        case HashAlgorithmEnum::SHA-224: return "SHA-224";
        case HashAlgorithmEnum::SHA-256: return "SHA-256";
        case HashAlgorithmEnum::SHA-384: return "SHA-384";
        case HashAlgorithmEnum::SHA-512: return "SHA-512";
        case HashAlgorithmEnum::SHA3-224: return "SHA3-224";
        case HashAlgorithmEnum::SHA3-256: return "SHA3-256";
        case HashAlgorithmEnum::SHA3-384: return "SHA3-384";
        case HashAlgorithmEnum::SHA3-512: return "SHA3-512";
    }
    return "unknown";
}

/// Parse a string into HashAlgorithmEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, HashAlgorithmEnum& out) {
    if (std::strcmp(str, "SHA-224") == 0) { out = HashAlgorithmEnum::SHA-224; return true; }
    if (std::strcmp(str, "SHA-256") == 0) { out = HashAlgorithmEnum::SHA-256; return true; }
    if (std::strcmp(str, "SHA-384") == 0) { out = HashAlgorithmEnum::SHA-384; return true; }
    if (std::strcmp(str, "SHA-512") == 0) { out = HashAlgorithmEnum::SHA-512; return true; }
    if (std::strcmp(str, "SHA3-224") == 0) { out = HashAlgorithmEnum::SHA3-224; return true; }
    if (std::strcmp(str, "SHA3-256") == 0) { out = HashAlgorithmEnum::SHA3-256; return true; }
    if (std::strcmp(str, "SHA3-384") == 0) { out = HashAlgorithmEnum::SHA3-384; return true; }
    if (std::strcmp(str, "SHA3-512") == 0) { out = HashAlgorithmEnum::SHA3-512; return true; }
    return false;
}

/// Curated telephone number type values. Other values are permitted (OSCAL allow-other="yes").
enum class PhoneTypeEnum {
    HOME,
    OFFICE,
    MOBILE
};

/// Convert PhoneTypeEnum to its string representation.
inline const char* to_string(PhoneTypeEnum value) {
    switch (value) {
        case PhoneTypeEnum::HOME: return "home";
        case PhoneTypeEnum::OFFICE: return "office";
        case PhoneTypeEnum::MOBILE: return "mobile";
    }
    return "unknown";
}

/// Parse a string into PhoneTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PhoneTypeEnum& out) {
    if (std::strcmp(str, "home") == 0) { out = PhoneTypeEnum::HOME; return true; }
    if (std::strcmp(str, "office") == 0) { out = PhoneTypeEnum::OFFICE; return true; }
    if (std::strcmp(str, "mobile") == 0) { out = PhoneTypeEnum::MOBILE; return true; }
    return false;
}

/// Curated address type values. Other values are permitted (OSCAL allow-other="yes").
enum class AddressTypeEnum {
    HOME,
    WORK
};

/// Convert AddressTypeEnum to its string representation.
inline const char* to_string(AddressTypeEnum value) {
    switch (value) {
        case AddressTypeEnum::HOME: return "home";
        case AddressTypeEnum::WORK: return "work";
    }
    return "unknown";
}

/// Parse a string into AddressTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AddressTypeEnum& out) {
    if (std::strcmp(str, "home") == 0) { out = AddressTypeEnum::HOME; return true; }
    if (std::strcmp(str, "work") == 0) { out = AddressTypeEnum::WORK; return true; }
    return false;
}

/// Curated metadata link relation values. Other values are permitted (OSCAL allow-other="yes").
enum class MetadataLinkRelEnum {
    CANONICAL,
    ALTERNATE,
    LATEST-VERSION,
    PREDECESSOR-VERSION,
    SUCCESSOR-VERSION,
    VERSION-HISTORY,
    REFERENCE,
    SOURCE-PROFILE,
    SOURCE-PROFILE-UUID
};

/// Convert MetadataLinkRelEnum to its string representation.
inline const char* to_string(MetadataLinkRelEnum value) {
    switch (value) {
        case MetadataLinkRelEnum::CANONICAL: return "canonical";
        case MetadataLinkRelEnum::ALTERNATE: return "alternate";
        case MetadataLinkRelEnum::LATEST-VERSION: return "latest-version";
        case MetadataLinkRelEnum::PREDECESSOR-VERSION: return "predecessor-version";
        case MetadataLinkRelEnum::SUCCESSOR-VERSION: return "successor-version";
        case MetadataLinkRelEnum::VERSION-HISTORY: return "version-history";
        case MetadataLinkRelEnum::REFERENCE: return "reference";
        case MetadataLinkRelEnum::SOURCE-PROFILE: return "source-profile";
        case MetadataLinkRelEnum::SOURCE-PROFILE-UUID: return "source-profile-uuid";
    }
    return "unknown";
}

/// Parse a string into MetadataLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MetadataLinkRelEnum& out) {
    if (std::strcmp(str, "canonical") == 0) { out = MetadataLinkRelEnum::CANONICAL; return true; }
    if (std::strcmp(str, "alternate") == 0) { out = MetadataLinkRelEnum::ALTERNATE; return true; }
    if (std::strcmp(str, "latest-version") == 0) { out = MetadataLinkRelEnum::LATEST-VERSION; return true; }
    if (std::strcmp(str, "predecessor-version") == 0) { out = MetadataLinkRelEnum::PREDECESSOR-VERSION; return true; }
    if (std::strcmp(str, "successor-version") == 0) { out = MetadataLinkRelEnum::SUCCESSOR-VERSION; return true; }
    if (std::strcmp(str, "version-history") == 0) { out = MetadataLinkRelEnum::VERSION-HISTORY; return true; }
    if (std::strcmp(str, "reference") == 0) { out = MetadataLinkRelEnum::REFERENCE; return true; }
    if (std::strcmp(str, "source-profile") == 0) { out = MetadataLinkRelEnum::SOURCE-PROFILE; return true; }
    if (std::strcmp(str, "source-profile-uuid") == 0) { out = MetadataLinkRelEnum::SOURCE-PROFILE-UUID; return true; }
    return false;
}

/// Allowed OSCAL property names for metadata locations.
enum class LocationPropNameEnum {
    TYPE
};

/// Convert LocationPropNameEnum to its string representation.
inline const char* to_string(LocationPropNameEnum value) {
    switch (value) {
        case LocationPropNameEnum::TYPE: return "type";
    }
    return "unknown";
}

/// Parse a string into LocationPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, LocationPropNameEnum& out) {
    if (std::strcmp(str, "type") == 0) { out = LocationPropNameEnum::TYPE; return true; }
    return false;
}

/// Curated OSCAL location type property values. Other values are permitted (OSCAL allow-other="yes").
enum class LocationPropTypeEnum {
    DATA-CENTER
};

/// Convert LocationPropTypeEnum to its string representation.
inline const char* to_string(LocationPropTypeEnum value) {
    switch (value) {
        case LocationPropTypeEnum::DATA-CENTER: return "data-center";
    }
    return "unknown";
}

/// Parse a string into LocationPropTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, LocationPropTypeEnum& out) {
    if (std::strcmp(str, "data-center") == 0) { out = LocationPropTypeEnum::DATA-CENTER; return true; }
    return false;
}

/// Curated OSCAL location class values for data-center type. Other values are permitted (OSCAL allow-other="yes").
enum class LocationDataCenterClassEnum {
    PRIMARY,
    ALTERNATE
};

/// Convert LocationDataCenterClassEnum to its string representation.
inline const char* to_string(LocationDataCenterClassEnum value) {
    switch (value) {
        case LocationDataCenterClassEnum::PRIMARY: return "primary";
        case LocationDataCenterClassEnum::ALTERNATE: return "alternate";
    }
    return "unknown";
}

/// Parse a string into LocationDataCenterClassEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, LocationDataCenterClassEnum& out) {
    if (std::strcmp(str, "primary") == 0) { out = LocationDataCenterClassEnum::PRIMARY; return true; }
    if (std::strcmp(str, "alternate") == 0) { out = LocationDataCenterClassEnum::ALTERNATE; return true; }
    return false;
}

/// Curated external identifier scheme values for metadata parties. Other values are permitted (OSCAL allow-other="yes").
enum class PartyExternalIdSchemeEnum {
    HTTP://ORCID.ORG/
};

/// Convert PartyExternalIdSchemeEnum to its string representation.
inline const char* to_string(PartyExternalIdSchemeEnum value) {
    switch (value) {
        case PartyExternalIdSchemeEnum::HTTP://ORCID.ORG/: return "http://orcid.org/";
    }
    return "unknown";
}

/// Parse a string into PartyExternalIdSchemeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PartyExternalIdSchemeEnum& out) {
    if (std::strcmp(str, "http://orcid.org/") == 0) { out = PartyExternalIdSchemeEnum::HTTP://ORCID.ORG/; return true; }
    return false;
}

/// Allowed OSCAL property names for metadata parties.
enum class PartyPropNameEnum {
    MAIL-STOP,
    OFFICE,
    JOB-TITLE
};

/// Convert PartyPropNameEnum to its string representation.
inline const char* to_string(PartyPropNameEnum value) {
    switch (value) {
        case PartyPropNameEnum::MAIL-STOP: return "mail-stop";
        case PartyPropNameEnum::OFFICE: return "office";
        case PartyPropNameEnum::JOB-TITLE: return "job-title";
    }
    return "unknown";
}

/// Parse a string into PartyPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PartyPropNameEnum& out) {
    if (std::strcmp(str, "mail-stop") == 0) { out = PartyPropNameEnum::MAIL-STOP; return true; }
    if (std::strcmp(str, "office") == 0) { out = PartyPropNameEnum::OFFICE; return true; }
    if (std::strcmp(str, "job-title") == 0) { out = PartyPropNameEnum::JOB-TITLE; return true; }
    return false;
}

/// Curated metadata responsible-party role identifiers. Other values are permitted (OSCAL allow-other="yes").
enum class MetadataResponsiblePartyRoleIdEnum {
    CREATOR,
    PREPARED-BY,
    PREPARED-FOR,
    CONTENT-APPROVER,
    CONTACT
};

/// Convert MetadataResponsiblePartyRoleIdEnum to its string representation.
inline const char* to_string(MetadataResponsiblePartyRoleIdEnum value) {
    switch (value) {
        case MetadataResponsiblePartyRoleIdEnum::CREATOR: return "creator";
        case MetadataResponsiblePartyRoleIdEnum::PREPARED-BY: return "prepared-by";
        case MetadataResponsiblePartyRoleIdEnum::PREPARED-FOR: return "prepared-for";
        case MetadataResponsiblePartyRoleIdEnum::CONTENT-APPROVER: return "content-approver";
        case MetadataResponsiblePartyRoleIdEnum::CONTACT: return "contact";
    }
    return "unknown";
}

/// Parse a string into MetadataResponsiblePartyRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MetadataResponsiblePartyRoleIdEnum& out) {
    if (std::strcmp(str, "creator") == 0) { out = MetadataResponsiblePartyRoleIdEnum::CREATOR; return true; }
    if (std::strcmp(str, "prepared-by") == 0) { out = MetadataResponsiblePartyRoleIdEnum::PREPARED-BY; return true; }
    if (std::strcmp(str, "prepared-for") == 0) { out = MetadataResponsiblePartyRoleIdEnum::PREPARED-FOR; return true; }
    if (std::strcmp(str, "content-approver") == 0) { out = MetadataResponsiblePartyRoleIdEnum::CONTENT-APPROVER; return true; }
    if (std::strcmp(str, "contact") == 0) { out = MetadataResponsiblePartyRoleIdEnum::CONTACT; return true; }
    return false;
}

/// Allowed OSCAL property names for metadata.
enum class MetadataPropNameEnum {
    KEYWORDS,
    RESOLUTION-TOOL,
    SOURCE-PROFILE-UUID
};

/// Convert MetadataPropNameEnum to its string representation.
inline const char* to_string(MetadataPropNameEnum value) {
    switch (value) {
        case MetadataPropNameEnum::KEYWORDS: return "keywords";
        case MetadataPropNameEnum::RESOLUTION-TOOL: return "resolution-tool";
        case MetadataPropNameEnum::SOURCE-PROFILE-UUID: return "source-profile-uuid";
    }
    return "unknown";
}

/// Parse a string into MetadataPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MetadataPropNameEnum& out) {
    if (std::strcmp(str, "keywords") == 0) { out = MetadataPropNameEnum::KEYWORDS; return true; }
    if (std::strcmp(str, "resolution-tool") == 0) { out = MetadataPropNameEnum::RESOLUTION-TOOL; return true; }
    if (std::strcmp(str, "source-profile-uuid") == 0) { out = MetadataPropNameEnum::SOURCE-PROFILE-UUID; return true; }
    return false;
}

/// Allowed OSCAL property names for metadata revisions.
enum class RevisionPropNameEnum {
    MARKING
};

/// Convert RevisionPropNameEnum to its string representation.
inline const char* to_string(RevisionPropNameEnum value) {
    switch (value) {
        case RevisionPropNameEnum::MARKING: return "marking";
    }
    return "unknown";
}

/// Parse a string into RevisionPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, RevisionPropNameEnum& out) {
    if (std::strcmp(str, "marking") == 0) { out = RevisionPropNameEnum::MARKING; return true; }
    return false;
}

/// Allowed OSCAL property names for back-matter resources.
enum class ResourcePropNameEnum {
    TYPE,
    VERSION,
    PUBLISHED
};

/// Convert ResourcePropNameEnum to its string representation.
inline const char* to_string(ResourcePropNameEnum value) {
    switch (value) {
        case ResourcePropNameEnum::TYPE: return "type";
        case ResourcePropNameEnum::VERSION: return "version";
        case ResourcePropNameEnum::PUBLISHED: return "published";
    }
    return "unknown";
}

/// Parse a string into ResourcePropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ResourcePropNameEnum& out) {
    if (std::strcmp(str, "type") == 0) { out = ResourcePropNameEnum::TYPE; return true; }
    if (std::strcmp(str, "version") == 0) { out = ResourcePropNameEnum::VERSION; return true; }
    if (std::strcmp(str, "published") == 0) { out = ResourcePropNameEnum::PUBLISHED; return true; }
    return false;
}

/// Allowed OSCAL back-matter resource type property values.
enum class ResourcePropTypeEnum {
    LOGO,
    IMAGE,
    SCREEN-SHOT,
    LAW,
    REGULATION,
    STANDARD,
    EXTERNAL-GUIDANCE,
    ACRONYMS,
    CITATION,
    POLICY,
    PROCEDURE,
    SYSTEM-GUIDE,
    USERS-GUIDE,
    ADMINISTRATORS-GUIDE,
    RULES-OF-BEHAVIOR,
    PLAN,
    ARTIFACT,
    EVIDENCE,
    TOOL-OUTPUT,
    RAW-DATA,
    INTERVIEW-NOTES,
    QUESTIONNAIRE,
    REPORT,
    AGREEMENT
};

/// Convert ResourcePropTypeEnum to its string representation.
inline const char* to_string(ResourcePropTypeEnum value) {
    switch (value) {
        case ResourcePropTypeEnum::LOGO: return "logo";
        case ResourcePropTypeEnum::IMAGE: return "image";
        case ResourcePropTypeEnum::SCREEN-SHOT: return "screen-shot";
        case ResourcePropTypeEnum::LAW: return "law";
        case ResourcePropTypeEnum::REGULATION: return "regulation";
        case ResourcePropTypeEnum::STANDARD: return "standard";
        case ResourcePropTypeEnum::EXTERNAL-GUIDANCE: return "external-guidance";
        case ResourcePropTypeEnum::ACRONYMS: return "acronyms";
        case ResourcePropTypeEnum::CITATION: return "citation";
        case ResourcePropTypeEnum::POLICY: return "policy";
        case ResourcePropTypeEnum::PROCEDURE: return "procedure";
        case ResourcePropTypeEnum::SYSTEM-GUIDE: return "system-guide";
        case ResourcePropTypeEnum::USERS-GUIDE: return "users-guide";
        case ResourcePropTypeEnum::ADMINISTRATORS-GUIDE: return "administrators-guide";
        case ResourcePropTypeEnum::RULES-OF-BEHAVIOR: return "rules-of-behavior";
        case ResourcePropTypeEnum::PLAN: return "plan";
        case ResourcePropTypeEnum::ARTIFACT: return "artifact";
        case ResourcePropTypeEnum::EVIDENCE: return "evidence";
        case ResourcePropTypeEnum::TOOL-OUTPUT: return "tool-output";
        case ResourcePropTypeEnum::RAW-DATA: return "raw-data";
        case ResourcePropTypeEnum::INTERVIEW-NOTES: return "interview-notes";
        case ResourcePropTypeEnum::QUESTIONNAIRE: return "questionnaire";
        case ResourcePropTypeEnum::REPORT: return "report";
        case ResourcePropTypeEnum::AGREEMENT: return "agreement";
    }
    return "unknown";
}

/// Parse a string into ResourcePropTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ResourcePropTypeEnum& out) {
    if (std::strcmp(str, "logo") == 0) { out = ResourcePropTypeEnum::LOGO; return true; }
    if (std::strcmp(str, "image") == 0) { out = ResourcePropTypeEnum::IMAGE; return true; }
    if (std::strcmp(str, "screen-shot") == 0) { out = ResourcePropTypeEnum::SCREEN-SHOT; return true; }
    if (std::strcmp(str, "law") == 0) { out = ResourcePropTypeEnum::LAW; return true; }
    if (std::strcmp(str, "regulation") == 0) { out = ResourcePropTypeEnum::REGULATION; return true; }
    if (std::strcmp(str, "standard") == 0) { out = ResourcePropTypeEnum::STANDARD; return true; }
    if (std::strcmp(str, "external-guidance") == 0) { out = ResourcePropTypeEnum::EXTERNAL-GUIDANCE; return true; }
    if (std::strcmp(str, "acronyms") == 0) { out = ResourcePropTypeEnum::ACRONYMS; return true; }
    if (std::strcmp(str, "citation") == 0) { out = ResourcePropTypeEnum::CITATION; return true; }
    if (std::strcmp(str, "policy") == 0) { out = ResourcePropTypeEnum::POLICY; return true; }
    if (std::strcmp(str, "procedure") == 0) { out = ResourcePropTypeEnum::PROCEDURE; return true; }
    if (std::strcmp(str, "system-guide") == 0) { out = ResourcePropTypeEnum::SYSTEM-GUIDE; return true; }
    if (std::strcmp(str, "users-guide") == 0) { out = ResourcePropTypeEnum::USERS-GUIDE; return true; }
    if (std::strcmp(str, "administrators-guide") == 0) { out = ResourcePropTypeEnum::ADMINISTRATORS-GUIDE; return true; }
    if (std::strcmp(str, "rules-of-behavior") == 0) { out = ResourcePropTypeEnum::RULES-OF-BEHAVIOR; return true; }
    if (std::strcmp(str, "plan") == 0) { out = ResourcePropTypeEnum::PLAN; return true; }
    if (std::strcmp(str, "artifact") == 0) { out = ResourcePropTypeEnum::ARTIFACT; return true; }
    if (std::strcmp(str, "evidence") == 0) { out = ResourcePropTypeEnum::EVIDENCE; return true; }
    if (std::strcmp(str, "tool-output") == 0) { out = ResourcePropTypeEnum::TOOL-OUTPUT; return true; }
    if (std::strcmp(str, "raw-data") == 0) { out = ResourcePropTypeEnum::RAW-DATA; return true; }
    if (std::strcmp(str, "interview-notes") == 0) { out = ResourcePropTypeEnum::INTERVIEW-NOTES; return true; }
    if (std::strcmp(str, "questionnaire") == 0) { out = ResourcePropTypeEnum::QUESTIONNAIRE; return true; }
    if (std::strcmp(str, "report") == 0) { out = ResourcePropTypeEnum::REPORT; return true; }
    if (std::strcmp(str, "agreement") == 0) { out = ResourcePropTypeEnum::AGREEMENT; return true; }
    return false;
}

/// Curated OSCAL action system values. Other values are permitted (OSCAL allow-other="yes").
enum class ActionSystemEnum {
    HTTP://CSRC.NIST.GOV/NS/OSCAL
};

/// Convert ActionSystemEnum to its string representation.
inline const char* to_string(ActionSystemEnum value) {
    switch (value) {
        case ActionSystemEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL: return "http://csrc.nist.gov/ns/oscal";
    }
    return "unknown";
}

/// Parse a string into ActionSystemEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ActionSystemEnum& out) {
    if (std::strcmp(str, "http://csrc.nist.gov/ns/oscal") == 0) { out = ActionSystemEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL; return true; }
    return false;
}

/// Allowed OSCAL action type values.
enum class ActionTypeEnum {
    APPROVAL,
    REQUEST-CHANGES
};

/// Convert ActionTypeEnum to its string representation.
inline const char* to_string(ActionTypeEnum value) {
    switch (value) {
        case ActionTypeEnum::APPROVAL: return "approval";
        case ActionTypeEnum::REQUEST-CHANGES: return "request-changes";
    }
    return "unknown";
}

/// Parse a string into ActionTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ActionTypeEnum& out) {
    if (std::strcmp(str, "approval") == 0) { out = ActionTypeEnum::APPROVAL; return true; }
    if (std::strcmp(str, "request-changes") == 0) { out = ActionTypeEnum::REQUEST-CHANGES; return true; }
    return false;
}

/// Curated document identifier scheme values. Other values are permitted (OSCAL allow-other="yes").
enum class DocumentIdSchemeEnum {
    HTTP://WWW.DOI.ORG/
};

/// Convert DocumentIdSchemeEnum to its string representation.
inline const char* to_string(DocumentIdSchemeEnum value) {
    switch (value) {
        case DocumentIdSchemeEnum::HTTP://WWW.DOI.ORG/: return "http://www.doi.org/";
    }
    return "unknown";
}

/// Parse a string into DocumentIdSchemeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, DocumentIdSchemeEnum& out) {
    if (std::strcmp(str, "http://www.doi.org/") == 0) { out = DocumentIdSchemeEnum::HTTP://WWW.DOI.ORG/; return true; }
    return false;
}

enum class ParameterCardinalityEnum {
    ONE,
    ONE-OR-MORE
};

/// Convert ParameterCardinalityEnum to its string representation.
inline const char* to_string(ParameterCardinalityEnum value) {
    switch (value) {
        case ParameterCardinalityEnum::ONE: return "one";
        case ParameterCardinalityEnum::ONE-OR-MORE: return "one-or-more";
    }
    return "unknown";
}

/// Parse a string into ParameterCardinalityEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ParameterCardinalityEnum& out) {
    if (std::strcmp(str, "one") == 0) { out = ParameterCardinalityEnum::ONE; return true; }
    if (std::strcmp(str, "one-or-more") == 0) { out = ParameterCardinalityEnum::ONE-OR-MORE; return true; }
    return false;
}

/// Allowed OSCAL property names for control-common parts.
enum class PartPropNameEnum {
    LABEL,
    SORT-ID,
    ALT-IDENTIFIER
};

/// Convert PartPropNameEnum to its string representation.
inline const char* to_string(PartPropNameEnum value) {
    switch (value) {
        case PartPropNameEnum::LABEL: return "label";
        case PartPropNameEnum::SORT-ID: return "sort-id";
        case PartPropNameEnum::ALT-IDENTIFIER: return "alt-identifier";
    }
    return "unknown";
}

/// Parse a string into PartPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PartPropNameEnum& out) {
    if (std::strcmp(str, "label") == 0) { out = PartPropNameEnum::LABEL; return true; }
    if (std::strcmp(str, "sort-id") == 0) { out = PartPropNameEnum::SORT-ID; return true; }
    if (std::strcmp(str, "alt-identifier") == 0) { out = PartPropNameEnum::ALT-IDENTIFIER; return true; }
    return false;
}

/// Allowed OSCAL property names for control-common parameters.
enum class ParameterPropNameEnum {
    LABEL,
    SORT-ID,
    ALT-IDENTIFIER,
    ALT-LABEL
};

/// Convert ParameterPropNameEnum to its string representation.
inline const char* to_string(ParameterPropNameEnum value) {
    switch (value) {
        case ParameterPropNameEnum::LABEL: return "label";
        case ParameterPropNameEnum::SORT-ID: return "sort-id";
        case ParameterPropNameEnum::ALT-IDENTIFIER: return "alt-identifier";
        case ParameterPropNameEnum::ALT-LABEL: return "alt-label";
    }
    return "unknown";
}

/// Parse a string into ParameterPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ParameterPropNameEnum& out) {
    if (std::strcmp(str, "label") == 0) { out = ParameterPropNameEnum::LABEL; return true; }
    if (std::strcmp(str, "sort-id") == 0) { out = ParameterPropNameEnum::SORT-ID; return true; }
    if (std::strcmp(str, "alt-identifier") == 0) { out = ParameterPropNameEnum::ALT-IDENTIFIER; return true; }
    if (std::strcmp(str, "alt-label") == 0) { out = ParameterPropNameEnum::ALT-LABEL; return true; }
    return false;
}

/// Allowed OSCAL RMF parameter property names.
enum class RmfParameterPropNameEnum {
    AGGREGATES
};

/// Convert RmfParameterPropNameEnum to its string representation.
inline const char* to_string(RmfParameterPropNameEnum value) {
    switch (value) {
        case RmfParameterPropNameEnum::AGGREGATES: return "aggregates";
    }
    return "unknown";
}

/// Parse a string into RmfParameterPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, RmfParameterPropNameEnum& out) {
    if (std::strcmp(str, "aggregates") == 0) { out = RmfParameterPropNameEnum::AGGREGATES; return true; }
    return false;
}

/// Allowed OSCAL part names for catalog groups.
enum class GroupPartNameEnum {
    OVERVIEW,
    INSTRUCTION
};

/// Convert GroupPartNameEnum to its string representation.
inline const char* to_string(GroupPartNameEnum value) {
    switch (value) {
        case GroupPartNameEnum::OVERVIEW: return "overview";
        case GroupPartNameEnum::INSTRUCTION: return "instruction";
    }
    return "unknown";
}

/// Parse a string into GroupPartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, GroupPartNameEnum& out) {
    if (std::strcmp(str, "overview") == 0) { out = GroupPartNameEnum::OVERVIEW; return true; }
    if (std::strcmp(str, "instruction") == 0) { out = GroupPartNameEnum::INSTRUCTION; return true; }
    return false;
}

/// Allowed OSCAL property names for catalog controls.
enum class ControlPropNameEnum {
    LABEL,
    SORT-ID,
    ALT-IDENTIFIER,
    STATUS
};

/// Convert ControlPropNameEnum to its string representation.
inline const char* to_string(ControlPropNameEnum value) {
    switch (value) {
        case ControlPropNameEnum::LABEL: return "label";
        case ControlPropNameEnum::SORT-ID: return "sort-id";
        case ControlPropNameEnum::ALT-IDENTIFIER: return "alt-identifier";
        case ControlPropNameEnum::STATUS: return "status";
    }
    return "unknown";
}

/// Parse a string into ControlPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlPropNameEnum& out) {
    if (std::strcmp(str, "label") == 0) { out = ControlPropNameEnum::LABEL; return true; }
    if (std::strcmp(str, "sort-id") == 0) { out = ControlPropNameEnum::SORT-ID; return true; }
    if (std::strcmp(str, "alt-identifier") == 0) { out = ControlPropNameEnum::ALT-IDENTIFIER; return true; }
    if (std::strcmp(str, "status") == 0) { out = ControlPropNameEnum::STATUS; return true; }
    return false;
}

/// Allowed OSCAL status property values for catalog controls.
enum class ControlPropStatusValueEnum {
    WITHDRAWN,
    WITHDRAWN
};

/// Convert ControlPropStatusValueEnum to its string representation.
inline const char* to_string(ControlPropStatusValueEnum value) {
    switch (value) {
        case ControlPropStatusValueEnum::WITHDRAWN: return "withdrawn";
        case ControlPropStatusValueEnum::WITHDRAWN: return "Withdrawn";
    }
    return "unknown";
}

/// Parse a string into ControlPropStatusValueEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlPropStatusValueEnum& out) {
    if (std::strcmp(str, "withdrawn") == 0) { out = ControlPropStatusValueEnum::WITHDRAWN; return true; }
    if (std::strcmp(str, "Withdrawn") == 0) { out = ControlPropStatusValueEnum::WITHDRAWN; return true; }
    return false;
}

/// Curated OSCAL link relation values for catalog controls. Other values are permitted (OSCAL allow-other="yes").
enum class ControlLinkRelEnum {
    REFERENCE,
    RELATED,
    REQUIRED,
    INCORPORATED-INTO,
    MOVED-TO
};

/// Convert ControlLinkRelEnum to its string representation.
inline const char* to_string(ControlLinkRelEnum value) {
    switch (value) {
        case ControlLinkRelEnum::REFERENCE: return "reference";
        case ControlLinkRelEnum::RELATED: return "related";
        case ControlLinkRelEnum::REQUIRED: return "required";
        case ControlLinkRelEnum::INCORPORATED-INTO: return "incorporated-into";
        case ControlLinkRelEnum::MOVED-TO: return "moved-to";
    }
    return "unknown";
}

/// Parse a string into ControlLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlLinkRelEnum& out) {
    if (std::strcmp(str, "reference") == 0) { out = ControlLinkRelEnum::REFERENCE; return true; }
    if (std::strcmp(str, "related") == 0) { out = ControlLinkRelEnum::RELATED; return true; }
    if (std::strcmp(str, "required") == 0) { out = ControlLinkRelEnum::REQUIRED; return true; }
    if (std::strcmp(str, "incorporated-into") == 0) { out = ControlLinkRelEnum::INCORPORATED-INTO; return true; }
    if (std::strcmp(str, "moved-to") == 0) { out = ControlLinkRelEnum::MOVED-TO; return true; }
    return false;
}

/// Allowed top-level OSCAL part names for catalog controls.
enum class ControlPartNameEnum {
    OVERVIEW,
    STATEMENT,
    GUIDANCE,
    EXAMPLE,
    ASSESSMENT,
    ASSESSMENT-METHOD
};

/// Convert ControlPartNameEnum to its string representation.
inline const char* to_string(ControlPartNameEnum value) {
    switch (value) {
        case ControlPartNameEnum::OVERVIEW: return "overview";
        case ControlPartNameEnum::STATEMENT: return "statement";
        case ControlPartNameEnum::GUIDANCE: return "guidance";
        case ControlPartNameEnum::EXAMPLE: return "example";
        case ControlPartNameEnum::ASSESSMENT: return "assessment";
        case ControlPartNameEnum::ASSESSMENT-METHOD: return "assessment-method";
    }
    return "unknown";
}

/// Parse a string into ControlPartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlPartNameEnum& out) {
    if (std::strcmp(str, "overview") == 0) { out = ControlPartNameEnum::OVERVIEW; return true; }
    if (std::strcmp(str, "statement") == 0) { out = ControlPartNameEnum::STATEMENT; return true; }
    if (std::strcmp(str, "guidance") == 0) { out = ControlPartNameEnum::GUIDANCE; return true; }
    if (std::strcmp(str, "example") == 0) { out = ControlPartNameEnum::EXAMPLE; return true; }
    if (std::strcmp(str, "assessment") == 0) { out = ControlPartNameEnum::ASSESSMENT; return true; }
    if (std::strcmp(str, "assessment-method") == 0) { out = ControlPartNameEnum::ASSESSMENT-METHOD; return true; }
    return false;
}

/// Allowed OSCAL subpart names for control statement parts.
enum class ControlStatementPartSubpartNameEnum {
    ITEM
};

/// Convert ControlStatementPartSubpartNameEnum to its string representation.
inline const char* to_string(ControlStatementPartSubpartNameEnum value) {
    switch (value) {
        case ControlStatementPartSubpartNameEnum::ITEM: return "item";
    }
    return "unknown";
}

/// Parse a string into ControlStatementPartSubpartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlStatementPartSubpartNameEnum& out) {
    if (std::strcmp(str, "item") == 0) { out = ControlStatementPartSubpartNameEnum::ITEM; return true; }
    return false;
}

/// Allowed OSCAL part names for control statement parts.
enum class ControlStatementPartNameEnum {
    OBJECTIVE,
    ASSESSMENT-OBJECTIVE
};

/// Convert ControlStatementPartNameEnum to its string representation.
inline const char* to_string(ControlStatementPartNameEnum value) {
    switch (value) {
        case ControlStatementPartNameEnum::OBJECTIVE: return "objective";
        case ControlStatementPartNameEnum::ASSESSMENT-OBJECTIVE: return "assessment-objective";
    }
    return "unknown";
}

/// Parse a string into ControlStatementPartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlStatementPartNameEnum& out) {
    if (std::strcmp(str, "objective") == 0) { out = ControlStatementPartNameEnum::OBJECTIVE; return true; }
    if (std::strcmp(str, "assessment-objective") == 0) { out = ControlStatementPartNameEnum::ASSESSMENT-OBJECTIVE; return true; }
    return false;
}

/// Allowed OSCAL subpart names for control objective parts.
enum class ControlObjectivePartSubpartNameEnum {
    OBJECTS,
    ASSESSMENT-OBJECTS
};

/// Convert ControlObjectivePartSubpartNameEnum to its string representation.
inline const char* to_string(ControlObjectivePartSubpartNameEnum value) {
    switch (value) {
        case ControlObjectivePartSubpartNameEnum::OBJECTS: return "objects";
        case ControlObjectivePartSubpartNameEnum::ASSESSMENT-OBJECTS: return "assessment-objects";
    }
    return "unknown";
}

/// Parse a string into ControlObjectivePartSubpartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlObjectivePartSubpartNameEnum& out) {
    if (std::strcmp(str, "objects") == 0) { out = ControlObjectivePartSubpartNameEnum::OBJECTS; return true; }
    if (std::strcmp(str, "assessment-objects") == 0) { out = ControlObjectivePartSubpartNameEnum::ASSESSMENT-OBJECTS; return true; }
    return false;
}

/// Allowed OSCAL property names for control statement parts.
enum class ControlStatementPartPropNameEnum {
    METHOD
};

/// Convert ControlStatementPartPropNameEnum to its string representation.
inline const char* to_string(ControlStatementPartPropNameEnum value) {
    switch (value) {
        case ControlStatementPartPropNameEnum::METHOD: return "method";
    }
    return "unknown";
}

/// Parse a string into ControlStatementPartPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlStatementPartPropNameEnum& out) {
    if (std::strcmp(str, "method") == 0) { out = ControlStatementPartPropNameEnum::METHOD; return true; }
    return false;
}

/// Allowed OSCAL RMF property names for control statement parts.
enum class ControlStatementPartRmfPropNameEnum {
    METHOD
};

/// Convert ControlStatementPartRmfPropNameEnum to its string representation.
inline const char* to_string(ControlStatementPartRmfPropNameEnum value) {
    switch (value) {
        case ControlStatementPartRmfPropNameEnum::METHOD: return "method";
    }
    return "unknown";
}

/// Parse a string into ControlStatementPartRmfPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlStatementPartRmfPropNameEnum& out) {
    if (std::strcmp(str, "method") == 0) { out = ControlStatementPartRmfPropNameEnum::METHOD; return true; }
    return false;
}

/// Allowed OSCAL method property values for control objectives.
enum class ControlObjectivePartMethodPropValueEnum {
    INTERVIEW,
    EXAMINE,
    TEST
};

/// Convert ControlObjectivePartMethodPropValueEnum to its string representation.
inline const char* to_string(ControlObjectivePartMethodPropValueEnum value) {
    switch (value) {
        case ControlObjectivePartMethodPropValueEnum::INTERVIEW: return "INTERVIEW";
        case ControlObjectivePartMethodPropValueEnum::EXAMINE: return "EXAMINE";
        case ControlObjectivePartMethodPropValueEnum::TEST: return "TEST";
    }
    return "unknown";
}

/// Parse a string into ControlObjectivePartMethodPropValueEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlObjectivePartMethodPropValueEnum& out) {
    if (std::strcmp(str, "INTERVIEW") == 0) { out = ControlObjectivePartMethodPropValueEnum::INTERVIEW; return true; }
    if (std::strcmp(str, "EXAMINE") == 0) { out = ControlObjectivePartMethodPropValueEnum::EXAMINE; return true; }
    if (std::strcmp(str, "TEST") == 0) { out = ControlObjectivePartMethodPropValueEnum::TEST; return true; }
    return false;
}

enum class WithChildControlsEnum {
    YES,
    NO
};

/// Convert WithChildControlsEnum to its string representation.
inline const char* to_string(WithChildControlsEnum value) {
    switch (value) {
        case WithChildControlsEnum::YES: return "yes";
        case WithChildControlsEnum::NO: return "no";
    }
    return "unknown";
}

/// Parse a string into WithChildControlsEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, WithChildControlsEnum& out) {
    if (std::strcmp(str, "yes") == 0) { out = WithChildControlsEnum::YES; return true; }
    if (std::strcmp(str, "no") == 0) { out = WithChildControlsEnum::NO; return true; }
    return false;
}

/// Ordering options for a selection of controls.
enum class InsertOrderEnum {
    KEEP,
    ASCENDING,
    DESCENDING
};

/// Convert InsertOrderEnum to its string representation.
inline const char* to_string(InsertOrderEnum value) {
    switch (value) {
        case InsertOrderEnum::KEEP: return "keep";
        case InsertOrderEnum::ASCENDING: return "ascending";
        case InsertOrderEnum::DESCENDING: return "descending";
    }
    return "unknown";
}

/// Parse a string into InsertOrderEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, InsertOrderEnum& out) {
    if (std::strcmp(str, "keep") == 0) { out = InsertOrderEnum::KEEP; return true; }
    if (std::strcmp(str, "ascending") == 0) { out = InsertOrderEnum::ASCENDING; return true; }
    if (std::strcmp(str, "descending") == 0) { out = InsertOrderEnum::DESCENDING; return true; }
    return false;
}

/// Where new content is added relative to the targeted element.
enum class AdditionPositionEnum {
    BEFORE,
    AFTER,
    STARTING,
    ENDING
};

/// Convert AdditionPositionEnum to its string representation.
inline const char* to_string(AdditionPositionEnum value) {
    switch (value) {
        case AdditionPositionEnum::BEFORE: return "before";
        case AdditionPositionEnum::AFTER: return "after";
        case AdditionPositionEnum::STARTING: return "starting";
        case AdditionPositionEnum::ENDING: return "ending";
    }
    return "unknown";
}

/// Parse a string into AdditionPositionEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AdditionPositionEnum& out) {
    if (std::strcmp(str, "before") == 0) { out = AdditionPositionEnum::BEFORE; return true; }
    if (std::strcmp(str, "after") == 0) { out = AdditionPositionEnum::AFTER; return true; }
    if (std::strcmp(str, "starting") == 0) { out = AdditionPositionEnum::STARTING; return true; }
    if (std::strcmp(str, "ending") == 0) { out = AdditionPositionEnum::ENDING; return true; }
    return false;
}

/// Identifies content to remove by the item's object type name.
enum class ByItemNameEnum {
    PARAM,
    PROP,
    LINK,
    PART,
    MAPPING,
    MAP
};

/// Convert ByItemNameEnum to its string representation.
inline const char* to_string(ByItemNameEnum value) {
    switch (value) {
        case ByItemNameEnum::PARAM: return "param";
        case ByItemNameEnum::PROP: return "prop";
        case ByItemNameEnum::LINK: return "link";
        case ByItemNameEnum::PART: return "part";
        case ByItemNameEnum::MAPPING: return "mapping";
        case ByItemNameEnum::MAP: return "map";
    }
    return "unknown";
}

/// Parse a string into ByItemNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ByItemNameEnum& out) {
    if (std::strcmp(str, "param") == 0) { out = ByItemNameEnum::PARAM; return true; }
    if (std::strcmp(str, "prop") == 0) { out = ByItemNameEnum::PROP; return true; }
    if (std::strcmp(str, "link") == 0) { out = ByItemNameEnum::LINK; return true; }
    if (std::strcmp(str, "part") == 0) { out = ByItemNameEnum::PART; return true; }
    if (std::strcmp(str, "mapping") == 0) { out = ByItemNameEnum::MAPPING; return true; }
    if (std::strcmp(str, "map") == 0) { out = ByItemNameEnum::MAP; return true; }
    return false;
}

/// Methods for resolving duplicate control instances during merge.
enum class CombinationMethodEnum {
    USE-FIRST,
    MERGE,
    KEEP
};

/// Convert CombinationMethodEnum to its string representation.
inline const char* to_string(CombinationMethodEnum value) {
    switch (value) {
        case CombinationMethodEnum::USE-FIRST: return "use-first";
        case CombinationMethodEnum::MERGE: return "merge";
        case CombinationMethodEnum::KEEP: return "keep";
    }
    return "unknown";
}

/// Parse a string into CombinationMethodEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, CombinationMethodEnum& out) {
    if (std::strcmp(str, "use-first") == 0) { out = CombinationMethodEnum::USE-FIRST; return true; }
    if (std::strcmp(str, "merge") == 0) { out = CombinationMethodEnum::MERGE; return true; }
    if (std::strcmp(str, "keep") == 0) { out = CombinationMethodEnum::KEEP; return true; }
    return false;
}

/// Allowed OSCAL property names for profile modification additions.
enum class AlterationPropNameEnum {
    LABEL,
    SORT-ID,
    ALT-IDENTIFIER
};

/// Convert AlterationPropNameEnum to its string representation.
inline const char* to_string(AlterationPropNameEnum value) {
    switch (value) {
        case AlterationPropNameEnum::LABEL: return "label";
        case AlterationPropNameEnum::SORT-ID: return "sort-id";
        case AlterationPropNameEnum::ALT-IDENTIFIER: return "alt-identifier";
    }
    return "unknown";
}

/// Parse a string into AlterationPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AlterationPropNameEnum& out) {
    if (std::strcmp(str, "label") == 0) { out = AlterationPropNameEnum::LABEL; return true; }
    if (std::strcmp(str, "sort-id") == 0) { out = AlterationPropNameEnum::SORT-ID; return true; }
    if (std::strcmp(str, "alt-identifier") == 0) { out = AlterationPropNameEnum::ALT-IDENTIFIER; return true; }
    return false;
}

/// Curated task type values. Other values are permitted (OSCAL allow-other="yes").
enum class TaskTypeEnum {
    MILESTONE,
    ACTION
};

/// Convert TaskTypeEnum to its string representation.
inline const char* to_string(TaskTypeEnum value) {
    switch (value) {
        case TaskTypeEnum::MILESTONE: return "milestone";
        case TaskTypeEnum::ACTION: return "action";
    }
    return "unknown";
}

/// Parse a string into TaskTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, TaskTypeEnum& out) {
    if (std::strcmp(str, "milestone") == 0) { out = TaskTypeEnum::MILESTONE; return true; }
    if (std::strcmp(str, "action") == 0) { out = TaskTypeEnum::ACTION; return true; }
    return false;
}

enum class TimingUnitEnum {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    MONTHS,
    YEARS
};

/// Convert TimingUnitEnum to its string representation.
inline const char* to_string(TimingUnitEnum value) {
    switch (value) {
        case TimingUnitEnum::SECONDS: return "seconds";
        case TimingUnitEnum::MINUTES: return "minutes";
        case TimingUnitEnum::HOURS: return "hours";
        case TimingUnitEnum::DAYS: return "days";
        case TimingUnitEnum::MONTHS: return "months";
        case TimingUnitEnum::YEARS: return "years";
    }
    return "unknown";
}

/// Parse a string into TimingUnitEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, TimingUnitEnum& out) {
    if (std::strcmp(str, "seconds") == 0) { out = TimingUnitEnum::SECONDS; return true; }
    if (std::strcmp(str, "minutes") == 0) { out = TimingUnitEnum::MINUTES; return true; }
    if (std::strcmp(str, "hours") == 0) { out = TimingUnitEnum::HOURS; return true; }
    if (std::strcmp(str, "days") == 0) { out = TimingUnitEnum::DAYS; return true; }
    if (std::strcmp(str, "months") == 0) { out = TimingUnitEnum::MONTHS; return true; }
    if (std::strcmp(str, "years") == 0) { out = TimingUnitEnum::YEARS; return true; }
    return false;
}

/// Curated assessment subject type values. Other values are permitted (OSCAL allow-other="yes").
enum class AssessmentSubjectTypeEnum {
    COMPONENT,
    INVENTORY-ITEM,
    LOCATION,
    PARTY,
    USER
};

/// Convert AssessmentSubjectTypeEnum to its string representation.
inline const char* to_string(AssessmentSubjectTypeEnum value) {
    switch (value) {
        case AssessmentSubjectTypeEnum::COMPONENT: return "component";
        case AssessmentSubjectTypeEnum::INVENTORY-ITEM: return "inventory-item";
        case AssessmentSubjectTypeEnum::LOCATION: return "location";
        case AssessmentSubjectTypeEnum::PARTY: return "party";
        case AssessmentSubjectTypeEnum::USER: return "user";
    }
    return "unknown";
}

/// Parse a string into AssessmentSubjectTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AssessmentSubjectTypeEnum& out) {
    if (std::strcmp(str, "component") == 0) { out = AssessmentSubjectTypeEnum::COMPONENT; return true; }
    if (std::strcmp(str, "inventory-item") == 0) { out = AssessmentSubjectTypeEnum::INVENTORY-ITEM; return true; }
    if (std::strcmp(str, "location") == 0) { out = AssessmentSubjectTypeEnum::LOCATION; return true; }
    if (std::strcmp(str, "party") == 0) { out = AssessmentSubjectTypeEnum::PARTY; return true; }
    if (std::strcmp(str, "user") == 0) { out = AssessmentSubjectTypeEnum::USER; return true; }
    return false;
}

/// Curated subject type values for subject selection. Other values are permitted (OSCAL allow-other="yes").
enum class SelectSubjectTypeEnum {
    COMPONENT,
    INVENTORY-ITEM,
    LOCATION,
    PARTY,
    USER,
    RESOURCE
};

/// Convert SelectSubjectTypeEnum to its string representation.
inline const char* to_string(SelectSubjectTypeEnum value) {
    switch (value) {
        case SelectSubjectTypeEnum::COMPONENT: return "component";
        case SelectSubjectTypeEnum::INVENTORY-ITEM: return "inventory-item";
        case SelectSubjectTypeEnum::LOCATION: return "location";
        case SelectSubjectTypeEnum::PARTY: return "party";
        case SelectSubjectTypeEnum::USER: return "user";
        case SelectSubjectTypeEnum::RESOURCE: return "resource";
    }
    return "unknown";
}

/// Parse a string into SelectSubjectTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SelectSubjectTypeEnum& out) {
    if (std::strcmp(str, "component") == 0) { out = SelectSubjectTypeEnum::COMPONENT; return true; }
    if (std::strcmp(str, "inventory-item") == 0) { out = SelectSubjectTypeEnum::INVENTORY-ITEM; return true; }
    if (std::strcmp(str, "location") == 0) { out = SelectSubjectTypeEnum::LOCATION; return true; }
    if (std::strcmp(str, "party") == 0) { out = SelectSubjectTypeEnum::PARTY; return true; }
    if (std::strcmp(str, "user") == 0) { out = SelectSubjectTypeEnum::USER; return true; }
    if (std::strcmp(str, "resource") == 0) { out = SelectSubjectTypeEnum::RESOURCE; return true; }
    return false;
}

enum class OriginActorTypeEnum {
    TOOL,
    ASSESSMENT-PLATFORM,
    PARTY
};

/// Convert OriginActorTypeEnum to its string representation.
inline const char* to_string(OriginActorTypeEnum value) {
    switch (value) {
        case OriginActorTypeEnum::TOOL: return "tool";
        case OriginActorTypeEnum::ASSESSMENT-PLATFORM: return "assessment-platform";
        case OriginActorTypeEnum::PARTY: return "party";
    }
    return "unknown";
}

/// Parse a string into OriginActorTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OriginActorTypeEnum& out) {
    if (std::strcmp(str, "tool") == 0) { out = OriginActorTypeEnum::TOOL; return true; }
    if (std::strcmp(str, "assessment-platform") == 0) { out = OriginActorTypeEnum::ASSESSMENT-PLATFORM; return true; }
    if (std::strcmp(str, "party") == 0) { out = OriginActorTypeEnum::PARTY; return true; }
    return false;
}

/// Curated observation method values. Other values are permitted (OSCAL allow-other="yes").
enum class ObservationMethodEnum {
    EXAMINE,
    INTERVIEW,
    TEST,
    UNKNOWN
};

/// Convert ObservationMethodEnum to its string representation.
inline const char* to_string(ObservationMethodEnum value) {
    switch (value) {
        case ObservationMethodEnum::EXAMINE: return "EXAMINE";
        case ObservationMethodEnum::INTERVIEW: return "INTERVIEW";
        case ObservationMethodEnum::TEST: return "TEST";
        case ObservationMethodEnum::UNKNOWN: return "UNKNOWN";
    }
    return "unknown";
}

/// Parse a string into ObservationMethodEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ObservationMethodEnum& out) {
    if (std::strcmp(str, "EXAMINE") == 0) { out = ObservationMethodEnum::EXAMINE; return true; }
    if (std::strcmp(str, "INTERVIEW") == 0) { out = ObservationMethodEnum::INTERVIEW; return true; }
    if (std::strcmp(str, "TEST") == 0) { out = ObservationMethodEnum::TEST; return true; }
    if (std::strcmp(str, "UNKNOWN") == 0) { out = ObservationMethodEnum::UNKNOWN; return true; }
    return false;
}

/// Curated observation type values. Other values are permitted (OSCAL allow-other="yes").
enum class ObservationTypeEnum {
    SSP-STATEMENT-ISSUE,
    CONTROL-OBJECTIVE,
    MITIGATION,
    FINDING,
    DISCOVERY,
    HISTORIC
};

/// Convert ObservationTypeEnum to its string representation.
inline const char* to_string(ObservationTypeEnum value) {
    switch (value) {
        case ObservationTypeEnum::SSP-STATEMENT-ISSUE: return "ssp-statement-issue";
        case ObservationTypeEnum::CONTROL-OBJECTIVE: return "control-objective";
        case ObservationTypeEnum::MITIGATION: return "mitigation";
        case ObservationTypeEnum::FINDING: return "finding";
        case ObservationTypeEnum::DISCOVERY: return "discovery";
        case ObservationTypeEnum::HISTORIC: return "historic";
    }
    return "unknown";
}

/// Parse a string into ObservationTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ObservationTypeEnum& out) {
    if (std::strcmp(str, "ssp-statement-issue") == 0) { out = ObservationTypeEnum::SSP-STATEMENT-ISSUE; return true; }
    if (std::strcmp(str, "control-objective") == 0) { out = ObservationTypeEnum::CONTROL-OBJECTIVE; return true; }
    if (std::strcmp(str, "mitigation") == 0) { out = ObservationTypeEnum::MITIGATION; return true; }
    if (std::strcmp(str, "finding") == 0) { out = ObservationTypeEnum::FINDING; return true; }
    if (std::strcmp(str, "discovery") == 0) { out = ObservationTypeEnum::DISCOVERY; return true; }
    if (std::strcmp(str, "historic") == 0) { out = ObservationTypeEnum::HISTORIC; return true; }
    return false;
}

/// Curated risk status values. Other values are permitted (OSCAL allow-other="yes").
enum class RiskStatusEnum {
    OPEN,
    INVESTIGATING,
    REMEDIATING,
    DEVIATION-REQUESTED,
    DEVIATION-APPROVED,
    CLOSED
};

/// Convert RiskStatusEnum to its string representation.
inline const char* to_string(RiskStatusEnum value) {
    switch (value) {
        case RiskStatusEnum::OPEN: return "open";
        case RiskStatusEnum::INVESTIGATING: return "investigating";
        case RiskStatusEnum::REMEDIATING: return "remediating";
        case RiskStatusEnum::DEVIATION-REQUESTED: return "deviation-requested";
        case RiskStatusEnum::DEVIATION-APPROVED: return "deviation-approved";
        case RiskStatusEnum::CLOSED: return "closed";
    }
    return "unknown";
}

/// Parse a string into RiskStatusEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, RiskStatusEnum& out) {
    if (std::strcmp(str, "open") == 0) { out = RiskStatusEnum::OPEN; return true; }
    if (std::strcmp(str, "investigating") == 0) { out = RiskStatusEnum::INVESTIGATING; return true; }
    if (std::strcmp(str, "remediating") == 0) { out = RiskStatusEnum::REMEDIATING; return true; }
    if (std::strcmp(str, "deviation-requested") == 0) { out = RiskStatusEnum::DEVIATION-REQUESTED; return true; }
    if (std::strcmp(str, "deviation-approved") == 0) { out = RiskStatusEnum::DEVIATION-APPROVED; return true; }
    if (std::strcmp(str, "closed") == 0) { out = RiskStatusEnum::CLOSED; return true; }
    return false;
}

enum class FindingTargetTypeEnum {
    STATEMENT-ID,
    OBJECTIVE-ID
};

/// Convert FindingTargetTypeEnum to its string representation.
inline const char* to_string(FindingTargetTypeEnum value) {
    switch (value) {
        case FindingTargetTypeEnum::STATEMENT-ID: return "statement-id";
        case FindingTargetTypeEnum::OBJECTIVE-ID: return "objective-id";
    }
    return "unknown";
}

/// Parse a string into FindingTargetTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, FindingTargetTypeEnum& out) {
    if (std::strcmp(str, "statement-id") == 0) { out = FindingTargetTypeEnum::STATEMENT-ID; return true; }
    if (std::strcmp(str, "objective-id") == 0) { out = FindingTargetTypeEnum::OBJECTIVE-ID; return true; }
    return false;
}

enum class ObjectiveStatusStateEnum {
    SATISFIED,
    NOT-SATISFIED
};

/// Convert ObjectiveStatusStateEnum to its string representation.
inline const char* to_string(ObjectiveStatusStateEnum value) {
    switch (value) {
        case ObjectiveStatusStateEnum::SATISFIED: return "satisfied";
        case ObjectiveStatusStateEnum::NOT-SATISFIED: return "not-satisfied";
    }
    return "unknown";
}

/// Parse a string into ObjectiveStatusStateEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ObjectiveStatusStateEnum& out) {
    if (std::strcmp(str, "satisfied") == 0) { out = ObjectiveStatusStateEnum::SATISFIED; return true; }
    if (std::strcmp(str, "not-satisfied") == 0) { out = ObjectiveStatusStateEnum::NOT-SATISFIED; return true; }
    return false;
}

/// Curated objective status reason values. Other values are permitted (OSCAL allow-other="yes").
enum class ObjectiveStatusReasonEnum {
    PASS,
    FAIL,
    OTHER
};

/// Convert ObjectiveStatusReasonEnum to its string representation.
inline const char* to_string(ObjectiveStatusReasonEnum value) {
    switch (value) {
        case ObjectiveStatusReasonEnum::PASS: return "pass";
        case ObjectiveStatusReasonEnum::FAIL: return "fail";
        case ObjectiveStatusReasonEnum::OTHER: return "other";
    }
    return "unknown";
}

/// Parse a string into ObjectiveStatusReasonEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ObjectiveStatusReasonEnum& out) {
    if (std::strcmp(str, "pass") == 0) { out = ObjectiveStatusReasonEnum::PASS; return true; }
    if (std::strcmp(str, "fail") == 0) { out = ObjectiveStatusReasonEnum::FAIL; return true; }
    if (std::strcmp(str, "other") == 0) { out = ObjectiveStatusReasonEnum::OTHER; return true; }
    return false;
}

/// Curated implementation status state values. Other values are permitted (OSCAL allow-other="yes").
enum class ImplementationStatusStateEnum {
    IMPLEMENTED,
    PARTIAL,
    PLANNED,
    ALTERNATIVE,
    NOT-APPLICABLE
};

/// Convert ImplementationStatusStateEnum to its string representation.
inline const char* to_string(ImplementationStatusStateEnum value) {
    switch (value) {
        case ImplementationStatusStateEnum::IMPLEMENTED: return "implemented";
        case ImplementationStatusStateEnum::PARTIAL: return "partial";
        case ImplementationStatusStateEnum::PLANNED: return "planned";
        case ImplementationStatusStateEnum::ALTERNATIVE: return "alternative";
        case ImplementationStatusStateEnum::NOT-APPLICABLE: return "not-applicable";
    }
    return "unknown";
}

/// Parse a string into ImplementationStatusStateEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationStatusStateEnum& out) {
    if (std::strcmp(str, "implemented") == 0) { out = ImplementationStatusStateEnum::IMPLEMENTED; return true; }
    if (std::strcmp(str, "partial") == 0) { out = ImplementationStatusStateEnum::PARTIAL; return true; }
    if (std::strcmp(str, "planned") == 0) { out = ImplementationStatusStateEnum::PLANNED; return true; }
    if (std::strcmp(str, "alternative") == 0) { out = ImplementationStatusStateEnum::ALTERNATIVE; return true; }
    if (std::strcmp(str, "not-applicable") == 0) { out = ImplementationStatusStateEnum::NOT-APPLICABLE; return true; }
    return false;
}

/// Curated component type values. Other values are permitted (OSCAL allow-other="yes").
enum class ComponentTypeEnum {
    THIS-SYSTEM,
    SYSTEM,
    INTERCONNECTION,
    SOFTWARE,
    HARDWARE,
    SERVICE,
    POLICY,
    PHYSICAL,
    PROCESS-PROCEDURE,
    PLAN,
    GUIDANCE,
    STANDARD,
    VALIDATION,
    NETWORK
};

/// Convert ComponentTypeEnum to its string representation.
inline const char* to_string(ComponentTypeEnum value) {
    switch (value) {
        case ComponentTypeEnum::THIS-SYSTEM: return "this-system";
        case ComponentTypeEnum::SYSTEM: return "system";
        case ComponentTypeEnum::INTERCONNECTION: return "interconnection";
        case ComponentTypeEnum::SOFTWARE: return "software";
        case ComponentTypeEnum::HARDWARE: return "hardware";
        case ComponentTypeEnum::SERVICE: return "service";
        case ComponentTypeEnum::POLICY: return "policy";
        case ComponentTypeEnum::PHYSICAL: return "physical";
        case ComponentTypeEnum::PROCESS-PROCEDURE: return "process-procedure";
        case ComponentTypeEnum::PLAN: return "plan";
        case ComponentTypeEnum::GUIDANCE: return "guidance";
        case ComponentTypeEnum::STANDARD: return "standard";
        case ComponentTypeEnum::VALIDATION: return "validation";
        case ComponentTypeEnum::NETWORK: return "network";
    }
    return "unknown";
}

/// Parse a string into ComponentTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ComponentTypeEnum& out) {
    if (std::strcmp(str, "this-system") == 0) { out = ComponentTypeEnum::THIS-SYSTEM; return true; }
    if (std::strcmp(str, "system") == 0) { out = ComponentTypeEnum::SYSTEM; return true; }
    if (std::strcmp(str, "interconnection") == 0) { out = ComponentTypeEnum::INTERCONNECTION; return true; }
    if (std::strcmp(str, "software") == 0) { out = ComponentTypeEnum::SOFTWARE; return true; }
    if (std::strcmp(str, "hardware") == 0) { out = ComponentTypeEnum::HARDWARE; return true; }
    if (std::strcmp(str, "service") == 0) { out = ComponentTypeEnum::SERVICE; return true; }
    if (std::strcmp(str, "policy") == 0) { out = ComponentTypeEnum::POLICY; return true; }
    if (std::strcmp(str, "physical") == 0) { out = ComponentTypeEnum::PHYSICAL; return true; }
    if (std::strcmp(str, "process-procedure") == 0) { out = ComponentTypeEnum::PROCESS-PROCEDURE; return true; }
    if (std::strcmp(str, "plan") == 0) { out = ComponentTypeEnum::PLAN; return true; }
    if (std::strcmp(str, "guidance") == 0) { out = ComponentTypeEnum::GUIDANCE; return true; }
    if (std::strcmp(str, "standard") == 0) { out = ComponentTypeEnum::STANDARD; return true; }
    if (std::strcmp(str, "validation") == 0) { out = ComponentTypeEnum::VALIDATION; return true; }
    if (std::strcmp(str, "network") == 0) { out = ComponentTypeEnum::NETWORK; return true; }
    return false;
}

enum class ComponentStateEnum {
    UNDER-DEVELOPMENT,
    OPERATIONAL,
    DISPOSITION,
    OTHER
};

/// Convert ComponentStateEnum to its string representation.
inline const char* to_string(ComponentStateEnum value) {
    switch (value) {
        case ComponentStateEnum::UNDER-DEVELOPMENT: return "under-development";
        case ComponentStateEnum::OPERATIONAL: return "operational";
        case ComponentStateEnum::DISPOSITION: return "disposition";
        case ComponentStateEnum::OTHER: return "other";
    }
    return "unknown";
}

/// Parse a string into ComponentStateEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ComponentStateEnum& out) {
    if (std::strcmp(str, "under-development") == 0) { out = ComponentStateEnum::UNDER-DEVELOPMENT; return true; }
    if (std::strcmp(str, "operational") == 0) { out = ComponentStateEnum::OPERATIONAL; return true; }
    if (std::strcmp(str, "disposition") == 0) { out = ComponentStateEnum::DISPOSITION; return true; }
    if (std::strcmp(str, "other") == 0) { out = ComponentStateEnum::OTHER; return true; }
    return false;
}

enum class TransportEnum {
    TCP,
    UDP
};

/// Convert TransportEnum to its string representation.
inline const char* to_string(TransportEnum value) {
    switch (value) {
        case TransportEnum::TCP: return "TCP";
        case TransportEnum::UDP: return "UDP";
    }
    return "unknown";
}

/// Parse a string into TransportEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, TransportEnum& out) {
    if (std::strcmp(str, "TCP") == 0) { out = TransportEnum::TCP; return true; }
    if (std::strcmp(str, "UDP") == 0) { out = TransportEnum::UDP; return true; }
    return false;
}

/// Allowed OSCAL implementation-common property names.
enum class ImplementationPropNameEnum {
    IMPLEMENTATION-POINT,
    LEVERAGED-AUTHORIZATION-UUID,
    INHERITED-UUID,
    ASSET-TYPE,
    ASSET-ID,
    ASSET-TAG,
    PUBLIC,
    VIRTUAL,
    VLAN-ID,
    NETWORK-ID,
    LABEL,
    SORT-ID,
    BASELINE-CONFIGURATION-NAME,
    ALLOWS-AUTHENTICATED-SCAN,
    FUNCTION,
    HARDWARE-MODEL,
    MODEL,
    OS-NAME,
    OS-VERSION,
    SOFTWARE-NAME,
    SOFTWARE-VERSION,
    SOFTWARE-PATCH-LEVEL,
    VERSION,
    PATCH-LEVEL,
    RELEASE-DATE,
    VALIDATION-TYPE,
    VALIDATION-REFERENCE,
    VENDOR-NAME,
    SOFTWARE-IDENTIFIER,
    ISA-TITLE,
    ISA-DATE,
    ISA-REMOTE-SYSTEM-NAME,
    IPV4-ADDRESS,
    IPV6-ADDRESS,
    DIRECTION,
    URI,
    FQDN,
    SERIAL-NUMBER,
    NETBIOS-NAME,
    MAC-ADDRESS,
    PHYSICAL-LOCATION,
    IS-SCANNED,
    TYPE,
    PRIVILEGE-LEVEL
};

/// Convert ImplementationPropNameEnum to its string representation.
inline const char* to_string(ImplementationPropNameEnum value) {
    switch (value) {
        case ImplementationPropNameEnum::IMPLEMENTATION-POINT: return "implementation-point";
        case ImplementationPropNameEnum::LEVERAGED-AUTHORIZATION-UUID: return "leveraged-authorization-uuid";
        case ImplementationPropNameEnum::INHERITED-UUID: return "inherited-uuid";
        case ImplementationPropNameEnum::ASSET-TYPE: return "asset-type";
        case ImplementationPropNameEnum::ASSET-ID: return "asset-id";
        case ImplementationPropNameEnum::ASSET-TAG: return "asset-tag";
        case ImplementationPropNameEnum::PUBLIC: return "public";
        case ImplementationPropNameEnum::VIRTUAL: return "virtual";
        case ImplementationPropNameEnum::VLAN-ID: return "vlan-id";
        case ImplementationPropNameEnum::NETWORK-ID: return "network-id";
        case ImplementationPropNameEnum::LABEL: return "label";
        case ImplementationPropNameEnum::SORT-ID: return "sort-id";
        case ImplementationPropNameEnum::BASELINE-CONFIGURATION-NAME: return "baseline-configuration-name";
        case ImplementationPropNameEnum::ALLOWS-AUTHENTICATED-SCAN: return "allows-authenticated-scan";
        case ImplementationPropNameEnum::FUNCTION: return "function";
        case ImplementationPropNameEnum::HARDWARE-MODEL: return "hardware-model";
        case ImplementationPropNameEnum::MODEL: return "model";
        case ImplementationPropNameEnum::OS-NAME: return "os-name";
        case ImplementationPropNameEnum::OS-VERSION: return "os-version";
        case ImplementationPropNameEnum::SOFTWARE-NAME: return "software-name";
        case ImplementationPropNameEnum::SOFTWARE-VERSION: return "software-version";
        case ImplementationPropNameEnum::SOFTWARE-PATCH-LEVEL: return "software-patch-level";
        case ImplementationPropNameEnum::VERSION: return "version";
        case ImplementationPropNameEnum::PATCH-LEVEL: return "patch-level";
        case ImplementationPropNameEnum::RELEASE-DATE: return "release-date";
        case ImplementationPropNameEnum::VALIDATION-TYPE: return "validation-type";
        case ImplementationPropNameEnum::VALIDATION-REFERENCE: return "validation-reference";
        case ImplementationPropNameEnum::VENDOR-NAME: return "vendor-name";
        case ImplementationPropNameEnum::SOFTWARE-IDENTIFIER: return "software-identifier";
        case ImplementationPropNameEnum::ISA-TITLE: return "isa-title";
        case ImplementationPropNameEnum::ISA-DATE: return "isa-date";
        case ImplementationPropNameEnum::ISA-REMOTE-SYSTEM-NAME: return "isa-remote-system-name";
        case ImplementationPropNameEnum::IPV4-ADDRESS: return "ipv4-address";
        case ImplementationPropNameEnum::IPV6-ADDRESS: return "ipv6-address";
        case ImplementationPropNameEnum::DIRECTION: return "direction";
        case ImplementationPropNameEnum::URI: return "uri";
        case ImplementationPropNameEnum::FQDN: return "fqdn";
        case ImplementationPropNameEnum::SERIAL-NUMBER: return "serial-number";
        case ImplementationPropNameEnum::NETBIOS-NAME: return "netbios-name";
        case ImplementationPropNameEnum::MAC-ADDRESS: return "mac-address";
        case ImplementationPropNameEnum::PHYSICAL-LOCATION: return "physical-location";
        case ImplementationPropNameEnum::IS-SCANNED: return "is-scanned";
        case ImplementationPropNameEnum::TYPE: return "type";
        case ImplementationPropNameEnum::PRIVILEGE-LEVEL: return "privilege-level";
    }
    return "unknown";
}

/// Parse a string into ImplementationPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationPropNameEnum& out) {
    if (std::strcmp(str, "implementation-point") == 0) { out = ImplementationPropNameEnum::IMPLEMENTATION-POINT; return true; }
    if (std::strcmp(str, "leveraged-authorization-uuid") == 0) { out = ImplementationPropNameEnum::LEVERAGED-AUTHORIZATION-UUID; return true; }
    if (std::strcmp(str, "inherited-uuid") == 0) { out = ImplementationPropNameEnum::INHERITED-UUID; return true; }
    if (std::strcmp(str, "asset-type") == 0) { out = ImplementationPropNameEnum::ASSET-TYPE; return true; }
    if (std::strcmp(str, "asset-id") == 0) { out = ImplementationPropNameEnum::ASSET-ID; return true; }
    if (std::strcmp(str, "asset-tag") == 0) { out = ImplementationPropNameEnum::ASSET-TAG; return true; }
    if (std::strcmp(str, "public") == 0) { out = ImplementationPropNameEnum::PUBLIC; return true; }
    if (std::strcmp(str, "virtual") == 0) { out = ImplementationPropNameEnum::VIRTUAL; return true; }
    if (std::strcmp(str, "vlan-id") == 0) { out = ImplementationPropNameEnum::VLAN-ID; return true; }
    if (std::strcmp(str, "network-id") == 0) { out = ImplementationPropNameEnum::NETWORK-ID; return true; }
    if (std::strcmp(str, "label") == 0) { out = ImplementationPropNameEnum::LABEL; return true; }
    if (std::strcmp(str, "sort-id") == 0) { out = ImplementationPropNameEnum::SORT-ID; return true; }
    if (std::strcmp(str, "baseline-configuration-name") == 0) { out = ImplementationPropNameEnum::BASELINE-CONFIGURATION-NAME; return true; }
    if (std::strcmp(str, "allows-authenticated-scan") == 0) { out = ImplementationPropNameEnum::ALLOWS-AUTHENTICATED-SCAN; return true; }
    if (std::strcmp(str, "function") == 0) { out = ImplementationPropNameEnum::FUNCTION; return true; }
    if (std::strcmp(str, "hardware-model") == 0) { out = ImplementationPropNameEnum::HARDWARE-MODEL; return true; }
    if (std::strcmp(str, "model") == 0) { out = ImplementationPropNameEnum::MODEL; return true; }
    if (std::strcmp(str, "os-name") == 0) { out = ImplementationPropNameEnum::OS-NAME; return true; }
    if (std::strcmp(str, "os-version") == 0) { out = ImplementationPropNameEnum::OS-VERSION; return true; }
    if (std::strcmp(str, "software-name") == 0) { out = ImplementationPropNameEnum::SOFTWARE-NAME; return true; }
    if (std::strcmp(str, "software-version") == 0) { out = ImplementationPropNameEnum::SOFTWARE-VERSION; return true; }
    if (std::strcmp(str, "software-patch-level") == 0) { out = ImplementationPropNameEnum::SOFTWARE-PATCH-LEVEL; return true; }
    if (std::strcmp(str, "version") == 0) { out = ImplementationPropNameEnum::VERSION; return true; }
    if (std::strcmp(str, "patch-level") == 0) { out = ImplementationPropNameEnum::PATCH-LEVEL; return true; }
    if (std::strcmp(str, "release-date") == 0) { out = ImplementationPropNameEnum::RELEASE-DATE; return true; }
    if (std::strcmp(str, "validation-type") == 0) { out = ImplementationPropNameEnum::VALIDATION-TYPE; return true; }
    if (std::strcmp(str, "validation-reference") == 0) { out = ImplementationPropNameEnum::VALIDATION-REFERENCE; return true; }
    if (std::strcmp(str, "vendor-name") == 0) { out = ImplementationPropNameEnum::VENDOR-NAME; return true; }
    if (std::strcmp(str, "software-identifier") == 0) { out = ImplementationPropNameEnum::SOFTWARE-IDENTIFIER; return true; }
    if (std::strcmp(str, "isa-title") == 0) { out = ImplementationPropNameEnum::ISA-TITLE; return true; }
    if (std::strcmp(str, "isa-date") == 0) { out = ImplementationPropNameEnum::ISA-DATE; return true; }
    if (std::strcmp(str, "isa-remote-system-name") == 0) { out = ImplementationPropNameEnum::ISA-REMOTE-SYSTEM-NAME; return true; }
    if (std::strcmp(str, "ipv4-address") == 0) { out = ImplementationPropNameEnum::IPV4-ADDRESS; return true; }
    if (std::strcmp(str, "ipv6-address") == 0) { out = ImplementationPropNameEnum::IPV6-ADDRESS; return true; }
    if (std::strcmp(str, "direction") == 0) { out = ImplementationPropNameEnum::DIRECTION; return true; }
    if (std::strcmp(str, "uri") == 0) { out = ImplementationPropNameEnum::URI; return true; }
    if (std::strcmp(str, "fqdn") == 0) { out = ImplementationPropNameEnum::FQDN; return true; }
    if (std::strcmp(str, "serial-number") == 0) { out = ImplementationPropNameEnum::SERIAL-NUMBER; return true; }
    if (std::strcmp(str, "netbios-name") == 0) { out = ImplementationPropNameEnum::NETBIOS-NAME; return true; }
    if (std::strcmp(str, "mac-address") == 0) { out = ImplementationPropNameEnum::MAC-ADDRESS; return true; }
    if (std::strcmp(str, "physical-location") == 0) { out = ImplementationPropNameEnum::PHYSICAL-LOCATION; return true; }
    if (std::strcmp(str, "is-scanned") == 0) { out = ImplementationPropNameEnum::IS-SCANNED; return true; }
    if (std::strcmp(str, "type") == 0) { out = ImplementationPropNameEnum::TYPE; return true; }
    if (std::strcmp(str, "privilege-level") == 0) { out = ImplementationPropNameEnum::PRIVILEGE-LEVEL; return true; }
    return false;
}

/// Curated implementation-common link relation values. Other values are permitted (OSCAL allow-other="yes").
enum class ImplementationLinkRelEnum {
    DEPENDS-ON,
    VALIDATION,
    PROOF-OF-COMPLIANCE,
    BASELINE-TEMPLATE,
    USES-SERVICE,
    SYSTEM-SECURITY-PLAN,
    USES-NETWORK,
    IMPORTED-FROM,
    VALIDATION-DETAILS,
    PROVIDED-BY,
    USED-BY,
    ISA-AGREEMENT
};

/// Convert ImplementationLinkRelEnum to its string representation.
inline const char* to_string(ImplementationLinkRelEnum value) {
    switch (value) {
        case ImplementationLinkRelEnum::DEPENDS-ON: return "depends-on";
        case ImplementationLinkRelEnum::VALIDATION: return "validation";
        case ImplementationLinkRelEnum::PROOF-OF-COMPLIANCE: return "proof-of-compliance";
        case ImplementationLinkRelEnum::BASELINE-TEMPLATE: return "baseline-template";
        case ImplementationLinkRelEnum::USES-SERVICE: return "uses-service";
        case ImplementationLinkRelEnum::SYSTEM-SECURITY-PLAN: return "system-security-plan";
        case ImplementationLinkRelEnum::USES-NETWORK: return "uses-network";
        case ImplementationLinkRelEnum::IMPORTED-FROM: return "imported-from";
        case ImplementationLinkRelEnum::VALIDATION-DETAILS: return "validation-details";
        case ImplementationLinkRelEnum::PROVIDED-BY: return "provided-by";
        case ImplementationLinkRelEnum::USED-BY: return "used-by";
        case ImplementationLinkRelEnum::ISA-AGREEMENT: return "isa-agreement";
    }
    return "unknown";
}

/// Parse a string into ImplementationLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationLinkRelEnum& out) {
    if (std::strcmp(str, "depends-on") == 0) { out = ImplementationLinkRelEnum::DEPENDS-ON; return true; }
    if (std::strcmp(str, "validation") == 0) { out = ImplementationLinkRelEnum::VALIDATION; return true; }
    if (std::strcmp(str, "proof-of-compliance") == 0) { out = ImplementationLinkRelEnum::PROOF-OF-COMPLIANCE; return true; }
    if (std::strcmp(str, "baseline-template") == 0) { out = ImplementationLinkRelEnum::BASELINE-TEMPLATE; return true; }
    if (std::strcmp(str, "uses-service") == 0) { out = ImplementationLinkRelEnum::USES-SERVICE; return true; }
    if (std::strcmp(str, "system-security-plan") == 0) { out = ImplementationLinkRelEnum::SYSTEM-SECURITY-PLAN; return true; }
    if (std::strcmp(str, "uses-network") == 0) { out = ImplementationLinkRelEnum::USES-NETWORK; return true; }
    if (std::strcmp(str, "imported-from") == 0) { out = ImplementationLinkRelEnum::IMPORTED-FROM; return true; }
    if (std::strcmp(str, "validation-details") == 0) { out = ImplementationLinkRelEnum::VALIDATION-DETAILS; return true; }
    if (std::strcmp(str, "provided-by") == 0) { out = ImplementationLinkRelEnum::PROVIDED-BY; return true; }
    if (std::strcmp(str, "used-by") == 0) { out = ImplementationLinkRelEnum::USED-BY; return true; }
    if (std::strcmp(str, "isa-agreement") == 0) { out = ImplementationLinkRelEnum::ISA-AGREEMENT; return true; }
    return false;
}

/// Curated implementation asset type values. Other values are permitted (OSCAL allow-other="yes").
enum class ImplementationAssetTypeEnum {
    OPERATING-SYSTEM,
    DATABASE,
    WEB-SERVER,
    DNS-SERVER,
    EMAIL-SERVER,
    DIRECTORY-SERVER,
    PBX,
    FIREWALL,
    ROUTER,
    SWITCH,
    STORAGE-ARRAY,
    APPLIANCE
};

/// Convert ImplementationAssetTypeEnum to its string representation.
inline const char* to_string(ImplementationAssetTypeEnum value) {
    switch (value) {
        case ImplementationAssetTypeEnum::OPERATING-SYSTEM: return "operating-system";
        case ImplementationAssetTypeEnum::DATABASE: return "database";
        case ImplementationAssetTypeEnum::WEB-SERVER: return "web-server";
        case ImplementationAssetTypeEnum::DNS-SERVER: return "dns-server";
        case ImplementationAssetTypeEnum::EMAIL-SERVER: return "email-server";
        case ImplementationAssetTypeEnum::DIRECTORY-SERVER: return "directory-server";
        case ImplementationAssetTypeEnum::PBX: return "pbx";
        case ImplementationAssetTypeEnum::FIREWALL: return "firewall";
        case ImplementationAssetTypeEnum::ROUTER: return "router";
        case ImplementationAssetTypeEnum::SWITCH: return "switch";
        case ImplementationAssetTypeEnum::STORAGE-ARRAY: return "storage-array";
        case ImplementationAssetTypeEnum::APPLIANCE: return "appliance";
    }
    return "unknown";
}

/// Parse a string into ImplementationAssetTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationAssetTypeEnum& out) {
    if (std::strcmp(str, "operating-system") == 0) { out = ImplementationAssetTypeEnum::OPERATING-SYSTEM; return true; }
    if (std::strcmp(str, "database") == 0) { out = ImplementationAssetTypeEnum::DATABASE; return true; }
    if (std::strcmp(str, "web-server") == 0) { out = ImplementationAssetTypeEnum::WEB-SERVER; return true; }
    if (std::strcmp(str, "dns-server") == 0) { out = ImplementationAssetTypeEnum::DNS-SERVER; return true; }
    if (std::strcmp(str, "email-server") == 0) { out = ImplementationAssetTypeEnum::EMAIL-SERVER; return true; }
    if (std::strcmp(str, "directory-server") == 0) { out = ImplementationAssetTypeEnum::DIRECTORY-SERVER; return true; }
    if (std::strcmp(str, "pbx") == 0) { out = ImplementationAssetTypeEnum::PBX; return true; }
    if (std::strcmp(str, "firewall") == 0) { out = ImplementationAssetTypeEnum::FIREWALL; return true; }
    if (std::strcmp(str, "router") == 0) { out = ImplementationAssetTypeEnum::ROUTER; return true; }
    if (std::strcmp(str, "switch") == 0) { out = ImplementationAssetTypeEnum::SWITCH; return true; }
    if (std::strcmp(str, "storage-array") == 0) { out = ImplementationAssetTypeEnum::STORAGE-ARRAY; return true; }
    if (std::strcmp(str, "appliance") == 0) { out = ImplementationAssetTypeEnum::APPLIANCE; return true; }
    return false;
}

enum class ImplementationPointEnum {
    INTERNAL,
    EXTERNAL
};

/// Convert ImplementationPointEnum to its string representation.
inline const char* to_string(ImplementationPointEnum value) {
    switch (value) {
        case ImplementationPointEnum::INTERNAL: return "internal";
        case ImplementationPointEnum::EXTERNAL: return "external";
    }
    return "unknown";
}

/// Parse a string into ImplementationPointEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationPointEnum& out) {
    if (std::strcmp(str, "internal") == 0) { out = ImplementationPointEnum::INTERNAL; return true; }
    if (std::strcmp(str, "external") == 0) { out = ImplementationPointEnum::EXTERNAL; return true; }
    return false;
}

enum class ImplementationIpAddressClassEnum {
    LOCAL,
    REMOTE
};

/// Convert ImplementationIpAddressClassEnum to its string representation.
inline const char* to_string(ImplementationIpAddressClassEnum value) {
    switch (value) {
        case ImplementationIpAddressClassEnum::LOCAL: return "local";
        case ImplementationIpAddressClassEnum::REMOTE: return "remote";
    }
    return "unknown";
}

/// Parse a string into ImplementationIpAddressClassEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationIpAddressClassEnum& out) {
    if (std::strcmp(str, "local") == 0) { out = ImplementationIpAddressClassEnum::LOCAL; return true; }
    if (std::strcmp(str, "remote") == 0) { out = ImplementationIpAddressClassEnum::REMOTE; return true; }
    return false;
}

enum class ImplementationDirectionEnum {
    INCOMING,
    OUTGOING
};

/// Convert ImplementationDirectionEnum to its string representation.
inline const char* to_string(ImplementationDirectionEnum value) {
    switch (value) {
        case ImplementationDirectionEnum::INCOMING: return "incoming";
        case ImplementationDirectionEnum::OUTGOING: return "outgoing";
    }
    return "unknown";
}

/// Parse a string into ImplementationDirectionEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationDirectionEnum& out) {
    if (std::strcmp(str, "incoming") == 0) { out = ImplementationDirectionEnum::INCOMING; return true; }
    if (std::strcmp(str, "outgoing") == 0) { out = ImplementationDirectionEnum::OUTGOING; return true; }
    return false;
}

enum class UserTypeEnum {
    INTERNAL,
    EXTERNAL,
    GENERAL-PUBLIC
};

/// Convert UserTypeEnum to its string representation.
inline const char* to_string(UserTypeEnum value) {
    switch (value) {
        case UserTypeEnum::INTERNAL: return "internal";
        case UserTypeEnum::EXTERNAL: return "external";
        case UserTypeEnum::GENERAL-PUBLIC: return "general-public";
    }
    return "unknown";
}

/// Parse a string into UserTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, UserTypeEnum& out) {
    if (std::strcmp(str, "internal") == 0) { out = UserTypeEnum::INTERNAL; return true; }
    if (std::strcmp(str, "external") == 0) { out = UserTypeEnum::EXTERNAL; return true; }
    if (std::strcmp(str, "general-public") == 0) { out = UserTypeEnum::GENERAL-PUBLIC; return true; }
    return false;
}

enum class UserPrivilegeLevelEnum {
    PRIVILEGED,
    NON-PRIVILEGED,
    NO-LOGICAL-ACCESS
};

/// Convert UserPrivilegeLevelEnum to its string representation.
inline const char* to_string(UserPrivilegeLevelEnum value) {
    switch (value) {
        case UserPrivilegeLevelEnum::PRIVILEGED: return "privileged";
        case UserPrivilegeLevelEnum::NON-PRIVILEGED: return "non-privileged";
        case UserPrivilegeLevelEnum::NO-LOGICAL-ACCESS: return "no-logical-access";
    }
    return "unknown";
}

/// Parse a string into UserPrivilegeLevelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, UserPrivilegeLevelEnum& out) {
    if (std::strcmp(str, "privileged") == 0) { out = UserPrivilegeLevelEnum::PRIVILEGED; return true; }
    if (std::strcmp(str, "non-privileged") == 0) { out = UserPrivilegeLevelEnum::NON-PRIVILEGED; return true; }
    if (std::strcmp(str, "no-logical-access") == 0) { out = UserPrivilegeLevelEnum::NO-LOGICAL-ACCESS; return true; }
    return false;
}

/// Curated interconnection responsible-role identifiers. Other values are permitted (OSCAL allow-other="yes").
enum class InterconnectionResponsibleRoleIdEnum {
    ISA-POC-LOCAL,
    ISA-POC-REMOTE,
    ISA-AUTHORIZING-OFFICIAL-LOCAL,
    ISA-AUTHORIZING-OFFICIAL-REMOTE
};

/// Convert InterconnectionResponsibleRoleIdEnum to its string representation.
inline const char* to_string(InterconnectionResponsibleRoleIdEnum value) {
    switch (value) {
        case InterconnectionResponsibleRoleIdEnum::ISA-POC-LOCAL: return "isa-poc-local";
        case InterconnectionResponsibleRoleIdEnum::ISA-POC-REMOTE: return "isa-poc-remote";
        case InterconnectionResponsibleRoleIdEnum::ISA-AUTHORIZING-OFFICIAL-LOCAL: return "isa-authorizing-official-local";
        case InterconnectionResponsibleRoleIdEnum::ISA-AUTHORIZING-OFFICIAL-REMOTE: return "isa-authorizing-official-remote";
    }
    return "unknown";
}

/// Parse a string into InterconnectionResponsibleRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, InterconnectionResponsibleRoleIdEnum& out) {
    if (std::strcmp(str, "isa-poc-local") == 0) { out = InterconnectionResponsibleRoleIdEnum::ISA-POC-LOCAL; return true; }
    if (std::strcmp(str, "isa-poc-remote") == 0) { out = InterconnectionResponsibleRoleIdEnum::ISA-POC-REMOTE; return true; }
    if (std::strcmp(str, "isa-authorizing-official-local") == 0) { out = InterconnectionResponsibleRoleIdEnum::ISA-AUTHORIZING-OFFICIAL-LOCAL; return true; }
    if (std::strcmp(str, "isa-authorizing-official-remote") == 0) { out = InterconnectionResponsibleRoleIdEnum::ISA-AUTHORIZING-OFFICIAL-REMOTE; return true; }
    return false;
}

/// Curated implementation-common role identifiers used in responsible role and responsible party contexts. Other values are permitted (OSCAL allow-other="yes").
enum class ImplementationResponsibleRoleIdEnum {
    ASSET-OWNER,
    ASSET-ADMINISTRATOR,
    SECURITY-OPERATIONS,
    NETWORK-OPERATIONS,
    INCIDENT-RESPONSE,
    HELP-DESK,
    CONFIGURATION-MANAGEMENT,
    MAINTAINER,
    PROVIDER
};

/// Convert ImplementationResponsibleRoleIdEnum to its string representation.
inline const char* to_string(ImplementationResponsibleRoleIdEnum value) {
    switch (value) {
        case ImplementationResponsibleRoleIdEnum::ASSET-OWNER: return "asset-owner";
        case ImplementationResponsibleRoleIdEnum::ASSET-ADMINISTRATOR: return "asset-administrator";
        case ImplementationResponsibleRoleIdEnum::SECURITY-OPERATIONS: return "security-operations";
        case ImplementationResponsibleRoleIdEnum::NETWORK-OPERATIONS: return "network-operations";
        case ImplementationResponsibleRoleIdEnum::INCIDENT-RESPONSE: return "incident-response";
        case ImplementationResponsibleRoleIdEnum::HELP-DESK: return "help-desk";
        case ImplementationResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT: return "configuration-management";
        case ImplementationResponsibleRoleIdEnum::MAINTAINER: return "maintainer";
        case ImplementationResponsibleRoleIdEnum::PROVIDER: return "provider";
    }
    return "unknown";
}

/// Parse a string into ImplementationResponsibleRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationResponsibleRoleIdEnum& out) {
    if (std::strcmp(str, "asset-owner") == 0) { out = ImplementationResponsibleRoleIdEnum::ASSET-OWNER; return true; }
    if (std::strcmp(str, "asset-administrator") == 0) { out = ImplementationResponsibleRoleIdEnum::ASSET-ADMINISTRATOR; return true; }
    if (std::strcmp(str, "security-operations") == 0) { out = ImplementationResponsibleRoleIdEnum::SECURITY-OPERATIONS; return true; }
    if (std::strcmp(str, "network-operations") == 0) { out = ImplementationResponsibleRoleIdEnum::NETWORK-OPERATIONS; return true; }
    if (std::strcmp(str, "incident-response") == 0) { out = ImplementationResponsibleRoleIdEnum::INCIDENT-RESPONSE; return true; }
    if (std::strcmp(str, "help-desk") == 0) { out = ImplementationResponsibleRoleIdEnum::HELP-DESK; return true; }
    if (std::strcmp(str, "configuration-management") == 0) { out = ImplementationResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT; return true; }
    if (std::strcmp(str, "maintainer") == 0) { out = ImplementationResponsibleRoleIdEnum::MAINTAINER; return true; }
    if (std::strcmp(str, "provider") == 0) { out = ImplementationResponsibleRoleIdEnum::PROVIDER; return true; }
    return false;
}

/// Implementation-common yes/no value set used by several properties.
enum class ImplementationYesNoEnum {
    YES,
    NO
};

/// Convert ImplementationYesNoEnum to its string representation.
inline const char* to_string(ImplementationYesNoEnum value) {
    switch (value) {
        case ImplementationYesNoEnum::YES: return "yes";
        case ImplementationYesNoEnum::NO: return "no";
    }
    return "unknown";
}

/// Parse a string into ImplementationYesNoEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementationYesNoEnum& out) {
    if (std::strcmp(str, "yes") == 0) { out = ImplementationYesNoEnum::YES; return true; }
    if (std::strcmp(str, "no") == 0) { out = ImplementationYesNoEnum::NO; return true; }
    return false;
}

/// Curated system identifier type URIs. Other values are permitted (OSCAL allow-other="yes").
enum class SystemIdentifierTypeEnum {
    HTTP://FEDRAMP.GOV,
    HTTP://FEDRAMP.GOV/NS/OSCAL,
    HTTPS://IETF.ORG/RFC/RFC4122,
    HTTP://IETF.ORG/RFC/RFC4122,
    HTTP://DATATRACKER.IETF.ORG/DOC/HTML/RFC4122
};

/// Convert SystemIdentifierTypeEnum to its string representation.
inline const char* to_string(SystemIdentifierTypeEnum value) {
    switch (value) {
        case SystemIdentifierTypeEnum::HTTP://FEDRAMP.GOV: return "http://fedramp.gov";
        case SystemIdentifierTypeEnum::HTTP://FEDRAMP.GOV/NS/OSCAL: return "http://fedramp.gov/ns/oscal";
        case SystemIdentifierTypeEnum::HTTPS://IETF.ORG/RFC/RFC4122: return "https://ietf.org/rfc/rfc4122";
        case SystemIdentifierTypeEnum::HTTP://IETF.ORG/RFC/RFC4122: return "http://ietf.org/rfc/rfc4122";
        case SystemIdentifierTypeEnum::HTTP://DATATRACKER.IETF.ORG/DOC/HTML/RFC4122: return "http://datatracker.ietf.org/doc/html/rfc4122";
    }
    return "unknown";
}

/// Parse a string into SystemIdentifierTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemIdentifierTypeEnum& out) {
    if (std::strcmp(str, "http://fedramp.gov") == 0) { out = SystemIdentifierTypeEnum::HTTP://FEDRAMP.GOV; return true; }
    if (std::strcmp(str, "http://fedramp.gov/ns/oscal") == 0) { out = SystemIdentifierTypeEnum::HTTP://FEDRAMP.GOV/NS/OSCAL; return true; }
    if (std::strcmp(str, "https://ietf.org/rfc/rfc4122") == 0) { out = SystemIdentifierTypeEnum::HTTPS://IETF.ORG/RFC/RFC4122; return true; }
    if (std::strcmp(str, "http://ietf.org/rfc/rfc4122") == 0) { out = SystemIdentifierTypeEnum::HTTP://IETF.ORG/RFC/RFC4122; return true; }
    if (std::strcmp(str, "http://datatracker.ietf.org/doc/html/rfc4122") == 0) { out = SystemIdentifierTypeEnum::HTTP://DATATRACKER.IETF.ORG/DOC/HTML/RFC4122; return true; }
    return false;
}

/// Curated assessment part name values. Other values are permitted (OSCAL allow-other="yes").
enum class AssessmentPartNameEnum {
    ASSET,
    METHOD,
    OBJECTIVE
};

/// Convert AssessmentPartNameEnum to its string representation.
inline const char* to_string(AssessmentPartNameEnum value) {
    switch (value) {
        case AssessmentPartNameEnum::ASSET: return "asset";
        case AssessmentPartNameEnum::METHOD: return "method";
        case AssessmentPartNameEnum::OBJECTIVE: return "objective";
    }
    return "unknown";
}

/// Parse a string into AssessmentPartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AssessmentPartNameEnum& out) {
    if (std::strcmp(str, "asset") == 0) { out = AssessmentPartNameEnum::ASSET; return true; }
    if (std::strcmp(str, "method") == 0) { out = AssessmentPartNameEnum::METHOD; return true; }
    if (std::strcmp(str, "objective") == 0) { out = AssessmentPartNameEnum::OBJECTIVE; return true; }
    return false;
}

/// Allowed part names in assessment plan terms-and-conditions.
enum class TermsAndConditionsPartNameEnum {
    RULES-OF-ENGAGEMENT,
    DISCLOSURES,
    ASSESSMENT-INCLUSIONS,
    ASSESSMENT-EXCLUSIONS,
    RESULTS-DELIVERY,
    ASSUMPTIONS,
    METHODOLOGY
};

/// Convert TermsAndConditionsPartNameEnum to its string representation.
inline const char* to_string(TermsAndConditionsPartNameEnum value) {
    switch (value) {
        case TermsAndConditionsPartNameEnum::RULES-OF-ENGAGEMENT: return "rules-of-engagement";
        case TermsAndConditionsPartNameEnum::DISCLOSURES: return "disclosures";
        case TermsAndConditionsPartNameEnum::ASSESSMENT-INCLUSIONS: return "assessment-inclusions";
        case TermsAndConditionsPartNameEnum::ASSESSMENT-EXCLUSIONS: return "assessment-exclusions";
        case TermsAndConditionsPartNameEnum::RESULTS-DELIVERY: return "results-delivery";
        case TermsAndConditionsPartNameEnum::ASSUMPTIONS: return "assumptions";
        case TermsAndConditionsPartNameEnum::METHODOLOGY: return "methodology";
    }
    return "unknown";
}

/// Parse a string into TermsAndConditionsPartNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, TermsAndConditionsPartNameEnum& out) {
    if (std::strcmp(str, "rules-of-engagement") == 0) { out = TermsAndConditionsPartNameEnum::RULES-OF-ENGAGEMENT; return true; }
    if (std::strcmp(str, "disclosures") == 0) { out = TermsAndConditionsPartNameEnum::DISCLOSURES; return true; }
    if (std::strcmp(str, "assessment-inclusions") == 0) { out = TermsAndConditionsPartNameEnum::ASSESSMENT-INCLUSIONS; return true; }
    if (std::strcmp(str, "assessment-exclusions") == 0) { out = TermsAndConditionsPartNameEnum::ASSESSMENT-EXCLUSIONS; return true; }
    if (std::strcmp(str, "results-delivery") == 0) { out = TermsAndConditionsPartNameEnum::RESULTS-DELIVERY; return true; }
    if (std::strcmp(str, "assumptions") == 0) { out = TermsAndConditionsPartNameEnum::ASSUMPTIONS; return true; }
    if (std::strcmp(str, "methodology") == 0) { out = TermsAndConditionsPartNameEnum::METHODOLOGY; return true; }
    return false;
}

/// Values from assessment-common set oscal-assessment-objective-types.
enum class OscalAssessmentObjectiveTypesEnum {
    OBJECTIVE,
    ASSESSMENT,
    ASSESSMENT-OBJECTIVE,
    ASSESSMENT-METHOD
};

/// Convert OscalAssessmentObjectiveTypesEnum to its string representation.
inline const char* to_string(OscalAssessmentObjectiveTypesEnum value) {
    switch (value) {
        case OscalAssessmentObjectiveTypesEnum::OBJECTIVE: return "objective";
        case OscalAssessmentObjectiveTypesEnum::ASSESSMENT: return "assessment";
        case OscalAssessmentObjectiveTypesEnum::ASSESSMENT-OBJECTIVE: return "assessment-objective";
        case OscalAssessmentObjectiveTypesEnum::ASSESSMENT-METHOD: return "assessment-method";
    }
    return "unknown";
}

/// Parse a string into OscalAssessmentObjectiveTypesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalAssessmentObjectiveTypesEnum& out) {
    if (std::strcmp(str, "objective") == 0) { out = OscalAssessmentObjectiveTypesEnum::OBJECTIVE; return true; }
    if (std::strcmp(str, "assessment") == 0) { out = OscalAssessmentObjectiveTypesEnum::ASSESSMENT; return true; }
    if (std::strcmp(str, "assessment-objective") == 0) { out = OscalAssessmentObjectiveTypesEnum::ASSESSMENT-OBJECTIVE; return true; }
    if (std::strcmp(str, "assessment-method") == 0) { out = OscalAssessmentObjectiveTypesEnum::ASSESSMENT-METHOD; return true; }
    return false;
}

/// Values from assessment-common set oscal-risk-prop-type-values.
enum class OscalRiskPropTypeValuesEnum {
    VENDOR-CHECK-IN,
    STATUS-UPDATE,
    MILESTONE-COMPLETE,
    MITIGATION,
    REMEDIATED,
    CLOSED,
    DR-SUBMISSION,
    DR-UPDATED,
    DR-APPROVED,
    DR-REJECTED
};

/// Convert OscalRiskPropTypeValuesEnum to its string representation.
inline const char* to_string(OscalRiskPropTypeValuesEnum value) {
    switch (value) {
        case OscalRiskPropTypeValuesEnum::VENDOR-CHECK-IN: return "vendor-check-in";
        case OscalRiskPropTypeValuesEnum::STATUS-UPDATE: return "status-update";
        case OscalRiskPropTypeValuesEnum::MILESTONE-COMPLETE: return "milestone-complete";
        case OscalRiskPropTypeValuesEnum::MITIGATION: return "mitigation";
        case OscalRiskPropTypeValuesEnum::REMEDIATED: return "remediated";
        case OscalRiskPropTypeValuesEnum::CLOSED: return "closed";
        case OscalRiskPropTypeValuesEnum::DR-SUBMISSION: return "dr-submission";
        case OscalRiskPropTypeValuesEnum::DR-UPDATED: return "dr-updated";
        case OscalRiskPropTypeValuesEnum::DR-APPROVED: return "dr-approved";
        case OscalRiskPropTypeValuesEnum::DR-REJECTED: return "dr-rejected";
    }
    return "unknown";
}

/// Parse a string into OscalRiskPropTypeValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalRiskPropTypeValuesEnum& out) {
    if (std::strcmp(str, "vendor-check-in") == 0) { out = OscalRiskPropTypeValuesEnum::VENDOR-CHECK-IN; return true; }
    if (std::strcmp(str, "status-update") == 0) { out = OscalRiskPropTypeValuesEnum::STATUS-UPDATE; return true; }
    if (std::strcmp(str, "milestone-complete") == 0) { out = OscalRiskPropTypeValuesEnum::MILESTONE-COMPLETE; return true; }
    if (std::strcmp(str, "mitigation") == 0) { out = OscalRiskPropTypeValuesEnum::MITIGATION; return true; }
    if (std::strcmp(str, "remediated") == 0) { out = OscalRiskPropTypeValuesEnum::REMEDIATED; return true; }
    if (std::strcmp(str, "closed") == 0) { out = OscalRiskPropTypeValuesEnum::CLOSED; return true; }
    if (std::strcmp(str, "dr-submission") == 0) { out = OscalRiskPropTypeValuesEnum::DR-SUBMISSION; return true; }
    if (std::strcmp(str, "dr-updated") == 0) { out = OscalRiskPropTypeValuesEnum::DR-UPDATED; return true; }
    if (std::strcmp(str, "dr-approved") == 0) { out = OscalRiskPropTypeValuesEnum::DR-APPROVED; return true; }
    if (std::strcmp(str, "dr-rejected") == 0) { out = OscalRiskPropTypeValuesEnum::DR-REJECTED; return true; }
    return false;
}

/// Values from assessment-common set oscal-risk-prop-name-values.
enum class OscalRiskPropNameValuesEnum {
    FALSE-POSITIVE,
    ACCEPTED,
    RISK-ADJUSTED,
    PRIORITY
};

/// Convert OscalRiskPropNameValuesEnum to its string representation.
inline const char* to_string(OscalRiskPropNameValuesEnum value) {
    switch (value) {
        case OscalRiskPropNameValuesEnum::FALSE-POSITIVE: return "false-positive";
        case OscalRiskPropNameValuesEnum::ACCEPTED: return "accepted";
        case OscalRiskPropNameValuesEnum::RISK-ADJUSTED: return "risk-adjusted";
        case OscalRiskPropNameValuesEnum::PRIORITY: return "priority";
    }
    return "unknown";
}

/// Parse a string into OscalRiskPropNameValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalRiskPropNameValuesEnum& out) {
    if (std::strcmp(str, "false-positive") == 0) { out = OscalRiskPropNameValuesEnum::FALSE-POSITIVE; return true; }
    if (std::strcmp(str, "accepted") == 0) { out = OscalRiskPropNameValuesEnum::ACCEPTED; return true; }
    if (std::strcmp(str, "risk-adjusted") == 0) { out = OscalRiskPropNameValuesEnum::RISK-ADJUSTED; return true; }
    if (std::strcmp(str, "priority") == 0) { out = OscalRiskPropNameValuesEnum::PRIORITY; return true; }
    return false;
}

/// Values from assessment-common set oscal-characterization-facet-name-system-values.
enum class OscalCharacterizationFacetNameSystemValuesEnum {
    HTTP://FEDRAMP.GOV,
    HTTP://FEDRAMP.GOV/NS/OSCAL,
    HTTP://CSRC.NIST.GOV/NS/OSCAL,
    HTTP://CSRC.NIST.GOV/NS/OSCAL/UNKNOWN,
    HTTP://CVE.MITRE.ORG,
    HTTP://WWW.FIRST.ORG/CVSS/V2.0,
    HTTP://WWW.FIRST.ORG/CVSS/V3.0,
    HTTP://WWW.FIRST.ORG/CVSS/V3.1,
    HTTPS://WWW.FIRST.ORG/CVSS/V4-0
};

/// Convert OscalCharacterizationFacetNameSystemValuesEnum to its string representation.
inline const char* to_string(OscalCharacterizationFacetNameSystemValuesEnum value) {
    switch (value) {
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://FEDRAMP.GOV: return "http://fedramp.gov";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://FEDRAMP.GOV/NS/OSCAL: return "http://fedramp.gov/ns/oscal";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL: return "http://csrc.nist.gov/ns/oscal";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL/UNKNOWN: return "http://csrc.nist.gov/ns/oscal/unknown";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CVE.MITRE.ORG: return "http://cve.mitre.org";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V2.0: return "http://www.first.org/cvss/v2.0";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V3.0: return "http://www.first.org/cvss/v3.0";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V3.1: return "http://www.first.org/cvss/v3.1";
        case OscalCharacterizationFacetNameSystemValuesEnum::HTTPS://WWW.FIRST.ORG/CVSS/V4-0: return "https://www.first.org/cvss/v4-0";
    }
    return "unknown";
}

/// Parse a string into OscalCharacterizationFacetNameSystemValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCharacterizationFacetNameSystemValuesEnum& out) {
    if (std::strcmp(str, "http://fedramp.gov") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://FEDRAMP.GOV; return true; }
    if (std::strcmp(str, "http://fedramp.gov/ns/oscal") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://FEDRAMP.GOV/NS/OSCAL; return true; }
    if (std::strcmp(str, "http://csrc.nist.gov/ns/oscal") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL; return true; }
    if (std::strcmp(str, "http://csrc.nist.gov/ns/oscal/unknown") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CSRC.NIST.GOV/NS/OSCAL/UNKNOWN; return true; }
    if (std::strcmp(str, "http://cve.mitre.org") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://CVE.MITRE.ORG; return true; }
    if (std::strcmp(str, "http://www.first.org/cvss/v2.0") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V2.0; return true; }
    if (std::strcmp(str, "http://www.first.org/cvss/v3.0") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V3.0; return true; }
    if (std::strcmp(str, "http://www.first.org/cvss/v3.1") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTP://WWW.FIRST.ORG/CVSS/V3.1; return true; }
    if (std::strcmp(str, "https://www.first.org/cvss/v4-0") == 0) { out = OscalCharacterizationFacetNameSystemValuesEnum::HTTPS://WWW.FIRST.ORG/CVSS/V4-0; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-prop-name-values.
enum class OscalFacetPropNameValuesEnum {
    STATE
};

/// Convert OscalFacetPropNameValuesEnum to its string representation.
inline const char* to_string(OscalFacetPropNameValuesEnum value) {
    switch (value) {
        case OscalFacetPropNameValuesEnum::STATE: return "state";
    }
    return "unknown";
}

/// Parse a string into OscalFacetPropNameValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetPropNameValuesEnum& out) {
    if (std::strcmp(str, "state") == 0) { out = OscalFacetPropNameValuesEnum::STATE; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-prop-state-values.
enum class OscalFacetPropStateValuesEnum {
    INITIAL,
    ADJUSTED
};

/// Convert OscalFacetPropStateValuesEnum to its string representation.
inline const char* to_string(OscalFacetPropStateValuesEnum value) {
    switch (value) {
        case OscalFacetPropStateValuesEnum::INITIAL: return "initial";
        case OscalFacetPropStateValuesEnum::ADJUSTED: return "adjusted";
    }
    return "unknown";
}

/// Parse a string into OscalFacetPropStateValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetPropStateValuesEnum& out) {
    if (std::strcmp(str, "initial") == 0) { out = OscalFacetPropStateValuesEnum::INITIAL; return true; }
    if (std::strcmp(str, "adjusted") == 0) { out = OscalFacetPropStateValuesEnum::ADJUSTED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-name-core-values.
enum class OscalFacetNameCoreValuesEnum {
    LIKELIHOOD,
    IMPACT,
    RISK,
    SEVERITY
};

/// Convert OscalFacetNameCoreValuesEnum to its string representation.
inline const char* to_string(OscalFacetNameCoreValuesEnum value) {
    switch (value) {
        case OscalFacetNameCoreValuesEnum::LIKELIHOOD: return "likelihood";
        case OscalFacetNameCoreValuesEnum::IMPACT: return "impact";
        case OscalFacetNameCoreValuesEnum::RISK: return "risk";
        case OscalFacetNameCoreValuesEnum::SEVERITY: return "severity";
    }
    return "unknown";
}

/// Parse a string into OscalFacetNameCoreValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetNameCoreValuesEnum& out) {
    if (std::strcmp(str, "likelihood") == 0) { out = OscalFacetNameCoreValuesEnum::LIKELIHOOD; return true; }
    if (std::strcmp(str, "impact") == 0) { out = OscalFacetNameCoreValuesEnum::IMPACT; return true; }
    if (std::strcmp(str, "risk") == 0) { out = OscalFacetNameCoreValuesEnum::RISK; return true; }
    if (std::strcmp(str, "severity") == 0) { out = OscalFacetNameCoreValuesEnum::SEVERITY; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-fedramp-values.
enum class OscalFacetFedrampValuesEnum {
    LIKELIHOOD,
    IMPACT,
    RISK
};

/// Convert OscalFacetFedrampValuesEnum to its string representation.
inline const char* to_string(OscalFacetFedrampValuesEnum value) {
    switch (value) {
        case OscalFacetFedrampValuesEnum::LIKELIHOOD: return "likelihood";
        case OscalFacetFedrampValuesEnum::IMPACT: return "impact";
        case OscalFacetFedrampValuesEnum::RISK: return "risk";
    }
    return "unknown";
}

/// Parse a string into OscalFacetFedrampValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetFedrampValuesEnum& out) {
    if (std::strcmp(str, "likelihood") == 0) { out = OscalFacetFedrampValuesEnum::LIKELIHOOD; return true; }
    if (std::strcmp(str, "impact") == 0) { out = OscalFacetFedrampValuesEnum::IMPACT; return true; }
    if (std::strcmp(str, "risk") == 0) { out = OscalFacetFedrampValuesEnum::RISK; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cve-values.
enum class OscalFacetCveValuesEnum {
    CVE-ID
};

/// Convert OscalFacetCveValuesEnum to its string representation.
inline const char* to_string(OscalFacetCveValuesEnum value) {
    switch (value) {
        case OscalFacetCveValuesEnum::CVE-ID: return "cve-id";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCveValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCveValuesEnum& out) {
    if (std::strcmp(str, "cve-id") == 0) { out = OscalFacetCveValuesEnum::CVE-ID; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-name-values.
enum class OscalFacetCvss2NameValuesEnum {
    ACCESS-VECTOR,
    ACCESS-COMPLEXITY,
    AUTHENTICATION,
    CONFIDENTIALITY-IMPACT,
    INTEGRITY-IMPACT,
    AVAILABILITY-IMPACT,
    EXPLOITABILITY,
    REMEDIATION-LEVEL,
    REPORT-CONFIDENCE,
    COLLATERAL-DAMAGE-POTENTIAL,
    TARGET-DISTRIBUTION,
    CONFIDENTIALITY-REQUIREMENT,
    INTEGRITY-REQUIREMENT,
    AVAILABILITY-REQUIREMENT
};

/// Convert OscalFacetCvss2NameValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2NameValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2NameValuesEnum::ACCESS-VECTOR: return "access-vector";
        case OscalFacetCvss2NameValuesEnum::ACCESS-COMPLEXITY: return "access-complexity";
        case OscalFacetCvss2NameValuesEnum::AUTHENTICATION: return "authentication";
        case OscalFacetCvss2NameValuesEnum::CONFIDENTIALITY-IMPACT: return "confidentiality-impact";
        case OscalFacetCvss2NameValuesEnum::INTEGRITY-IMPACT: return "integrity-impact";
        case OscalFacetCvss2NameValuesEnum::AVAILABILITY-IMPACT: return "availability-impact";
        case OscalFacetCvss2NameValuesEnum::EXPLOITABILITY: return "exploitability";
        case OscalFacetCvss2NameValuesEnum::REMEDIATION-LEVEL: return "remediation-level";
        case OscalFacetCvss2NameValuesEnum::REPORT-CONFIDENCE: return "report-confidence";
        case OscalFacetCvss2NameValuesEnum::COLLATERAL-DAMAGE-POTENTIAL: return "collateral-damage-potential";
        case OscalFacetCvss2NameValuesEnum::TARGET-DISTRIBUTION: return "target-distribution";
        case OscalFacetCvss2NameValuesEnum::CONFIDENTIALITY-REQUIREMENT: return "confidentiality-requirement";
        case OscalFacetCvss2NameValuesEnum::INTEGRITY-REQUIREMENT: return "integrity-requirement";
        case OscalFacetCvss2NameValuesEnum::AVAILABILITY-REQUIREMENT: return "availability-requirement";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2NameValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2NameValuesEnum& out) {
    if (std::strcmp(str, "access-vector") == 0) { out = OscalFacetCvss2NameValuesEnum::ACCESS-VECTOR; return true; }
    if (std::strcmp(str, "access-complexity") == 0) { out = OscalFacetCvss2NameValuesEnum::ACCESS-COMPLEXITY; return true; }
    if (std::strcmp(str, "authentication") == 0) { out = OscalFacetCvss2NameValuesEnum::AUTHENTICATION; return true; }
    if (std::strcmp(str, "confidentiality-impact") == 0) { out = OscalFacetCvss2NameValuesEnum::CONFIDENTIALITY-IMPACT; return true; }
    if (std::strcmp(str, "integrity-impact") == 0) { out = OscalFacetCvss2NameValuesEnum::INTEGRITY-IMPACT; return true; }
    if (std::strcmp(str, "availability-impact") == 0) { out = OscalFacetCvss2NameValuesEnum::AVAILABILITY-IMPACT; return true; }
    if (std::strcmp(str, "exploitability") == 0) { out = OscalFacetCvss2NameValuesEnum::EXPLOITABILITY; return true; }
    if (std::strcmp(str, "remediation-level") == 0) { out = OscalFacetCvss2NameValuesEnum::REMEDIATION-LEVEL; return true; }
    if (std::strcmp(str, "report-confidence") == 0) { out = OscalFacetCvss2NameValuesEnum::REPORT-CONFIDENCE; return true; }
    if (std::strcmp(str, "collateral-damage-potential") == 0) { out = OscalFacetCvss2NameValuesEnum::COLLATERAL-DAMAGE-POTENTIAL; return true; }
    if (std::strcmp(str, "target-distribution") == 0) { out = OscalFacetCvss2NameValuesEnum::TARGET-DISTRIBUTION; return true; }
    if (std::strcmp(str, "confidentiality-requirement") == 0) { out = OscalFacetCvss2NameValuesEnum::CONFIDENTIALITY-REQUIREMENT; return true; }
    if (std::strcmp(str, "integrity-requirement") == 0) { out = OscalFacetCvss2NameValuesEnum::INTEGRITY-REQUIREMENT; return true; }
    if (std::strcmp(str, "availability-requirement") == 0) { out = OscalFacetCvss2NameValuesEnum::AVAILABILITY-REQUIREMENT; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-access-vector-values.
enum class OscalFacetCvss2AccessVectorValuesEnum {
    LOCAL,
    ADJACENT-NETWORK,
    NETWORK
};

/// Convert OscalFacetCvss2AccessVectorValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2AccessVectorValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2AccessVectorValuesEnum::LOCAL: return "local";
        case OscalFacetCvss2AccessVectorValuesEnum::ADJACENT-NETWORK: return "adjacent-network";
        case OscalFacetCvss2AccessVectorValuesEnum::NETWORK: return "network";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2AccessVectorValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2AccessVectorValuesEnum& out) {
    if (std::strcmp(str, "local") == 0) { out = OscalFacetCvss2AccessVectorValuesEnum::LOCAL; return true; }
    if (std::strcmp(str, "adjacent-network") == 0) { out = OscalFacetCvss2AccessVectorValuesEnum::ADJACENT-NETWORK; return true; }
    if (std::strcmp(str, "network") == 0) { out = OscalFacetCvss2AccessVectorValuesEnum::NETWORK; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-access-complexity-values.
enum class OscalFacetCvss2AccessComplexityValuesEnum {
    HIGH,
    MEDIUM,
    LOW
};

/// Convert OscalFacetCvss2AccessComplexityValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2AccessComplexityValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2AccessComplexityValuesEnum::HIGH: return "high";
        case OscalFacetCvss2AccessComplexityValuesEnum::MEDIUM: return "medium";
        case OscalFacetCvss2AccessComplexityValuesEnum::LOW: return "low";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2AccessComplexityValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2AccessComplexityValuesEnum& out) {
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss2AccessComplexityValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "medium") == 0) { out = OscalFacetCvss2AccessComplexityValuesEnum::MEDIUM; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss2AccessComplexityValuesEnum::LOW; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-authentication-values.
enum class OscalFacetCvss2AuthenticationValuesEnum {
    MULTIPLE,
    SINGLE,
    NONE
};

/// Convert OscalFacetCvss2AuthenticationValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2AuthenticationValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2AuthenticationValuesEnum::MULTIPLE: return "multiple";
        case OscalFacetCvss2AuthenticationValuesEnum::SINGLE: return "single";
        case OscalFacetCvss2AuthenticationValuesEnum::NONE: return "none";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2AuthenticationValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2AuthenticationValuesEnum& out) {
    if (std::strcmp(str, "multiple") == 0) { out = OscalFacetCvss2AuthenticationValuesEnum::MULTIPLE; return true; }
    if (std::strcmp(str, "single") == 0) { out = OscalFacetCvss2AuthenticationValuesEnum::SINGLE; return true; }
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss2AuthenticationValuesEnum::NONE; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-confidentiality-impact-values.
enum class OscalFacetCvss2ConfidentialityImpactValuesEnum {
    NONE,
    PARTIAL,
    COMPLETE
};

/// Convert OscalFacetCvss2ConfidentialityImpactValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2ConfidentialityImpactValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2ConfidentialityImpactValuesEnum::NONE: return "none";
        case OscalFacetCvss2ConfidentialityImpactValuesEnum::PARTIAL: return "partial";
        case OscalFacetCvss2ConfidentialityImpactValuesEnum::COMPLETE: return "complete";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2ConfidentialityImpactValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2ConfidentialityImpactValuesEnum& out) {
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss2ConfidentialityImpactValuesEnum::NONE; return true; }
    if (std::strcmp(str, "partial") == 0) { out = OscalFacetCvss2ConfidentialityImpactValuesEnum::PARTIAL; return true; }
    if (std::strcmp(str, "complete") == 0) { out = OscalFacetCvss2ConfidentialityImpactValuesEnum::COMPLETE; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-exploitability-values.
enum class OscalFacetCvss2ExploitabilityValuesEnum {
    UNPROVEN,
    PROOF-OF-CONCEPT,
    FUNCTIONAL,
    HIGH,
    NOT-DEFINED
};

/// Convert OscalFacetCvss2ExploitabilityValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2ExploitabilityValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2ExploitabilityValuesEnum::UNPROVEN: return "unproven";
        case OscalFacetCvss2ExploitabilityValuesEnum::PROOF-OF-CONCEPT: return "proof-of-concept";
        case OscalFacetCvss2ExploitabilityValuesEnum::FUNCTIONAL: return "functional";
        case OscalFacetCvss2ExploitabilityValuesEnum::HIGH: return "high";
        case OscalFacetCvss2ExploitabilityValuesEnum::NOT-DEFINED: return "not-defined";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2ExploitabilityValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2ExploitabilityValuesEnum& out) {
    if (std::strcmp(str, "unproven") == 0) { out = OscalFacetCvss2ExploitabilityValuesEnum::UNPROVEN; return true; }
    if (std::strcmp(str, "proof-of-concept") == 0) { out = OscalFacetCvss2ExploitabilityValuesEnum::PROOF-OF-CONCEPT; return true; }
    if (std::strcmp(str, "functional") == 0) { out = OscalFacetCvss2ExploitabilityValuesEnum::FUNCTIONAL; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss2ExploitabilityValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss2ExploitabilityValuesEnum::NOT-DEFINED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-remediation-level-values.
enum class OscalFacetCvss2RemediationLevelValuesEnum {
    OFFICIAL-FIX,
    TEMPORARY-FIX,
    WORKAROUND,
    UNAVAILABLE,
    NOT-DEFINED
};

/// Convert OscalFacetCvss2RemediationLevelValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2RemediationLevelValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2RemediationLevelValuesEnum::OFFICIAL-FIX: return "official-fix";
        case OscalFacetCvss2RemediationLevelValuesEnum::TEMPORARY-FIX: return "temporary-fix";
        case OscalFacetCvss2RemediationLevelValuesEnum::WORKAROUND: return "workaround";
        case OscalFacetCvss2RemediationLevelValuesEnum::UNAVAILABLE: return "unavailable";
        case OscalFacetCvss2RemediationLevelValuesEnum::NOT-DEFINED: return "not-defined";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2RemediationLevelValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2RemediationLevelValuesEnum& out) {
    if (std::strcmp(str, "official-fix") == 0) { out = OscalFacetCvss2RemediationLevelValuesEnum::OFFICIAL-FIX; return true; }
    if (std::strcmp(str, "temporary-fix") == 0) { out = OscalFacetCvss2RemediationLevelValuesEnum::TEMPORARY-FIX; return true; }
    if (std::strcmp(str, "workaround") == 0) { out = OscalFacetCvss2RemediationLevelValuesEnum::WORKAROUND; return true; }
    if (std::strcmp(str, "unavailable") == 0) { out = OscalFacetCvss2RemediationLevelValuesEnum::UNAVAILABLE; return true; }
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss2RemediationLevelValuesEnum::NOT-DEFINED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-report-confidence-values.
enum class OscalFacetCvss2ReportConfidenceValuesEnum {
    UNCONFIRMED,
    UNCORROBORATED,
    CONFIRMED,
    NOT-DEFINED
};

/// Convert OscalFacetCvss2ReportConfidenceValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2ReportConfidenceValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2ReportConfidenceValuesEnum::UNCONFIRMED: return "unconfirmed";
        case OscalFacetCvss2ReportConfidenceValuesEnum::UNCORROBORATED: return "uncorroborated";
        case OscalFacetCvss2ReportConfidenceValuesEnum::CONFIRMED: return "confirmed";
        case OscalFacetCvss2ReportConfidenceValuesEnum::NOT-DEFINED: return "not-defined";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2ReportConfidenceValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2ReportConfidenceValuesEnum& out) {
    if (std::strcmp(str, "unconfirmed") == 0) { out = OscalFacetCvss2ReportConfidenceValuesEnum::UNCONFIRMED; return true; }
    if (std::strcmp(str, "uncorroborated") == 0) { out = OscalFacetCvss2ReportConfidenceValuesEnum::UNCORROBORATED; return true; }
    if (std::strcmp(str, "confirmed") == 0) { out = OscalFacetCvss2ReportConfidenceValuesEnum::CONFIRMED; return true; }
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss2ReportConfidenceValuesEnum::NOT-DEFINED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-collateral-damage-potential-values.
enum class OscalFacetCvss2CollateralDamagePotentialValuesEnum {
    NONE,
    LOW,
    LOW-MEDIUM,
    MEDIUM-HIGH,
    HIGH,
    NOT-DEFINED
};

/// Convert OscalFacetCvss2CollateralDamagePotentialValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2CollateralDamagePotentialValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::NONE: return "none";
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::LOW: return "low";
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::LOW-MEDIUM: return "low-medium";
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::MEDIUM-HIGH: return "medium-high";
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::HIGH: return "high";
        case OscalFacetCvss2CollateralDamagePotentialValuesEnum::NOT-DEFINED: return "not-defined";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2CollateralDamagePotentialValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2CollateralDamagePotentialValuesEnum& out) {
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::NONE; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::LOW; return true; }
    if (std::strcmp(str, "low-medium") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::LOW-MEDIUM; return true; }
    if (std::strcmp(str, "medium-high") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::MEDIUM-HIGH; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss2CollateralDamagePotentialValuesEnum::NOT-DEFINED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss2-cia-requirement-values.
enum class OscalFacetCvss2CiaRequirementValuesEnum {
    NONE,
    LOW,
    MEDIUM,
    HIGH,
    NOT-DEFINED
};

/// Convert OscalFacetCvss2CiaRequirementValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss2CiaRequirementValuesEnum value) {
    switch (value) {
        case OscalFacetCvss2CiaRequirementValuesEnum::NONE: return "none";
        case OscalFacetCvss2CiaRequirementValuesEnum::LOW: return "low";
        case OscalFacetCvss2CiaRequirementValuesEnum::MEDIUM: return "medium";
        case OscalFacetCvss2CiaRequirementValuesEnum::HIGH: return "high";
        case OscalFacetCvss2CiaRequirementValuesEnum::NOT-DEFINED: return "not-defined";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss2CiaRequirementValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss2CiaRequirementValuesEnum& out) {
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss2CiaRequirementValuesEnum::NONE; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss2CiaRequirementValuesEnum::LOW; return true; }
    if (std::strcmp(str, "medium") == 0) { out = OscalFacetCvss2CiaRequirementValuesEnum::MEDIUM; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss2CiaRequirementValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss2CiaRequirementValuesEnum::NOT-DEFINED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-name-values.
enum class OscalFacetCvss3NameValuesEnum {
    ATTACK-VECTOR,
    ACCESS-COMPLEXITY,
    PRIVILEGES-REQUIRED,
    USER-INTERACTION,
    SCOPE,
    CONFIDENTIALITY-IMPACT,
    INTEGRITY-IMPACT,
    AVAILABILITY-IMPACT,
    EXPLOIT-CODE-MATURITY,
    REMEDIATION-LEVEL,
    REPORT-CONFIDENCE,
    MODIFIED-ATTACK-VECTOR,
    MODIFIED-ATTACK-COMPLEXITY,
    MODIFIED-PRIVILEGES-REQUIRED,
    MODIFIED-USER-INTERACTION,
    MODIFIED-SCOPE,
    MODIFIED-CONFIDENTIALITY,
    MODIFIED-INTEGRITY,
    MODIFIED-AVAILABILITY,
    CONFIDENTIALITY-REQUIREMENT,
    INTEGRITY-REQUIREMENT,
    AVAILABILITY-REQUIREMENT
};

/// Convert OscalFacetCvss3NameValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3NameValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3NameValuesEnum::ATTACK-VECTOR: return "attack-vector";
        case OscalFacetCvss3NameValuesEnum::ACCESS-COMPLEXITY: return "access-complexity";
        case OscalFacetCvss3NameValuesEnum::PRIVILEGES-REQUIRED: return "privileges-required";
        case OscalFacetCvss3NameValuesEnum::USER-INTERACTION: return "user-interaction";
        case OscalFacetCvss3NameValuesEnum::SCOPE: return "scope";
        case OscalFacetCvss3NameValuesEnum::CONFIDENTIALITY-IMPACT: return "confidentiality-impact";
        case OscalFacetCvss3NameValuesEnum::INTEGRITY-IMPACT: return "integrity-impact";
        case OscalFacetCvss3NameValuesEnum::AVAILABILITY-IMPACT: return "availability-impact";
        case OscalFacetCvss3NameValuesEnum::EXPLOIT-CODE-MATURITY: return "exploit-code-maturity";
        case OscalFacetCvss3NameValuesEnum::REMEDIATION-LEVEL: return "remediation-level";
        case OscalFacetCvss3NameValuesEnum::REPORT-CONFIDENCE: return "report-confidence";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-ATTACK-VECTOR: return "modified-attack-vector";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-ATTACK-COMPLEXITY: return "modified-attack-complexity";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-PRIVILEGES-REQUIRED: return "modified-privileges-required";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-USER-INTERACTION: return "modified-user-interaction";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-SCOPE: return "modified-scope";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-CONFIDENTIALITY: return "modified-confidentiality";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-INTEGRITY: return "modified-integrity";
        case OscalFacetCvss3NameValuesEnum::MODIFIED-AVAILABILITY: return "modified-availability";
        case OscalFacetCvss3NameValuesEnum::CONFIDENTIALITY-REQUIREMENT: return "confidentiality-requirement";
        case OscalFacetCvss3NameValuesEnum::INTEGRITY-REQUIREMENT: return "integrity-requirement";
        case OscalFacetCvss3NameValuesEnum::AVAILABILITY-REQUIREMENT: return "availability-requirement";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3NameValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3NameValuesEnum& out) {
    if (std::strcmp(str, "attack-vector") == 0) { out = OscalFacetCvss3NameValuesEnum::ATTACK-VECTOR; return true; }
    if (std::strcmp(str, "access-complexity") == 0) { out = OscalFacetCvss3NameValuesEnum::ACCESS-COMPLEXITY; return true; }
    if (std::strcmp(str, "privileges-required") == 0) { out = OscalFacetCvss3NameValuesEnum::PRIVILEGES-REQUIRED; return true; }
    if (std::strcmp(str, "user-interaction") == 0) { out = OscalFacetCvss3NameValuesEnum::USER-INTERACTION; return true; }
    if (std::strcmp(str, "scope") == 0) { out = OscalFacetCvss3NameValuesEnum::SCOPE; return true; }
    if (std::strcmp(str, "confidentiality-impact") == 0) { out = OscalFacetCvss3NameValuesEnum::CONFIDENTIALITY-IMPACT; return true; }
    if (std::strcmp(str, "integrity-impact") == 0) { out = OscalFacetCvss3NameValuesEnum::INTEGRITY-IMPACT; return true; }
    if (std::strcmp(str, "availability-impact") == 0) { out = OscalFacetCvss3NameValuesEnum::AVAILABILITY-IMPACT; return true; }
    if (std::strcmp(str, "exploit-code-maturity") == 0) { out = OscalFacetCvss3NameValuesEnum::EXPLOIT-CODE-MATURITY; return true; }
    if (std::strcmp(str, "remediation-level") == 0) { out = OscalFacetCvss3NameValuesEnum::REMEDIATION-LEVEL; return true; }
    if (std::strcmp(str, "report-confidence") == 0) { out = OscalFacetCvss3NameValuesEnum::REPORT-CONFIDENCE; return true; }
    if (std::strcmp(str, "modified-attack-vector") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-ATTACK-VECTOR; return true; }
    if (std::strcmp(str, "modified-attack-complexity") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-ATTACK-COMPLEXITY; return true; }
    if (std::strcmp(str, "modified-privileges-required") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-PRIVILEGES-REQUIRED; return true; }
    if (std::strcmp(str, "modified-user-interaction") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-USER-INTERACTION; return true; }
    if (std::strcmp(str, "modified-scope") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-SCOPE; return true; }
    if (std::strcmp(str, "modified-confidentiality") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-CONFIDENTIALITY; return true; }
    if (std::strcmp(str, "modified-integrity") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-INTEGRITY; return true; }
    if (std::strcmp(str, "modified-availability") == 0) { out = OscalFacetCvss3NameValuesEnum::MODIFIED-AVAILABILITY; return true; }
    if (std::strcmp(str, "confidentiality-requirement") == 0) { out = OscalFacetCvss3NameValuesEnum::CONFIDENTIALITY-REQUIREMENT; return true; }
    if (std::strcmp(str, "integrity-requirement") == 0) { out = OscalFacetCvss3NameValuesEnum::INTEGRITY-REQUIREMENT; return true; }
    if (std::strcmp(str, "availability-requirement") == 0) { out = OscalFacetCvss3NameValuesEnum::AVAILABILITY-REQUIREMENT; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-access-vector-values.
enum class OscalFacetCvss3AccessVectorValuesEnum {
    NETWORK,
    ADJACENT,
    LOCAL,
    PHYSICAL
};

/// Convert OscalFacetCvss3AccessVectorValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3AccessVectorValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3AccessVectorValuesEnum::NETWORK: return "network";
        case OscalFacetCvss3AccessVectorValuesEnum::ADJACENT: return "adjacent";
        case OscalFacetCvss3AccessVectorValuesEnum::LOCAL: return "local";
        case OscalFacetCvss3AccessVectorValuesEnum::PHYSICAL: return "physical";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3AccessVectorValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3AccessVectorValuesEnum& out) {
    if (std::strcmp(str, "network") == 0) { out = OscalFacetCvss3AccessVectorValuesEnum::NETWORK; return true; }
    if (std::strcmp(str, "adjacent") == 0) { out = OscalFacetCvss3AccessVectorValuesEnum::ADJACENT; return true; }
    if (std::strcmp(str, "local") == 0) { out = OscalFacetCvss3AccessVectorValuesEnum::LOCAL; return true; }
    if (std::strcmp(str, "physical") == 0) { out = OscalFacetCvss3AccessVectorValuesEnum::PHYSICAL; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-access-complexity-values.
enum class OscalFacetCvss3AccessComplexityValuesEnum {
    HIGH,
    LOW
};

/// Convert OscalFacetCvss3AccessComplexityValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3AccessComplexityValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3AccessComplexityValuesEnum::HIGH: return "high";
        case OscalFacetCvss3AccessComplexityValuesEnum::LOW: return "low";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3AccessComplexityValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3AccessComplexityValuesEnum& out) {
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3AccessComplexityValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss3AccessComplexityValuesEnum::LOW; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-cia-impact-values.
enum class OscalFacetCvss3CiaImpactValuesEnum {
    NONE,
    LOW,
    HIGH
};

/// Convert OscalFacetCvss3CiaImpactValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3CiaImpactValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3CiaImpactValuesEnum::NONE: return "none";
        case OscalFacetCvss3CiaImpactValuesEnum::LOW: return "low";
        case OscalFacetCvss3CiaImpactValuesEnum::HIGH: return "high";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3CiaImpactValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3CiaImpactValuesEnum& out) {
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss3CiaImpactValuesEnum::NONE; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss3CiaImpactValuesEnum::LOW; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3CiaImpactValuesEnum::HIGH; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-user-interaction.
enum class OscalFacetCvss3UserInteractionEnum {
    NONE,
    REQUIRED
};

/// Convert OscalFacetCvss3UserInteractionEnum to its string representation.
inline const char* to_string(OscalFacetCvss3UserInteractionEnum value) {
    switch (value) {
        case OscalFacetCvss3UserInteractionEnum::NONE: return "none";
        case OscalFacetCvss3UserInteractionEnum::REQUIRED: return "required";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3UserInteractionEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3UserInteractionEnum& out) {
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss3UserInteractionEnum::NONE; return true; }
    if (std::strcmp(str, "required") == 0) { out = OscalFacetCvss3UserInteractionEnum::REQUIRED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-scope.
enum class OscalFacetCvss3ScopeEnum {
    UNCHANGED,
    CHANGED
};

/// Convert OscalFacetCvss3ScopeEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ScopeEnum value) {
    switch (value) {
        case OscalFacetCvss3ScopeEnum::UNCHANGED: return "unchanged";
        case OscalFacetCvss3ScopeEnum::CHANGED: return "changed";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ScopeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ScopeEnum& out) {
    if (std::strcmp(str, "unchanged") == 0) { out = OscalFacetCvss3ScopeEnum::UNCHANGED; return true; }
    if (std::strcmp(str, "changed") == 0) { out = OscalFacetCvss3ScopeEnum::CHANGED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-exploit-code-maturity-values.
enum class OscalFacetCvss3ExploitCodeMaturityValuesEnum {
    NOT-DEFINED,
    UNPROVEN,
    PROOF-OF-CONCEPT,
    FUNCTIONAL,
    HIGH
};

/// Convert OscalFacetCvss3ExploitCodeMaturityValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ExploitCodeMaturityValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ExploitCodeMaturityValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ExploitCodeMaturityValuesEnum::UNPROVEN: return "unproven";
        case OscalFacetCvss3ExploitCodeMaturityValuesEnum::PROOF-OF-CONCEPT: return "proof-of-concept";
        case OscalFacetCvss3ExploitCodeMaturityValuesEnum::FUNCTIONAL: return "functional";
        case OscalFacetCvss3ExploitCodeMaturityValuesEnum::HIGH: return "high";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ExploitCodeMaturityValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ExploitCodeMaturityValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ExploitCodeMaturityValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "unproven") == 0) { out = OscalFacetCvss3ExploitCodeMaturityValuesEnum::UNPROVEN; return true; }
    if (std::strcmp(str, "proof-of-concept") == 0) { out = OscalFacetCvss3ExploitCodeMaturityValuesEnum::PROOF-OF-CONCEPT; return true; }
    if (std::strcmp(str, "functional") == 0) { out = OscalFacetCvss3ExploitCodeMaturityValuesEnum::FUNCTIONAL; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3ExploitCodeMaturityValuesEnum::HIGH; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-remediation-level.
enum class OscalFacetCvss3RemediationLevelEnum {
    NOT-DEFINED,
    OFFICIAL-FIX,
    TEMPORARY-FIX,
    WORKAROUND,
    UNAVAILABLE
};

/// Convert OscalFacetCvss3RemediationLevelEnum to its string representation.
inline const char* to_string(OscalFacetCvss3RemediationLevelEnum value) {
    switch (value) {
        case OscalFacetCvss3RemediationLevelEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3RemediationLevelEnum::OFFICIAL-FIX: return "official-fix";
        case OscalFacetCvss3RemediationLevelEnum::TEMPORARY-FIX: return "temporary-fix";
        case OscalFacetCvss3RemediationLevelEnum::WORKAROUND: return "workaround";
        case OscalFacetCvss3RemediationLevelEnum::UNAVAILABLE: return "unavailable";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3RemediationLevelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3RemediationLevelEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3RemediationLevelEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "official-fix") == 0) { out = OscalFacetCvss3RemediationLevelEnum::OFFICIAL-FIX; return true; }
    if (std::strcmp(str, "temporary-fix") == 0) { out = OscalFacetCvss3RemediationLevelEnum::TEMPORARY-FIX; return true; }
    if (std::strcmp(str, "workaround") == 0) { out = OscalFacetCvss3RemediationLevelEnum::WORKAROUND; return true; }
    if (std::strcmp(str, "unavailable") == 0) { out = OscalFacetCvss3RemediationLevelEnum::UNAVAILABLE; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-report-confidence-values.
enum class OscalFacetCvss3ReportConfidenceValuesEnum {
    NOT-DEFINED,
    UNKNOWN,
    REASONABLE,
    CONFIRMED
};

/// Convert OscalFacetCvss3ReportConfidenceValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ReportConfidenceValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ReportConfidenceValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ReportConfidenceValuesEnum::UNKNOWN: return "unknown";
        case OscalFacetCvss3ReportConfidenceValuesEnum::REASONABLE: return "reasonable";
        case OscalFacetCvss3ReportConfidenceValuesEnum::CONFIRMED: return "confirmed";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ReportConfidenceValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ReportConfidenceValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ReportConfidenceValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "unknown") == 0) { out = OscalFacetCvss3ReportConfidenceValuesEnum::UNKNOWN; return true; }
    if (std::strcmp(str, "reasonable") == 0) { out = OscalFacetCvss3ReportConfidenceValuesEnum::REASONABLE; return true; }
    if (std::strcmp(str, "confirmed") == 0) { out = OscalFacetCvss3ReportConfidenceValuesEnum::CONFIRMED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-cia-requirement-values.
enum class OscalFacetCvss3CiaRequirementValuesEnum {
    NOT-DEFINED,
    LOW,
    MEDIUM,
    HIGH
};

/// Convert OscalFacetCvss3CiaRequirementValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3CiaRequirementValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3CiaRequirementValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3CiaRequirementValuesEnum::LOW: return "low";
        case OscalFacetCvss3CiaRequirementValuesEnum::MEDIUM: return "medium";
        case OscalFacetCvss3CiaRequirementValuesEnum::HIGH: return "high";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3CiaRequirementValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3CiaRequirementValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3CiaRequirementValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss3CiaRequirementValuesEnum::LOW; return true; }
    if (std::strcmp(str, "medium") == 0) { out = OscalFacetCvss3CiaRequirementValuesEnum::MEDIUM; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3CiaRequirementValuesEnum::HIGH; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-modified-attack-vector-values.
enum class OscalFacetCvss3ModifiedAttackVectorValuesEnum {
    NOT-DEFINED,
    NETWORK,
    ADJACENT,
    LOCAL,
    PHYSICAL
};

/// Convert OscalFacetCvss3ModifiedAttackVectorValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ModifiedAttackVectorValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ModifiedAttackVectorValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ModifiedAttackVectorValuesEnum::NETWORK: return "network";
        case OscalFacetCvss3ModifiedAttackVectorValuesEnum::ADJACENT: return "adjacent";
        case OscalFacetCvss3ModifiedAttackVectorValuesEnum::LOCAL: return "local";
        case OscalFacetCvss3ModifiedAttackVectorValuesEnum::PHYSICAL: return "physical";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ModifiedAttackVectorValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ModifiedAttackVectorValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ModifiedAttackVectorValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "network") == 0) { out = OscalFacetCvss3ModifiedAttackVectorValuesEnum::NETWORK; return true; }
    if (std::strcmp(str, "adjacent") == 0) { out = OscalFacetCvss3ModifiedAttackVectorValuesEnum::ADJACENT; return true; }
    if (std::strcmp(str, "local") == 0) { out = OscalFacetCvss3ModifiedAttackVectorValuesEnum::LOCAL; return true; }
    if (std::strcmp(str, "physical") == 0) { out = OscalFacetCvss3ModifiedAttackVectorValuesEnum::PHYSICAL; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-modified-attack-complexity-values.
enum class OscalFacetCvss3ModifiedAttackComplexityValuesEnum {
    NOT-DEFINED,
    HIGH,
    LOW
};

/// Convert OscalFacetCvss3ModifiedAttackComplexityValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ModifiedAttackComplexityValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ModifiedAttackComplexityValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ModifiedAttackComplexityValuesEnum::HIGH: return "high";
        case OscalFacetCvss3ModifiedAttackComplexityValuesEnum::LOW: return "low";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ModifiedAttackComplexityValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ModifiedAttackComplexityValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ModifiedAttackComplexityValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3ModifiedAttackComplexityValuesEnum::HIGH; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss3ModifiedAttackComplexityValuesEnum::LOW; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-modified-cia-values.
enum class OscalFacetCvss3ModifiedCiaValuesEnum {
    NOT-DEFINED,
    NONE,
    LOW,
    HIGH
};

/// Convert OscalFacetCvss3ModifiedCiaValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ModifiedCiaValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ModifiedCiaValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ModifiedCiaValuesEnum::NONE: return "none";
        case OscalFacetCvss3ModifiedCiaValuesEnum::LOW: return "low";
        case OscalFacetCvss3ModifiedCiaValuesEnum::HIGH: return "high";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ModifiedCiaValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ModifiedCiaValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ModifiedCiaValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss3ModifiedCiaValuesEnum::NONE; return true; }
    if (std::strcmp(str, "low") == 0) { out = OscalFacetCvss3ModifiedCiaValuesEnum::LOW; return true; }
    if (std::strcmp(str, "high") == 0) { out = OscalFacetCvss3ModifiedCiaValuesEnum::HIGH; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-modified-user-interaction-values.
enum class OscalFacetCvss3ModifiedUserInteractionValuesEnum {
    NOT-DEFINED,
    NONE,
    REQUIRED
};

/// Convert OscalFacetCvss3ModifiedUserInteractionValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ModifiedUserInteractionValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ModifiedUserInteractionValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ModifiedUserInteractionValuesEnum::NONE: return "none";
        case OscalFacetCvss3ModifiedUserInteractionValuesEnum::REQUIRED: return "required";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ModifiedUserInteractionValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ModifiedUserInteractionValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ModifiedUserInteractionValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "none") == 0) { out = OscalFacetCvss3ModifiedUserInteractionValuesEnum::NONE; return true; }
    if (std::strcmp(str, "required") == 0) { out = OscalFacetCvss3ModifiedUserInteractionValuesEnum::REQUIRED; return true; }
    return false;
}

/// Values from assessment-common set oscal-facet-cvss3-modified-scope-values.
enum class OscalFacetCvss3ModifiedScopeValuesEnum {
    NOT-DEFINED,
    UNCHANGED,
    CHANGED
};

/// Convert OscalFacetCvss3ModifiedScopeValuesEnum to its string representation.
inline const char* to_string(OscalFacetCvss3ModifiedScopeValuesEnum value) {
    switch (value) {
        case OscalFacetCvss3ModifiedScopeValuesEnum::NOT-DEFINED: return "not-defined";
        case OscalFacetCvss3ModifiedScopeValuesEnum::UNCHANGED: return "unchanged";
        case OscalFacetCvss3ModifiedScopeValuesEnum::CHANGED: return "changed";
    }
    return "unknown";
}

/// Parse a string into OscalFacetCvss3ModifiedScopeValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalFacetCvss3ModifiedScopeValuesEnum& out) {
    if (std::strcmp(str, "not-defined") == 0) { out = OscalFacetCvss3ModifiedScopeValuesEnum::NOT-DEFINED; return true; }
    if (std::strcmp(str, "unchanged") == 0) { out = OscalFacetCvss3ModifiedScopeValuesEnum::UNCHANGED; return true; }
    if (std::strcmp(str, "changed") == 0) { out = OscalFacetCvss3ModifiedScopeValuesEnum::CHANGED; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-vectors.
enum class OscalCvssV40VectorsEnum {
    AV,
    AC,
    AT,
    PR,
    UI,
    VC,
    VI,
    VA,
    SC,
    SI,
    SA,
    S,
    AU,
    R,
    V,
    RE,
    U,
    MAV,
    MAC,
    MAT,
    MPR,
    MUI,
    MVC,
    MVI,
    MVA,
    MSC,
    MSI,
    MSA,
    CR,
    IR,
    AR,
    E
};

/// Convert OscalCvssV40VectorsEnum to its string representation.
inline const char* to_string(OscalCvssV40VectorsEnum value) {
    switch (value) {
        case OscalCvssV40VectorsEnum::AV: return "av";
        case OscalCvssV40VectorsEnum::AC: return "ac";
        case OscalCvssV40VectorsEnum::AT: return "at";
        case OscalCvssV40VectorsEnum::PR: return "pr";
        case OscalCvssV40VectorsEnum::UI: return "ui";
        case OscalCvssV40VectorsEnum::VC: return "vc";
        case OscalCvssV40VectorsEnum::VI: return "vi";
        case OscalCvssV40VectorsEnum::VA: return "va";
        case OscalCvssV40VectorsEnum::SC: return "sc";
        case OscalCvssV40VectorsEnum::SI: return "si";
        case OscalCvssV40VectorsEnum::SA: return "sa";
        case OscalCvssV40VectorsEnum::S: return "s";
        case OscalCvssV40VectorsEnum::AU: return "au";
        case OscalCvssV40VectorsEnum::R: return "r";
        case OscalCvssV40VectorsEnum::V: return "v";
        case OscalCvssV40VectorsEnum::RE: return "re";
        case OscalCvssV40VectorsEnum::U: return "u";
        case OscalCvssV40VectorsEnum::MAV: return "mav";
        case OscalCvssV40VectorsEnum::MAC: return "mac";
        case OscalCvssV40VectorsEnum::MAT: return "mat";
        case OscalCvssV40VectorsEnum::MPR: return "mpr";
        case OscalCvssV40VectorsEnum::MUI: return "mui";
        case OscalCvssV40VectorsEnum::MVC: return "mvc";
        case OscalCvssV40VectorsEnum::MVI: return "mvi";
        case OscalCvssV40VectorsEnum::MVA: return "mva";
        case OscalCvssV40VectorsEnum::MSC: return "msc";
        case OscalCvssV40VectorsEnum::MSI: return "msi";
        case OscalCvssV40VectorsEnum::MSA: return "msa";
        case OscalCvssV40VectorsEnum::CR: return "cr";
        case OscalCvssV40VectorsEnum::IR: return "ir";
        case OscalCvssV40VectorsEnum::AR: return "ar";
        case OscalCvssV40VectorsEnum::E: return "e";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40VectorsEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40VectorsEnum& out) {
    if (std::strcmp(str, "av") == 0) { out = OscalCvssV40VectorsEnum::AV; return true; }
    if (std::strcmp(str, "ac") == 0) { out = OscalCvssV40VectorsEnum::AC; return true; }
    if (std::strcmp(str, "at") == 0) { out = OscalCvssV40VectorsEnum::AT; return true; }
    if (std::strcmp(str, "pr") == 0) { out = OscalCvssV40VectorsEnum::PR; return true; }
    if (std::strcmp(str, "ui") == 0) { out = OscalCvssV40VectorsEnum::UI; return true; }
    if (std::strcmp(str, "vc") == 0) { out = OscalCvssV40VectorsEnum::VC; return true; }
    if (std::strcmp(str, "vi") == 0) { out = OscalCvssV40VectorsEnum::VI; return true; }
    if (std::strcmp(str, "va") == 0) { out = OscalCvssV40VectorsEnum::VA; return true; }
    if (std::strcmp(str, "sc") == 0) { out = OscalCvssV40VectorsEnum::SC; return true; }
    if (std::strcmp(str, "si") == 0) { out = OscalCvssV40VectorsEnum::SI; return true; }
    if (std::strcmp(str, "sa") == 0) { out = OscalCvssV40VectorsEnum::SA; return true; }
    if (std::strcmp(str, "s") == 0) { out = OscalCvssV40VectorsEnum::S; return true; }
    if (std::strcmp(str, "au") == 0) { out = OscalCvssV40VectorsEnum::AU; return true; }
    if (std::strcmp(str, "r") == 0) { out = OscalCvssV40VectorsEnum::R; return true; }
    if (std::strcmp(str, "v") == 0) { out = OscalCvssV40VectorsEnum::V; return true; }
    if (std::strcmp(str, "re") == 0) { out = OscalCvssV40VectorsEnum::RE; return true; }
    if (std::strcmp(str, "u") == 0) { out = OscalCvssV40VectorsEnum::U; return true; }
    if (std::strcmp(str, "mav") == 0) { out = OscalCvssV40VectorsEnum::MAV; return true; }
    if (std::strcmp(str, "mac") == 0) { out = OscalCvssV40VectorsEnum::MAC; return true; }
    if (std::strcmp(str, "mat") == 0) { out = OscalCvssV40VectorsEnum::MAT; return true; }
    if (std::strcmp(str, "mpr") == 0) { out = OscalCvssV40VectorsEnum::MPR; return true; }
    if (std::strcmp(str, "mui") == 0) { out = OscalCvssV40VectorsEnum::MUI; return true; }
    if (std::strcmp(str, "mvc") == 0) { out = OscalCvssV40VectorsEnum::MVC; return true; }
    if (std::strcmp(str, "mvi") == 0) { out = OscalCvssV40VectorsEnum::MVI; return true; }
    if (std::strcmp(str, "mva") == 0) { out = OscalCvssV40VectorsEnum::MVA; return true; }
    if (std::strcmp(str, "msc") == 0) { out = OscalCvssV40VectorsEnum::MSC; return true; }
    if (std::strcmp(str, "msi") == 0) { out = OscalCvssV40VectorsEnum::MSI; return true; }
    if (std::strcmp(str, "msa") == 0) { out = OscalCvssV40VectorsEnum::MSA; return true; }
    if (std::strcmp(str, "cr") == 0) { out = OscalCvssV40VectorsEnum::CR; return true; }
    if (std::strcmp(str, "ir") == 0) { out = OscalCvssV40VectorsEnum::IR; return true; }
    if (std::strcmp(str, "ar") == 0) { out = OscalCvssV40VectorsEnum::AR; return true; }
    if (std::strcmp(str, "e") == 0) { out = OscalCvssV40VectorsEnum::E; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-av-values.
enum class OscalCvssV40AvValuesEnum {
    N,
    A,
    L,
    P
};

/// Convert OscalCvssV40AvValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40AvValuesEnum value) {
    switch (value) {
        case OscalCvssV40AvValuesEnum::N: return "n";
        case OscalCvssV40AvValuesEnum::A: return "a";
        case OscalCvssV40AvValuesEnum::L: return "l";
        case OscalCvssV40AvValuesEnum::P: return "p";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40AvValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40AvValuesEnum& out) {
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40AvValuesEnum::N; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40AvValuesEnum::A; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40AvValuesEnum::L; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40AvValuesEnum::P; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-ac-values.
enum class OscalCvssV40AcValuesEnum {
    H,
    L
};

/// Convert OscalCvssV40AcValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40AcValuesEnum value) {
    switch (value) {
        case OscalCvssV40AcValuesEnum::H: return "h";
        case OscalCvssV40AcValuesEnum::L: return "l";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40AcValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40AcValuesEnum& out) {
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40AcValuesEnum::H; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40AcValuesEnum::L; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-at-values.
enum class OscalCvssV40AtValuesEnum {
    N,
    P
};

/// Convert OscalCvssV40AtValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40AtValuesEnum value) {
    switch (value) {
        case OscalCvssV40AtValuesEnum::N: return "n";
        case OscalCvssV40AtValuesEnum::P: return "p";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40AtValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40AtValuesEnum& out) {
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40AtValuesEnum::N; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40AtValuesEnum::P; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-pr-cia-values.
enum class OscalCvssV40PrCiaValuesEnum {
    N,
    L,
    H
};

/// Convert OscalCvssV40PrCiaValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40PrCiaValuesEnum value) {
    switch (value) {
        case OscalCvssV40PrCiaValuesEnum::N: return "n";
        case OscalCvssV40PrCiaValuesEnum::L: return "l";
        case OscalCvssV40PrCiaValuesEnum::H: return "h";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40PrCiaValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40PrCiaValuesEnum& out) {
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40PrCiaValuesEnum::N; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40PrCiaValuesEnum::L; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40PrCiaValuesEnum::H; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-ui-values.
enum class OscalCvssV40UiValuesEnum {
    N,
    P,
    A
};

/// Convert OscalCvssV40UiValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40UiValuesEnum value) {
    switch (value) {
        case OscalCvssV40UiValuesEnum::N: return "n";
        case OscalCvssV40UiValuesEnum::P: return "p";
        case OscalCvssV40UiValuesEnum::A: return "a";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40UiValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40UiValuesEnum& out) {
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40UiValuesEnum::N; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40UiValuesEnum::P; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40UiValuesEnum::A; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-s-values.
enum class OscalCvssV40SValuesEnum {
    X,
    N,
    P
};

/// Convert OscalCvssV40SValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40SValuesEnum value) {
    switch (value) {
        case OscalCvssV40SValuesEnum::X: return "x";
        case OscalCvssV40SValuesEnum::N: return "n";
        case OscalCvssV40SValuesEnum::P: return "p";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40SValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40SValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40SValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40SValuesEnum::N; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40SValuesEnum::P; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-au-values.
enum class OscalCvssV40AuValuesEnum {
    X,
    N,
    Y
};

/// Convert OscalCvssV40AuValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40AuValuesEnum value) {
    switch (value) {
        case OscalCvssV40AuValuesEnum::X: return "x";
        case OscalCvssV40AuValuesEnum::N: return "n";
        case OscalCvssV40AuValuesEnum::Y: return "y";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40AuValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40AuValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40AuValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40AuValuesEnum::N; return true; }
    if (std::strcmp(str, "y") == 0) { out = OscalCvssV40AuValuesEnum::Y; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-r-values.
enum class OscalCvssV40RValuesEnum {
    X,
    A,
    U,
    I
};

/// Convert OscalCvssV40RValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40RValuesEnum value) {
    switch (value) {
        case OscalCvssV40RValuesEnum::X: return "x";
        case OscalCvssV40RValuesEnum::A: return "a";
        case OscalCvssV40RValuesEnum::U: return "u";
        case OscalCvssV40RValuesEnum::I: return "i";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40RValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40RValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40RValuesEnum::X; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40RValuesEnum::A; return true; }
    if (std::strcmp(str, "u") == 0) { out = OscalCvssV40RValuesEnum::U; return true; }
    if (std::strcmp(str, "i") == 0) { out = OscalCvssV40RValuesEnum::I; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-v-values.
enum class OscalCvssV40VValuesEnum {
    X,
    A,
    U,
    I
};

/// Convert OscalCvssV40VValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40VValuesEnum value) {
    switch (value) {
        case OscalCvssV40VValuesEnum::X: return "x";
        case OscalCvssV40VValuesEnum::A: return "a";
        case OscalCvssV40VValuesEnum::U: return "u";
        case OscalCvssV40VValuesEnum::I: return "i";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40VValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40VValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40VValuesEnum::X; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40VValuesEnum::A; return true; }
    if (std::strcmp(str, "u") == 0) { out = OscalCvssV40VValuesEnum::U; return true; }
    if (std::strcmp(str, "i") == 0) { out = OscalCvssV40VValuesEnum::I; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-re-values.
enum class OscalCvssV40ReValuesEnum {
    X,
    L,
    M,
    H
};

/// Convert OscalCvssV40ReValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40ReValuesEnum value) {
    switch (value) {
        case OscalCvssV40ReValuesEnum::X: return "x";
        case OscalCvssV40ReValuesEnum::L: return "l";
        case OscalCvssV40ReValuesEnum::M: return "m";
        case OscalCvssV40ReValuesEnum::H: return "h";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40ReValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40ReValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40ReValuesEnum::X; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40ReValuesEnum::L; return true; }
    if (std::strcmp(str, "m") == 0) { out = OscalCvssV40ReValuesEnum::M; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40ReValuesEnum::H; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-u-values.
enum class OscalCvssV40UValuesEnum {
    X,
    CLEAR,
    GREEN,
    AMBER,
    RED
};

/// Convert OscalCvssV40UValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40UValuesEnum value) {
    switch (value) {
        case OscalCvssV40UValuesEnum::X: return "x";
        case OscalCvssV40UValuesEnum::CLEAR: return "clear";
        case OscalCvssV40UValuesEnum::GREEN: return "green";
        case OscalCvssV40UValuesEnum::AMBER: return "amber";
        case OscalCvssV40UValuesEnum::RED: return "red";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40UValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40UValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40UValuesEnum::X; return true; }
    if (std::strcmp(str, "clear") == 0) { out = OscalCvssV40UValuesEnum::CLEAR; return true; }
    if (std::strcmp(str, "green") == 0) { out = OscalCvssV40UValuesEnum::GREEN; return true; }
    if (std::strcmp(str, "amber") == 0) { out = OscalCvssV40UValuesEnum::AMBER; return true; }
    if (std::strcmp(str, "red") == 0) { out = OscalCvssV40UValuesEnum::RED; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-mav-values.
enum class OscalCvssV40MavValuesEnum {
    X,
    N,
    A,
    L,
    P
};

/// Convert OscalCvssV40MavValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MavValuesEnum value) {
    switch (value) {
        case OscalCvssV40MavValuesEnum::X: return "x";
        case OscalCvssV40MavValuesEnum::N: return "n";
        case OscalCvssV40MavValuesEnum::A: return "a";
        case OscalCvssV40MavValuesEnum::L: return "l";
        case OscalCvssV40MavValuesEnum::P: return "p";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MavValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MavValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MavValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MavValuesEnum::N; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40MavValuesEnum::A; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40MavValuesEnum::L; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40MavValuesEnum::P; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-mac-values.
enum class OscalCvssV40MacValuesEnum {
    X,
    H,
    L
};

/// Convert OscalCvssV40MacValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MacValuesEnum value) {
    switch (value) {
        case OscalCvssV40MacValuesEnum::X: return "x";
        case OscalCvssV40MacValuesEnum::H: return "h";
        case OscalCvssV40MacValuesEnum::L: return "l";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MacValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MacValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MacValuesEnum::X; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40MacValuesEnum::H; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40MacValuesEnum::L; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-mat-values.
enum class OscalCvssV40MatValuesEnum {
    X,
    N,
    P
};

/// Convert OscalCvssV40MatValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MatValuesEnum value) {
    switch (value) {
        case OscalCvssV40MatValuesEnum::X: return "x";
        case OscalCvssV40MatValuesEnum::N: return "n";
        case OscalCvssV40MatValuesEnum::P: return "p";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MatValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MatValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MatValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MatValuesEnum::N; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40MatValuesEnum::P; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-mpr-mvs-cia-values.
enum class OscalCvssV40MprMvsCiaValuesEnum {
    X,
    N,
    L,
    H
};

/// Convert OscalCvssV40MprMvsCiaValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MprMvsCiaValuesEnum value) {
    switch (value) {
        case OscalCvssV40MprMvsCiaValuesEnum::X: return "x";
        case OscalCvssV40MprMvsCiaValuesEnum::N: return "n";
        case OscalCvssV40MprMvsCiaValuesEnum::L: return "l";
        case OscalCvssV40MprMvsCiaValuesEnum::H: return "h";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MprMvsCiaValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MprMvsCiaValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MprMvsCiaValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MprMvsCiaValuesEnum::N; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40MprMvsCiaValuesEnum::L; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40MprMvsCiaValuesEnum::H; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-mui-values.
enum class OscalCvssV40MuiValuesEnum {
    X,
    N,
    P,
    A
};

/// Convert OscalCvssV40MuiValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MuiValuesEnum value) {
    switch (value) {
        case OscalCvssV40MuiValuesEnum::X: return "x";
        case OscalCvssV40MuiValuesEnum::N: return "n";
        case OscalCvssV40MuiValuesEnum::P: return "p";
        case OscalCvssV40MuiValuesEnum::A: return "a";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MuiValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MuiValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MuiValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MuiValuesEnum::N; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40MuiValuesEnum::P; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40MuiValuesEnum::A; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-msc-values.
enum class OscalCvssV40MscValuesEnum {
    X,
    N,
    L,
    H
};

/// Convert OscalCvssV40MscValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MscValuesEnum value) {
    switch (value) {
        case OscalCvssV40MscValuesEnum::X: return "x";
        case OscalCvssV40MscValuesEnum::N: return "n";
        case OscalCvssV40MscValuesEnum::L: return "l";
        case OscalCvssV40MscValuesEnum::H: return "h";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MscValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MscValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MscValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MscValuesEnum::N; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40MscValuesEnum::L; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40MscValuesEnum::H; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-msi-msa-cia-values.
enum class OscalCvssV40MsiMsaCiaValuesEnum {
    X,
    N,
    L,
    H,
    S
};

/// Convert OscalCvssV40MsiMsaCiaValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40MsiMsaCiaValuesEnum value) {
    switch (value) {
        case OscalCvssV40MsiMsaCiaValuesEnum::X: return "x";
        case OscalCvssV40MsiMsaCiaValuesEnum::N: return "n";
        case OscalCvssV40MsiMsaCiaValuesEnum::L: return "l";
        case OscalCvssV40MsiMsaCiaValuesEnum::H: return "h";
        case OscalCvssV40MsiMsaCiaValuesEnum::S: return "s";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40MsiMsaCiaValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40MsiMsaCiaValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40MsiMsaCiaValuesEnum::X; return true; }
    if (std::strcmp(str, "n") == 0) { out = OscalCvssV40MsiMsaCiaValuesEnum::N; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40MsiMsaCiaValuesEnum::L; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40MsiMsaCiaValuesEnum::H; return true; }
    if (std::strcmp(str, "s") == 0) { out = OscalCvssV40MsiMsaCiaValuesEnum::S; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-env-cia-values.
enum class OscalCvssV40EnvCiaValuesEnum {
    X,
    L,
    M,
    H
};

/// Convert OscalCvssV40EnvCiaValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40EnvCiaValuesEnum value) {
    switch (value) {
        case OscalCvssV40EnvCiaValuesEnum::X: return "x";
        case OscalCvssV40EnvCiaValuesEnum::L: return "l";
        case OscalCvssV40EnvCiaValuesEnum::M: return "m";
        case OscalCvssV40EnvCiaValuesEnum::H: return "h";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40EnvCiaValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40EnvCiaValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40EnvCiaValuesEnum::X; return true; }
    if (std::strcmp(str, "l") == 0) { out = OscalCvssV40EnvCiaValuesEnum::L; return true; }
    if (std::strcmp(str, "m") == 0) { out = OscalCvssV40EnvCiaValuesEnum::M; return true; }
    if (std::strcmp(str, "h") == 0) { out = OscalCvssV40EnvCiaValuesEnum::H; return true; }
    return false;
}

/// Values from assessment-common set oscal-cvss-v4.0-e-values.
enum class OscalCvssV40EValuesEnum {
    X,
    A,
    P,
    U
};

/// Convert OscalCvssV40EValuesEnum to its string representation.
inline const char* to_string(OscalCvssV40EValuesEnum value) {
    switch (value) {
        case OscalCvssV40EValuesEnum::X: return "x";
        case OscalCvssV40EValuesEnum::A: return "a";
        case OscalCvssV40EValuesEnum::P: return "p";
        case OscalCvssV40EValuesEnum::U: return "u";
    }
    return "unknown";
}

/// Parse a string into OscalCvssV40EValuesEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalCvssV40EValuesEnum& out) {
    if (std::strcmp(str, "x") == 0) { out = OscalCvssV40EValuesEnum::X; return true; }
    if (std::strcmp(str, "a") == 0) { out = OscalCvssV40EValuesEnum::A; return true; }
    if (std::strcmp(str, "p") == 0) { out = OscalCvssV40EValuesEnum::P; return true; }
    if (std::strcmp(str, "u") == 0) { out = OscalCvssV40EValuesEnum::U; return true; }
    return false;
}

/// Values from assessment-common set oscal-response-prop-type-value.
enum class OscalResponsePropTypeValueEnum {
    AVOID,
    MITIGATE,
    TRANSFER,
    ACCEPT,
    SHARE,
    CONTINGENCY,
    NONE
};

/// Convert OscalResponsePropTypeValueEnum to its string representation.
inline const char* to_string(OscalResponsePropTypeValueEnum value) {
    switch (value) {
        case OscalResponsePropTypeValueEnum::AVOID: return "avoid";
        case OscalResponsePropTypeValueEnum::MITIGATE: return "mitigate";
        case OscalResponsePropTypeValueEnum::TRANSFER: return "transfer";
        case OscalResponsePropTypeValueEnum::ACCEPT: return "accept";
        case OscalResponsePropTypeValueEnum::SHARE: return "share";
        case OscalResponsePropTypeValueEnum::CONTINGENCY: return "contingency";
        case OscalResponsePropTypeValueEnum::NONE: return "none";
    }
    return "unknown";
}

/// Parse a string into OscalResponsePropTypeValueEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, OscalResponsePropTypeValueEnum& out) {
    if (std::strcmp(str, "avoid") == 0) { out = OscalResponsePropTypeValueEnum::AVOID; return true; }
    if (std::strcmp(str, "mitigate") == 0) { out = OscalResponsePropTypeValueEnum::MITIGATE; return true; }
    if (std::strcmp(str, "transfer") == 0) { out = OscalResponsePropTypeValueEnum::TRANSFER; return true; }
    if (std::strcmp(str, "accept") == 0) { out = OscalResponsePropTypeValueEnum::ACCEPT; return true; }
    if (std::strcmp(str, "share") == 0) { out = OscalResponsePropTypeValueEnum::SHARE; return true; }
    if (std::strcmp(str, "contingency") == 0) { out = OscalResponsePropTypeValueEnum::CONTINGENCY; return true; }
    if (std::strcmp(str, "none") == 0) { out = OscalResponsePropTypeValueEnum::NONE; return true; }
    return false;
}

/// Curated response lifecycle values. Other values are permitted (OSCAL allow-other="yes").
enum class ResponseLifecycleEnum {
    RECOMMENDATION,
    PLANNED,
    COMPLETED
};

/// Convert ResponseLifecycleEnum to its string representation.
inline const char* to_string(ResponseLifecycleEnum value) {
    switch (value) {
        case ResponseLifecycleEnum::RECOMMENDATION: return "recommendation";
        case ResponseLifecycleEnum::PLANNED: return "planned";
        case ResponseLifecycleEnum::COMPLETED: return "completed";
    }
    return "unknown";
}

/// Parse a string into ResponseLifecycleEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ResponseLifecycleEnum& out) {
    if (std::strcmp(str, "recommendation") == 0) { out = ResponseLifecycleEnum::RECOMMENDATION; return true; }
    if (std::strcmp(str, "planned") == 0) { out = ResponseLifecycleEnum::PLANNED; return true; }
    if (std::strcmp(str, "completed") == 0) { out = ResponseLifecycleEnum::COMPLETED; return true; }
    return false;
}

/// Allowable operational states for an OSCAL-described system.
enum class SystemOperatingStatusEnum {
    /// The system is currently operating in production.
    OPERATIONAL,
    /// The system is being designed, developed, or implemented.
    UNDER-DEVELOPMENT,
    /// The system is undergoing a major change, development, or transition.
    UNDER-MAJOR-MODIFICATION,
    /// The system is no longer operational.
    DISPOSITION,
    /// Some other state.
    OTHER
};

/// Convert SystemOperatingStatusEnum to its string representation.
inline const char* to_string(SystemOperatingStatusEnum value) {
    switch (value) {
        case SystemOperatingStatusEnum::OPERATIONAL: return "operational";
        case SystemOperatingStatusEnum::UNDER-DEVELOPMENT: return "under-development";
        case SystemOperatingStatusEnum::UNDER-MAJOR-MODIFICATION: return "under-major-modification";
        case SystemOperatingStatusEnum::DISPOSITION: return "disposition";
        case SystemOperatingStatusEnum::OTHER: return "other";
    }
    return "unknown";
}

/// Parse a string into SystemOperatingStatusEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemOperatingStatusEnum& out) {
    if (std::strcmp(str, "operational") == 0) { out = SystemOperatingStatusEnum::OPERATIONAL; return true; }
    if (std::strcmp(str, "under-development") == 0) { out = SystemOperatingStatusEnum::UNDER-DEVELOPMENT; return true; }
    if (std::strcmp(str, "under-major-modification") == 0) { out = SystemOperatingStatusEnum::UNDER-MAJOR-MODIFICATION; return true; }
    if (std::strcmp(str, "disposition") == 0) { out = SystemOperatingStatusEnum::DISPOSITION; return true; }
    if (std::strcmp(str, "other") == 0) { out = SystemOperatingStatusEnum::OTHER; return true; }
    return false;
}

/// OSCAL-defined property names used within system characteristics.
enum class SystemCharacteristicsPropNameEnum {
    IDENTITY-ASSURANCE-LEVEL,
    AUTHENTICATOR-ASSURANCE-LEVEL,
    FEDERATION-ASSURANCE-LEVEL,
    CLOUD-DEPLOYMENT-MODEL,
    CLOUD-SERVICE-MODEL
};

/// Convert SystemCharacteristicsPropNameEnum to its string representation.
inline const char* to_string(SystemCharacteristicsPropNameEnum value) {
    switch (value) {
        case SystemCharacteristicsPropNameEnum::IDENTITY-ASSURANCE-LEVEL: return "identity-assurance-level";
        case SystemCharacteristicsPropNameEnum::AUTHENTICATOR-ASSURANCE-LEVEL: return "authenticator-assurance-level";
        case SystemCharacteristicsPropNameEnum::FEDERATION-ASSURANCE-LEVEL: return "federation-assurance-level";
        case SystemCharacteristicsPropNameEnum::CLOUD-DEPLOYMENT-MODEL: return "cloud-deployment-model";
        case SystemCharacteristicsPropNameEnum::CLOUD-SERVICE-MODEL: return "cloud-service-model";
    }
    return "unknown";
}

/// Parse a string into SystemCharacteristicsPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemCharacteristicsPropNameEnum& out) {
    if (std::strcmp(str, "identity-assurance-level") == 0) { out = SystemCharacteristicsPropNameEnum::IDENTITY-ASSURANCE-LEVEL; return true; }
    if (std::strcmp(str, "authenticator-assurance-level") == 0) { out = SystemCharacteristicsPropNameEnum::AUTHENTICATOR-ASSURANCE-LEVEL; return true; }
    if (std::strcmp(str, "federation-assurance-level") == 0) { out = SystemCharacteristicsPropNameEnum::FEDERATION-ASSURANCE-LEVEL; return true; }
    if (std::strcmp(str, "cloud-deployment-model") == 0) { out = SystemCharacteristicsPropNameEnum::CLOUD-DEPLOYMENT-MODEL; return true; }
    if (std::strcmp(str, "cloud-service-model") == 0) { out = SystemCharacteristicsPropNameEnum::CLOUD-SERVICE-MODEL; return true; }
    return false;
}

/// NIST SP 800-63 assurance level values.
enum class AssuranceLevelValueEnum {
    1,
    2,
    3
};

/// Convert AssuranceLevelValueEnum to its string representation.
inline const char* to_string(AssuranceLevelValueEnum value) {
    switch (value) {
        case AssuranceLevelValueEnum::1: return "1";
        case AssuranceLevelValueEnum::2: return "2";
        case AssuranceLevelValueEnum::3: return "3";
    }
    return "unknown";
}

/// Parse a string into AssuranceLevelValueEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AssuranceLevelValueEnum& out) {
    if (std::strcmp(str, "1") == 0) { out = AssuranceLevelValueEnum::1; return true; }
    if (std::strcmp(str, "2") == 0) { out = AssuranceLevelValueEnum::2; return true; }
    if (std::strcmp(str, "3") == 0) { out = AssuranceLevelValueEnum::3; return true; }
    return false;
}

/// Cloud deployment model values used by OSCAL SSP properties.
enum class CloudDeploymentModelEnum {
    PUBLIC-CLOUD,
    PRIVATE-CLOUD,
    COMMUNITY-CLOUD,
    HYBRID-CLOUD,
    GOVERNMENT-ONLY-CLOUD,
    OTHER
};

/// Convert CloudDeploymentModelEnum to its string representation.
inline const char* to_string(CloudDeploymentModelEnum value) {
    switch (value) {
        case CloudDeploymentModelEnum::PUBLIC-CLOUD: return "public-cloud";
        case CloudDeploymentModelEnum::PRIVATE-CLOUD: return "private-cloud";
        case CloudDeploymentModelEnum::COMMUNITY-CLOUD: return "community-cloud";
        case CloudDeploymentModelEnum::HYBRID-CLOUD: return "hybrid-cloud";
        case CloudDeploymentModelEnum::GOVERNMENT-ONLY-CLOUD: return "government-only-cloud";
        case CloudDeploymentModelEnum::OTHER: return "other";
    }
    return "unknown";
}

/// Parse a string into CloudDeploymentModelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, CloudDeploymentModelEnum& out) {
    if (std::strcmp(str, "public-cloud") == 0) { out = CloudDeploymentModelEnum::PUBLIC-CLOUD; return true; }
    if (std::strcmp(str, "private-cloud") == 0) { out = CloudDeploymentModelEnum::PRIVATE-CLOUD; return true; }
    if (std::strcmp(str, "community-cloud") == 0) { out = CloudDeploymentModelEnum::COMMUNITY-CLOUD; return true; }
    if (std::strcmp(str, "hybrid-cloud") == 0) { out = CloudDeploymentModelEnum::HYBRID-CLOUD; return true; }
    if (std::strcmp(str, "government-only-cloud") == 0) { out = CloudDeploymentModelEnum::GOVERNMENT-ONLY-CLOUD; return true; }
    if (std::strcmp(str, "other") == 0) { out = CloudDeploymentModelEnum::OTHER; return true; }
    return false;
}

/// Cloud service model values used by OSCAL SSP properties.
enum class CloudServiceModelEnum {
    SAAS,
    PAAS,
    IAAS,
    OTHER
};

/// Convert CloudServiceModelEnum to its string representation.
inline const char* to_string(CloudServiceModelEnum value) {
    switch (value) {
        case CloudServiceModelEnum::SAAS: return "saas";
        case CloudServiceModelEnum::PAAS: return "paas";
        case CloudServiceModelEnum::IAAS: return "iaas";
        case CloudServiceModelEnum::OTHER: return "other";
    }
    return "unknown";
}

/// Parse a string into CloudServiceModelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, CloudServiceModelEnum& out) {
    if (std::strcmp(str, "saas") == 0) { out = CloudServiceModelEnum::SAAS; return true; }
    if (std::strcmp(str, "paas") == 0) { out = CloudServiceModelEnum::PAAS; return true; }
    if (std::strcmp(str, "iaas") == 0) { out = CloudServiceModelEnum::IAAS; return true; }
    if (std::strcmp(str, "other") == 0) { out = CloudServiceModelEnum::OTHER; return true; }
    return false;
}

/// Curated role identifiers for system characteristics responsible parties. Other values are permitted (OSCAL allow-other="yes").
enum class SystemCharacteristicsResponsibleRoleIdEnum {
    AUTHORIZING-OFFICIAL,
    AUTHORIZING-OFFICIAL-POC,
    SYSTEM-OWNER,
    SYSTEM-POC-MANAGEMENT,
    SYSTEM-POC-TECHNICAL,
    SYSTEM-POC-OTHER,
    INFORMATION-SYSTEM-SECURITY-OFFICER,
    PRIVACY-POC
};

/// Convert SystemCharacteristicsResponsibleRoleIdEnum to its string representation.
inline const char* to_string(SystemCharacteristicsResponsibleRoleIdEnum value) {
    switch (value) {
        case SystemCharacteristicsResponsibleRoleIdEnum::AUTHORIZING-OFFICIAL: return "authorizing-official";
        case SystemCharacteristicsResponsibleRoleIdEnum::AUTHORIZING-OFFICIAL-POC: return "authorizing-official-poc";
        case SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-OWNER: return "system-owner";
        case SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-MANAGEMENT: return "system-poc-management";
        case SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-TECHNICAL: return "system-poc-technical";
        case SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-OTHER: return "system-poc-other";
        case SystemCharacteristicsResponsibleRoleIdEnum::INFORMATION-SYSTEM-SECURITY-OFFICER: return "information-system-security-officer";
        case SystemCharacteristicsResponsibleRoleIdEnum::PRIVACY-POC: return "privacy-poc";
    }
    return "unknown";
}

/// Parse a string into SystemCharacteristicsResponsibleRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemCharacteristicsResponsibleRoleIdEnum& out) {
    if (std::strcmp(str, "authorizing-official") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::AUTHORIZING-OFFICIAL; return true; }
    if (std::strcmp(str, "authorizing-official-poc") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::AUTHORIZING-OFFICIAL-POC; return true; }
    if (std::strcmp(str, "system-owner") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-OWNER; return true; }
    if (std::strcmp(str, "system-poc-management") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-MANAGEMENT; return true; }
    if (std::strcmp(str, "system-poc-technical") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-TECHNICAL; return true; }
    if (std::strcmp(str, "system-poc-other") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::SYSTEM-POC-OTHER; return true; }
    if (std::strcmp(str, "information-system-security-officer") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::INFORMATION-SYSTEM-SECURITY-OFFICER; return true; }
    if (std::strcmp(str, "privacy-poc") == 0) { out = SystemCharacteristicsResponsibleRoleIdEnum::PRIVACY-POC; return true; }
    return false;
}

/// Curated information type categorization system URIs. Other values are permitted (OSCAL allow-other="yes").
enum class InformationTypeCategorizationSystemEnum {
    HTTP://DOI.ORG/10.6028/NIST.SP.800-60V2R1
};

/// Convert InformationTypeCategorizationSystemEnum to its string representation.
inline const char* to_string(InformationTypeCategorizationSystemEnum value) {
    switch (value) {
        case InformationTypeCategorizationSystemEnum::HTTP://DOI.ORG/10.6028/NIST.SP.800-60V2R1: return "http://doi.org/10.6028/NIST.SP.800-60v2r1";
    }
    return "unknown";
}

/// Parse a string into InformationTypeCategorizationSystemEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, InformationTypeCategorizationSystemEnum& out) {
    if (std::strcmp(str, "http://doi.org/10.6028/NIST.SP.800-60v2r1") == 0) { out = InformationTypeCategorizationSystemEnum::HTTP://DOI.ORG/10.6028/NIST.SP.800-60V2R1; return true; }
    return false;
}

/// OSCAL-defined property names used within system information.
enum class SystemInformationPropNameEnum {
    PRIVACY-DESIGNATION
};

/// Convert SystemInformationPropNameEnum to its string representation.
inline const char* to_string(SystemInformationPropNameEnum value) {
    switch (value) {
        case SystemInformationPropNameEnum::PRIVACY-DESIGNATION: return "privacy-designation";
    }
    return "unknown";
}

/// Parse a string into SystemInformationPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemInformationPropNameEnum& out) {
    if (std::strcmp(str, "privacy-designation") == 0) { out = SystemInformationPropNameEnum::PRIVACY-DESIGNATION; return true; }
    return false;
}

/// Privacy designation property values.
enum class PrivacyDesignationEnum {
    YES,
    NO
};

/// Convert PrivacyDesignationEnum to its string representation.
inline const char* to_string(PrivacyDesignationEnum value) {
    switch (value) {
        case PrivacyDesignationEnum::YES: return "yes";
        case PrivacyDesignationEnum::NO: return "no";
    }
    return "unknown";
}

/// Parse a string into PrivacyDesignationEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, PrivacyDesignationEnum& out) {
    if (std::strcmp(str, "yes") == 0) { out = PrivacyDesignationEnum::YES; return true; }
    if (std::strcmp(str, "no") == 0) { out = PrivacyDesignationEnum::NO; return true; }
    return false;
}

/// Curated relation values for links in system information. Other values are permitted (OSCAL allow-other="yes").
enum class SystemInformationLinkRelEnum {
    PRIVACY-IMPACT-ASSESSMENT
};

/// Convert SystemInformationLinkRelEnum to its string representation.
inline const char* to_string(SystemInformationLinkRelEnum value) {
    switch (value) {
        case SystemInformationLinkRelEnum::PRIVACY-IMPACT-ASSESSMENT: return "privacy-impact-assessment";
    }
    return "unknown";
}

/// Parse a string into SystemInformationLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, SystemInformationLinkRelEnum& out) {
    if (std::strcmp(str, "privacy-impact-assessment") == 0) { out = SystemInformationLinkRelEnum::PRIVACY-IMPACT-ASSESSMENT; return true; }
    return false;
}

/// Curated FIPS 199 impact level values. Other values are permitted (OSCAL allow-other="yes").
enum class Fips199ImpactLevelEnum {
    FIPS-199-LOW,
    FIPS-199-MODERATE,
    FIPS-199-HIGH
};

/// Convert Fips199ImpactLevelEnum to its string representation.
inline const char* to_string(Fips199ImpactLevelEnum value) {
    switch (value) {
        case Fips199ImpactLevelEnum::FIPS-199-LOW: return "fips-199-low";
        case Fips199ImpactLevelEnum::FIPS-199-MODERATE: return "fips-199-moderate";
        case Fips199ImpactLevelEnum::FIPS-199-HIGH: return "fips-199-high";
    }
    return "unknown";
}

/// Parse a string into Fips199ImpactLevelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, Fips199ImpactLevelEnum& out) {
    if (std::strcmp(str, "fips-199-low") == 0) { out = Fips199ImpactLevelEnum::FIPS-199-LOW; return true; }
    if (std::strcmp(str, "fips-199-moderate") == 0) { out = Fips199ImpactLevelEnum::FIPS-199-MODERATE; return true; }
    if (std::strcmp(str, "fips-199-high") == 0) { out = Fips199ImpactLevelEnum::FIPS-199-HIGH; return true; }
    return false;
}

/// Curated relation values for links in diagram objects. Other values are permitted (OSCAL allow-other="yes").
enum class DiagramLinkRelEnum {
    DIAGRAM
};

/// Convert DiagramLinkRelEnum to its string representation.
inline const char* to_string(DiagramLinkRelEnum value) {
    switch (value) {
        case DiagramLinkRelEnum::DIAGRAM: return "diagram";
    }
    return "unknown";
}

/// Parse a string into DiagramLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, DiagramLinkRelEnum& out) {
    if (std::strcmp(str, "diagram") == 0) { out = DiagramLinkRelEnum::DIAGRAM; return true; }
    return false;
}

/// Curated relation values for links in leveraged authorization objects. Other values are permitted (OSCAL allow-other="yes").
enum class LeveragedAuthorizationLinkRelEnum {
    SYSTEM-SECURITY-PLAN
};

/// Convert LeveragedAuthorizationLinkRelEnum to its string representation.
inline const char* to_string(LeveragedAuthorizationLinkRelEnum value) {
    switch (value) {
        case LeveragedAuthorizationLinkRelEnum::SYSTEM-SECURITY-PLAN: return "system-security-plan";
    }
    return "unknown";
}

/// Parse a string into LeveragedAuthorizationLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, LeveragedAuthorizationLinkRelEnum& out) {
    if (std::strcmp(str, "system-security-plan") == 0) { out = LeveragedAuthorizationLinkRelEnum::SYSTEM-SECURITY-PLAN; return true; }
    return false;
}

/// Values for allows-authenticated-scan property in SSP component and inventory contexts.
enum class AllowsAuthenticatedScanEnum {
    YES,
    NO
};

/// Convert AllowsAuthenticatedScanEnum to its string representation.
inline const char* to_string(AllowsAuthenticatedScanEnum value) {
    switch (value) {
        case AllowsAuthenticatedScanEnum::YES: return "yes";
        case AllowsAuthenticatedScanEnum::NO: return "no";
    }
    return "unknown";
}

/// Parse a string into AllowsAuthenticatedScanEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, AllowsAuthenticatedScanEnum& out) {
    if (std::strcmp(str, "yes") == 0) { out = AllowsAuthenticatedScanEnum::YES; return true; }
    if (std::strcmp(str, "no") == 0) { out = AllowsAuthenticatedScanEnum::NO; return true; }
    return false;
}

/// OSCAL-defined property names used in implementation statements.
enum class ControlOriginationPropNameEnum {
    CONTROL-ORIGINATION
};

/// Convert ControlOriginationPropNameEnum to its string representation.
inline const char* to_string(ControlOriginationPropNameEnum value) {
    switch (value) {
        case ControlOriginationPropNameEnum::CONTROL-ORIGINATION: return "control-origination";
    }
    return "unknown";
}

/// Parse a string into ControlOriginationPropNameEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlOriginationPropNameEnum& out) {
    if (std::strcmp(str, "control-origination") == 0) { out = ControlOriginationPropNameEnum::CONTROL-ORIGINATION; return true; }
    return false;
}

/// Control origination values.
enum class ControlOriginationValueEnum {
    ORGANIZATION,
    SYSTEM-SPECIFIC,
    CUSTOMER-CONFIGURED,
    CUSTOMER-PROVIDED,
    INHERITED
};

/// Convert ControlOriginationValueEnum to its string representation.
inline const char* to_string(ControlOriginationValueEnum value) {
    switch (value) {
        case ControlOriginationValueEnum::ORGANIZATION: return "organization";
        case ControlOriginationValueEnum::SYSTEM-SPECIFIC: return "system-specific";
        case ControlOriginationValueEnum::CUSTOMER-CONFIGURED: return "customer-configured";
        case ControlOriginationValueEnum::CUSTOMER-PROVIDED: return "customer-provided";
        case ControlOriginationValueEnum::INHERITED: return "inherited";
    }
    return "unknown";
}

/// Parse a string into ControlOriginationValueEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ControlOriginationValueEnum& out) {
    if (std::strcmp(str, "organization") == 0) { out = ControlOriginationValueEnum::ORGANIZATION; return true; }
    if (std::strcmp(str, "system-specific") == 0) { out = ControlOriginationValueEnum::SYSTEM-SPECIFIC; return true; }
    if (std::strcmp(str, "customer-configured") == 0) { out = ControlOriginationValueEnum::CUSTOMER-CONFIGURED; return true; }
    if (std::strcmp(str, "customer-provided") == 0) { out = ControlOriginationValueEnum::CUSTOMER-PROVIDED; return true; }
    if (std::strcmp(str, "inherited") == 0) { out = ControlOriginationValueEnum::INHERITED; return true; }
    return false;
}

/// Curated role identifiers for implemented requirement and statement responsible roles. Other values are permitted (OSCAL allow-other="yes").
enum class ImplementedRequirementResponsibleRoleIdEnum {
    ASSET-OWNER,
    ASSET-ADMINISTRATOR,
    SECURITY-OPERATIONS,
    NETWORK-OPERATIONS,
    INCIDENT-RESPONSE,
    HELP-DESK,
    CONFIGURATION-MANAGEMENT
};

/// Convert ImplementedRequirementResponsibleRoleIdEnum to its string representation.
inline const char* to_string(ImplementedRequirementResponsibleRoleIdEnum value) {
    switch (value) {
        case ImplementedRequirementResponsibleRoleIdEnum::ASSET-OWNER: return "asset-owner";
        case ImplementedRequirementResponsibleRoleIdEnum::ASSET-ADMINISTRATOR: return "asset-administrator";
        case ImplementedRequirementResponsibleRoleIdEnum::SECURITY-OPERATIONS: return "security-operations";
        case ImplementedRequirementResponsibleRoleIdEnum::NETWORK-OPERATIONS: return "network-operations";
        case ImplementedRequirementResponsibleRoleIdEnum::INCIDENT-RESPONSE: return "incident-response";
        case ImplementedRequirementResponsibleRoleIdEnum::HELP-DESK: return "help-desk";
        case ImplementedRequirementResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT: return "configuration-management";
    }
    return "unknown";
}

/// Parse a string into ImplementedRequirementResponsibleRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ImplementedRequirementResponsibleRoleIdEnum& out) {
    if (std::strcmp(str, "asset-owner") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::ASSET-OWNER; return true; }
    if (std::strcmp(str, "asset-administrator") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::ASSET-ADMINISTRATOR; return true; }
    if (std::strcmp(str, "security-operations") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::SECURITY-OPERATIONS; return true; }
    if (std::strcmp(str, "network-operations") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::NETWORK-OPERATIONS; return true; }
    if (std::strcmp(str, "incident-response") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::INCIDENT-RESPONSE; return true; }
    if (std::strcmp(str, "help-desk") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::HELP-DESK; return true; }
    if (std::strcmp(str, "configuration-management") == 0) { out = ImplementedRequirementResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT; return true; }
    return false;
}

/// Curated relation values for links in by-component objects. Other values are permitted (OSCAL allow-other="yes").
enum class ByComponentLinkRelEnum {
    IMPORTED-FROM,
    PROVIDED-BY
};

/// Convert ByComponentLinkRelEnum to its string representation.
inline const char* to_string(ByComponentLinkRelEnum value) {
    switch (value) {
        case ByComponentLinkRelEnum::IMPORTED-FROM: return "imported-from";
        case ByComponentLinkRelEnum::PROVIDED-BY: return "provided-by";
    }
    return "unknown";
}

/// Parse a string into ByComponentLinkRelEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ByComponentLinkRelEnum& out) {
    if (std::strcmp(str, "imported-from") == 0) { out = ByComponentLinkRelEnum::IMPORTED-FROM; return true; }
    if (std::strcmp(str, "provided-by") == 0) { out = ByComponentLinkRelEnum::PROVIDED-BY; return true; }
    return false;
}

/// Curated role identifiers for by-component responsible roles. Other values are permitted (OSCAL allow-other="yes").
enum class ByComponentResponsibleRoleIdEnum {
    ASSET-OWNER,
    ASSET-ADMINISTRATOR,
    SECURITY-OPERATIONS,
    NETWORK-OPERATIONS,
    INCIDENT-RESPONSE,
    HELP-DESK,
    CONFIGURATION-MANAGEMENT,
    MAINTAINER,
    PROVIDER
};

/// Convert ByComponentResponsibleRoleIdEnum to its string representation.
inline const char* to_string(ByComponentResponsibleRoleIdEnum value) {
    switch (value) {
        case ByComponentResponsibleRoleIdEnum::ASSET-OWNER: return "asset-owner";
        case ByComponentResponsibleRoleIdEnum::ASSET-ADMINISTRATOR: return "asset-administrator";
        case ByComponentResponsibleRoleIdEnum::SECURITY-OPERATIONS: return "security-operations";
        case ByComponentResponsibleRoleIdEnum::NETWORK-OPERATIONS: return "network-operations";
        case ByComponentResponsibleRoleIdEnum::INCIDENT-RESPONSE: return "incident-response";
        case ByComponentResponsibleRoleIdEnum::HELP-DESK: return "help-desk";
        case ByComponentResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT: return "configuration-management";
        case ByComponentResponsibleRoleIdEnum::MAINTAINER: return "maintainer";
        case ByComponentResponsibleRoleIdEnum::PROVIDER: return "provider";
    }
    return "unknown";
}

/// Parse a string into ByComponentResponsibleRoleIdEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ByComponentResponsibleRoleIdEnum& out) {
    if (std::strcmp(str, "asset-owner") == 0) { out = ByComponentResponsibleRoleIdEnum::ASSET-OWNER; return true; }
    if (std::strcmp(str, "asset-administrator") == 0) { out = ByComponentResponsibleRoleIdEnum::ASSET-ADMINISTRATOR; return true; }
    if (std::strcmp(str, "security-operations") == 0) { out = ByComponentResponsibleRoleIdEnum::SECURITY-OPERATIONS; return true; }
    if (std::strcmp(str, "network-operations") == 0) { out = ByComponentResponsibleRoleIdEnum::NETWORK-OPERATIONS; return true; }
    if (std::strcmp(str, "incident-response") == 0) { out = ByComponentResponsibleRoleIdEnum::INCIDENT-RESPONSE; return true; }
    if (std::strcmp(str, "help-desk") == 0) { out = ByComponentResponsibleRoleIdEnum::HELP-DESK; return true; }
    if (std::strcmp(str, "configuration-management") == 0) { out = ByComponentResponsibleRoleIdEnum::CONFIGURATION-MANAGEMENT; return true; }
    if (std::strcmp(str, "maintainer") == 0) { out = ByComponentResponsibleRoleIdEnum::MAINTAINER; return true; }
    if (std::strcmp(str, "provider") == 0) { out = ByComponentResponsibleRoleIdEnum::PROVIDER; return true; }
    return false;
}

enum class MappingMethodEnum {
    HUMAN,
    AUTOMATION,
    HYBRID
};

/// Convert MappingMethodEnum to its string representation.
inline const char* to_string(MappingMethodEnum value) {
    switch (value) {
        case MappingMethodEnum::HUMAN: return "human";
        case MappingMethodEnum::AUTOMATION: return "automation";
        case MappingMethodEnum::HYBRID: return "hybrid";
    }
    return "unknown";
}

/// Parse a string into MappingMethodEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MappingMethodEnum& out) {
    if (std::strcmp(str, "human") == 0) { out = MappingMethodEnum::HUMAN; return true; }
    if (std::strcmp(str, "automation") == 0) { out = MappingMethodEnum::AUTOMATION; return true; }
    if (std::strcmp(str, "hybrid") == 0) { out = MappingMethodEnum::HYBRID; return true; }
    return false;
}

enum class MatchingRationaleEnum {
    SYNTACTIC,
    SEMANTIC,
    FUNCTIONAL
};

/// Convert MatchingRationaleEnum to its string representation.
inline const char* to_string(MatchingRationaleEnum value) {
    switch (value) {
        case MatchingRationaleEnum::SYNTACTIC: return "syntactic";
        case MatchingRationaleEnum::SEMANTIC: return "semantic";
        case MatchingRationaleEnum::FUNCTIONAL: return "functional";
    }
    return "unknown";
}

/// Parse a string into MatchingRationaleEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MatchingRationaleEnum& out) {
    if (std::strcmp(str, "syntactic") == 0) { out = MatchingRationaleEnum::SYNTACTIC; return true; }
    if (std::strcmp(str, "semantic") == 0) { out = MatchingRationaleEnum::SEMANTIC; return true; }
    if (std::strcmp(str, "functional") == 0) { out = MatchingRationaleEnum::FUNCTIONAL; return true; }
    return false;
}

enum class MappingStatusEnum {
    COMPLETE,
    NOT-COMPLETE,
    DRAFT,
    DEPRECATED,
    SUPERSEDED
};

/// Convert MappingStatusEnum to its string representation.
inline const char* to_string(MappingStatusEnum value) {
    switch (value) {
        case MappingStatusEnum::COMPLETE: return "complete";
        case MappingStatusEnum::NOT-COMPLETE: return "not-complete";
        case MappingStatusEnum::DRAFT: return "draft";
        case MappingStatusEnum::DEPRECATED: return "deprecated";
        case MappingStatusEnum::SUPERSEDED: return "superseded";
    }
    return "unknown";
}

/// Parse a string into MappingStatusEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MappingStatusEnum& out) {
    if (std::strcmp(str, "complete") == 0) { out = MappingStatusEnum::COMPLETE; return true; }
    if (std::strcmp(str, "not-complete") == 0) { out = MappingStatusEnum::NOT-COMPLETE; return true; }
    if (std::strcmp(str, "draft") == 0) { out = MappingStatusEnum::DRAFT; return true; }
    if (std::strcmp(str, "deprecated") == 0) { out = MappingStatusEnum::DEPRECATED; return true; }
    if (std::strcmp(str, "superseded") == 0) { out = MappingStatusEnum::SUPERSEDED; return true; }
    return false;
}

enum class MappingSubjectTypeEnum {
    CONTROL,
    STATEMENT
};

/// Convert MappingSubjectTypeEnum to its string representation.
inline const char* to_string(MappingSubjectTypeEnum value) {
    switch (value) {
        case MappingSubjectTypeEnum::CONTROL: return "control";
        case MappingSubjectTypeEnum::STATEMENT: return "statement";
    }
    return "unknown";
}

/// Parse a string into MappingSubjectTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MappingSubjectTypeEnum& out) {
    if (std::strcmp(str, "control") == 0) { out = MappingSubjectTypeEnum::CONTROL; return true; }
    if (std::strcmp(str, "statement") == 0) { out = MappingSubjectTypeEnum::STATEMENT; return true; }
    return false;
}

enum class QualifierSubjectEnum {
    SOURCE,
    TARGET,
    BOTH
};

/// Convert QualifierSubjectEnum to its string representation.
inline const char* to_string(QualifierSubjectEnum value) {
    switch (value) {
        case QualifierSubjectEnum::SOURCE: return "source";
        case QualifierSubjectEnum::TARGET: return "target";
        case QualifierSubjectEnum::BOTH: return "both";
    }
    return "unknown";
}

/// Parse a string into QualifierSubjectEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, QualifierSubjectEnum& out) {
    if (std::strcmp(str, "source") == 0) { out = QualifierSubjectEnum::SOURCE; return true; }
    if (std::strcmp(str, "target") == 0) { out = QualifierSubjectEnum::TARGET; return true; }
    if (std::strcmp(str, "both") == 0) { out = QualifierSubjectEnum::BOTH; return true; }
    return false;
}

enum class QualifierPredicateEnum {
    HAS-REQUIREMENT,
    HAS-INCOMPATIBILITY
};

/// Convert QualifierPredicateEnum to its string representation.
inline const char* to_string(QualifierPredicateEnum value) {
    switch (value) {
        case QualifierPredicateEnum::HAS-REQUIREMENT: return "has-requirement";
        case QualifierPredicateEnum::HAS-INCOMPATIBILITY: return "has-incompatibility";
    }
    return "unknown";
}

/// Parse a string into QualifierPredicateEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, QualifierPredicateEnum& out) {
    if (std::strcmp(str, "has-requirement") == 0) { out = QualifierPredicateEnum::HAS-REQUIREMENT; return true; }
    if (std::strcmp(str, "has-incompatibility") == 0) { out = QualifierPredicateEnum::HAS-INCOMPATIBILITY; return true; }
    return false;
}

enum class QualifierCategoryEnum {
    RESTRICTED,
    ADDRESSABLE,
    BLOCKED
};

/// Convert QualifierCategoryEnum to its string representation.
inline const char* to_string(QualifierCategoryEnum value) {
    switch (value) {
        case QualifierCategoryEnum::RESTRICTED: return "restricted";
        case QualifierCategoryEnum::ADDRESSABLE: return "addressable";
        case QualifierCategoryEnum::BLOCKED: return "blocked";
    }
    return "unknown";
}

/// Parse a string into QualifierCategoryEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, QualifierCategoryEnum& out) {
    if (std::strcmp(str, "restricted") == 0) { out = QualifierCategoryEnum::RESTRICTED; return true; }
    if (std::strcmp(str, "addressable") == 0) { out = QualifierCategoryEnum::ADDRESSABLE; return true; }
    if (std::strcmp(str, "blocked") == 0) { out = QualifierCategoryEnum::BLOCKED; return true; }
    return false;
}

/// Relationship values used for mapping entries in the OSCAL namespace.
enum class RelationshipEnum {
    EQUIVALENT-TO,
    EQUAL-TO,
    SUBSET-OF,
    SUPERSET-OF,
    INTERSECTS-WITH,
    NO-RELATIONSHIP
};

/// Convert RelationshipEnum to its string representation.
inline const char* to_string(RelationshipEnum value) {
    switch (value) {
        case RelationshipEnum::EQUIVALENT-TO: return "equivalent-to";
        case RelationshipEnum::EQUAL-TO: return "equal-to";
        case RelationshipEnum::SUBSET-OF: return "subset-of";
        case RelationshipEnum::SUPERSET-OF: return "superset-of";
        case RelationshipEnum::INTERSECTS-WITH: return "intersects-with";
        case RelationshipEnum::NO-RELATIONSHIP: return "no-relationship";
    }
    return "unknown";
}

/// Parse a string into RelationshipEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, RelationshipEnum& out) {
    if (std::strcmp(str, "equivalent-to") == 0) { out = RelationshipEnum::EQUIVALENT-TO; return true; }
    if (std::strcmp(str, "equal-to") == 0) { out = RelationshipEnum::EQUAL-TO; return true; }
    if (std::strcmp(str, "subset-of") == 0) { out = RelationshipEnum::SUBSET-OF; return true; }
    if (std::strcmp(str, "superset-of") == 0) { out = RelationshipEnum::SUPERSET-OF; return true; }
    if (std::strcmp(str, "intersects-with") == 0) { out = RelationshipEnum::INTERSECTS-WITH; return true; }
    if (std::strcmp(str, "no-relationship") == 0) { out = RelationshipEnum::NO-RELATIONSHIP; return true; }
    return false;
}

/// Curated mapped resource type values. Other values are permitted (OSCAL allow-other="yes").
enum class MappingResourceTypeEnum {
    CATALOG,
    PROFILE
};

/// Convert MappingResourceTypeEnum to its string representation.
inline const char* to_string(MappingResourceTypeEnum value) {
    switch (value) {
        case MappingResourceTypeEnum::CATALOG: return "catalog";
        case MappingResourceTypeEnum::PROFILE: return "profile";
    }
    return "unknown";
}

/// Parse a string into MappingResourceTypeEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, MappingResourceTypeEnum& out) {
    if (std::strcmp(str, "catalog") == 0) { out = MappingResourceTypeEnum::CATALOG; return true; }
    if (std::strcmp(str, "profile") == 0) { out = MappingResourceTypeEnum::PROFILE; return true; }
    return false;
}

/// Curated confidence category values for OSCAL mappings. Other values are permitted (OSCAL allow-other="yes").
enum class ConfidenceCategoryEnum {
    UNSPECIFIED,
    HIGH,
    MEDIUM,
    LOW
};

/// Convert ConfidenceCategoryEnum to its string representation.
inline const char* to_string(ConfidenceCategoryEnum value) {
    switch (value) {
        case ConfidenceCategoryEnum::UNSPECIFIED: return "unspecified";
        case ConfidenceCategoryEnum::HIGH: return "high";
        case ConfidenceCategoryEnum::MEDIUM: return "medium";
        case ConfidenceCategoryEnum::LOW: return "low";
    }
    return "unknown";
}

/// Parse a string into ConfidenceCategoryEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, ConfidenceCategoryEnum& out) {
    if (std::strcmp(str, "unspecified") == 0) { out = ConfidenceCategoryEnum::UNSPECIFIED; return true; }
    if (std::strcmp(str, "high") == 0) { out = ConfidenceCategoryEnum::HIGH; return true; }
    if (std::strcmp(str, "medium") == 0) { out = ConfidenceCategoryEnum::MEDIUM; return true; }
    if (std::strcmp(str, "low") == 0) { out = ConfidenceCategoryEnum::LOW; return true; }
    return false;
}

/// Curated coverage generation method values. Other values are permitted (OSCAL allow-other="yes").
enum class CoverageGenerationMethodEnum {
    ARBITRARY
};

/// Convert CoverageGenerationMethodEnum to its string representation.
inline const char* to_string(CoverageGenerationMethodEnum value) {
    switch (value) {
        case CoverageGenerationMethodEnum::ARBITRARY: return "arbitrary";
    }
    return "unknown";
}

/// Parse a string into CoverageGenerationMethodEnum. Returns false if the string is not a valid value.
inline bool from_string(const char* str, CoverageGenerationMethodEnum& out) {
    if (std::strcmp(str, "arbitrary") == 0) { out = CoverageGenerationMethodEnum::ARBITRARY; return true; }
    return false;
}

/// Mixin providing the props and links slots that are common to many OSCAL objects.
struct HasPropsAndLinks {
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
};


/// Mixin providing props, links, and remarks slots common to most OSCAL objects. Extends HasPropsAndLinks.
struct OscalCommon : public HasPropsAndLinks {
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Mixin providing the responsible-roles slot for objects that carry role assignments.
struct HasResponsibleRoles {
    /// Responsible role assignments.
    std::vector<ResponsibleRole> responsible_roles = {};
};


/// Mixin providing the responsible-parties slot for objects that carry party assignments.
struct HasResponsibleParties {
    /// Responsible party assignments.
    std::vector<ResponsibleParty> responsible_parties = {};
};


/// A root wrapper for an OSCAL document, which may be of any OSCAL document type (e.g. Catalog, Profile, Assessment Plan, SSP).
struct OscalDocument {
};


/// Root wrapper for an OSCAL Catalog document.
struct CatalogDocument : public OscalDocument {
    /// Root catalog document.
    Catalog catalog;
};


/// A structured, organized collection of control information.
struct Catalog {
    /// Provides a globally unique means to identify a given catalog instance.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
    /// Parameters providing a mechanism for the dynamic assignment of value(s) in a control.
    std::vector<Parameter> params = {};
    /// A collection of controls.
    std::vector<Control> controls = {};
    /// A collection of control groups.
    std::vector<Group> groups = {};
};


/// A group of controls, or of groups of controls.
struct Group : public HasPropsAndLinks {
    /// Identifies the group for the purpose of cross-linking within the defining instance or from other instances that reference the catalog.
    std::optional<std::string> id = std::nullopt;
    /// A textual label that provides a sub-type or characterization of the group.
    std::optional<std::string> _class = std::nullopt;
    /// A name given to the group, which may be used by a tool for display and navigation.
    std::string title;
    /// Parameters providing a mechanism for the dynamic assignment of value(s) in a control.
    std::vector<Parameter> params = {};
    /// A collection of parts.
    std::vector<Part> parts = {};
    /// A collection of control groups.
    std::vector<Group> groups = {};
    /// A collection of controls.
    std::vector<Control> controls = {};
};


/// A structured object representing a requirement or guideline, which when implemented will reduce an aspect of risk related to an information system and its information.
struct Control : public HasPropsAndLinks {
    /// Identifies a control such that it can be referenced in the defining catalog and other OSCAL instances (e.g., profiles).
    std::string id;
    /// A textual label that provides a sub-type or characterization of the control.
    std::optional<std::string> _class = std::nullopt;
    /// A name given to the control, which may be used by a tool for display and navigation.
    std::string title;
    /// Parameters providing a mechanism for the dynamic assignment of value(s) in a control.
    std::vector<Parameter> params = {};
    /// A collection of parts.
    std::vector<Part> parts = {};
    /// A collection of controls.
    std::vector<Control> controls = {};
};


/// Provides information about the containing document, and defines concepts shared across the document.
struct Metadata : public OscalCommon, public HasResponsibleParties {
    /// A name given to the document.
    std::string title;
    /// The date and time the document was last made available.
    std::optional<std::string> published = std::nullopt;
    /// The date and time the document was last stored for later retrieval.
    std::string last_modified;
    /// Used to distinguish a specific revision of an OSCAL document.
    std::string version;
    /// The OSCAL model version the document was authored against.
    std::string oscal_version;
    /// Document identifiers qualified by an identifier scheme.
    std::vector<DocumentId> document_ids = {};
    /// An entry in a sequential list of revisions to the containing document, expected to be in reverse chronological order (i.e. latest first).
    std::vector<Revision> revisions = {};
    /// Defines a function, which might be assigned to a party in a specific situation.
    std::vector<Role> roles = {};
    /// A physical point of presence, which may be associated with people, organizations, or other concepts within the current or linked OSCAL document.
    std::vector<Location> locations = {};
    /// An organization or person, which may be associated with roles or other concepts within the current or linked OSCAL document.
    std::vector<Party> parties = {};
    /// An action applied by a role within a given party to the content.
    std::vector<Action> actions = {};
};


/// An entry in a sequential list of revisions to the containing document.
struct Revision : public OscalCommon {
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// The date and time the document was last made available.
    std::optional<std::string> published = std::nullopt;
    /// The date and time the document was last modified.
    std::optional<std::string> last_modified = std::nullopt;
    /// Used to distinguish a specific revision of an OSCAL document from other previous and future versions.
    std::string version;
    /// The OSCAL model version the document was authored against and will conform to as valid.
    std::optional<std::string> oscal_version = std::nullopt;
};


/// A document identifier qualified by an identifier scheme.
struct DocumentId {
    /// Qualifies the kind of identifier using a URI.
    std::optional<std::string> scheme = std::nullopt;
    /// A document identifier value.
    std::string identifier;
};


/// Defines a function, which might be assigned to a party in a specific situation.
struct Role : public OscalCommon {
    /// A unique identifier for the role.
    std::string id;
    /// A human-readable name or title.
    std::string title;
    /// A short common name, abbreviation, or acronym.
    std::optional<std::string> short_name = std::nullopt;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
};


/// A physical point of presence, which may be associated with people, organizations, or other concepts within the current or linked OSCAL document.
struct Location : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// Email addresses associated with the containing object.
    std::vector<std::string> email_addresses = {};
    /// Telephone numbers associated with the containing object.
    std::vector<TelephoneNumber> telephone_numbers = {};
    /// A postal address for the location.
    std::optional<Address> address = std::nullopt;
    /// The uniform resource locator (URL) for a web site or other resource associated with the location.
    std::vector<std::string> urls = {};
};


/// An organization or person, which may be associated with roles or other concepts within the current or linked OSCAL document.
struct Party : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A category describing the kind of party the object describes.
    PartyTypeEnum type;
    /// The full name of the party.
    std::optional<std::string> name = std::nullopt;
    /// A short common name, abbreviation, or acronym.
    std::optional<std::string> short_name = std::nullopt;
    /// Email addresses associated with the containing object.
    std::vector<std::string> email_addresses = {};
    /// Telephone numbers associated with the containing object.
    std::vector<TelephoneNumber> telephone_numbers = {};
    /// An identifier for a person or organization using a designated scheme, e.g. an Open Researcher and Contributor ID (ORCID).
    std::vector<MetadataPartyExternalId> external_ids = {};
    /// Postal addresses associated with the containing object.
    std::vector<Address> addresses = {};
    /// Reference to a location by UUID.
    std::vector<std::string> location_uuids = {};
    /// A reference to another party by UUID, typically an organization, that this subject is associated with.
    std::vector<std::string> member_of_organizations = {};
};


/// An identifier for a person or organization using a designated scheme, e.g. an Open Researcher and Contributor ID (ORCID).
struct PartyExternalId {
    /// Indicates the type of external identifier.
    std::string scheme;
    /// A unique human-oriented identifier within a particular context.
    std::string id;
};


/// A reference to a set of persons and/or organizations that have responsibility for performing the referenced role in the context of the containing object.
struct ResponsibleParty : public OscalCommon {
    /// A reference to a role performed by a party.
    std::string role_id;
    /// References to party UUIDs.
    std::vector<std::string> party_uuids = {};
};


/// A reference to a role with responsibility for performing a function relative to the containing object, optionally associated with a set of persons and/or organizations that perform that role.
struct ResponsibleRole : public OscalCommon {
    /// A human-oriented identifier reference to a role performed.
    std::string role_id;
    /// References to party UUIDs.
    std::vector<std::string> party_uuids = {};
};


/// An action applied by a role within a given party to the content.
struct Action : public OscalCommon, public HasResponsibleParties {
    /// A unique identifier that can be used to reference this defined action elsewhere in an OSCAL document.
    std::string uuid;
    /// The type of action documented by the assembly, such as an approval.
    ActionTypeEnum type;
    /// The date and time when the action occurred.
    std::optional<std::string> date = std::nullopt;
    /// Specifies the action type system used.
    std::string system;
};


/// A telephone service number as defined by ITU-T E.164.
struct TelephoneNumber {
    /// Indicates the type of phone number. Recommended values: home, office, mobile. Other values are permitted (OSCAL allow-other="yes").
    std::optional<std::string> type = std::nullopt;
    /// A telephone number value.
    std::string number;
};


/// A postal address for the location.
struct Address {
    /// Indicates the type of address. Recommended values: home, work. Other values are permitted (OSCAL allow-other="yes").
    std::optional<std::string> type = std::nullopt;
    /// A single line of an address.
    std::vector<std::string> addr_lines = {};
    /// City, town or geographical region for the mailing address.
    std::optional<std::string> city = std::nullopt;
    /// State, province or analogous geographical region for a mailing address.
    std::optional<std::string> state = std::nullopt;
    /// Postal or ZIP code for mailing address.
    std::optional<std::string> postal_code = std::nullopt;
    /// The ISO 3166-1 alpha-2 country code for the mailing address.
    std::optional<std::string> country = std::nullopt;
};


/// A representation of a cryptographic digest generated over a resource using a specified hash algorithm.
struct Hash {
    /// The value associated with the containing object.
    std::string value;
    /// The digest method by which a hash is derived. Recommended values are in HashAlgorithmEnum; other values are permitted (OSCAL allow-other="yes").
    std::string algorithm;
};


/// An attribute, characteristic, or quality of the containing object expressed as a namespace qualified name/value pair.
struct Property {
    /// A textual label, within a namespace, that identifies a specific attribute, characteristic, or quality of the property's containing object.
    std::string name;
    /// A unique identifier for a property.
    std::optional<std::string> uuid = std::nullopt;
    /// A namespace qualifying the property's name. This allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// Indicates the value of the attribute, characteristic, or quality.
    std::string value;
    /// A textual label that provides a sub-type or characterization of the property's name.
    std::optional<std::string> _class = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
    /// An identifier for relating distinct sets of properties.
    std::optional<std::string> group = std::nullopt;
};


/// Metadata-scoped OSCAL property.
struct MetadataProperty : public Property {
};


/// Revision-scoped OSCAL property.
struct RevisionProperty : public Property {
};


/// Location-scoped OSCAL property.
struct LocationProperty : public Property {
};


/// Party-scoped OSCAL property.
struct PartyProperty : public Property {
};


/// Back-matter resource-scoped OSCAL property.
struct ResourceProperty : public Property {
};


/// Control-common part-scoped OSCAL property.
struct PartProperty : public Property {
};


/// Control-common parameter-scoped OSCAL property.
struct ParameterProperty : public Property {
};


/// Metadata-scoped external identifier.
struct MetadataPartyExternalId : public PartyExternalId {
};


/// A reference to a local or remote resource, that has a specific relation to the containing object.
struct Link {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Describes the type of relationship provided by the link's hypertext reference. This can be an indicator of the link's purpose.
    std::optional<std::string> rel = std::nullopt;
    /// In case where the href points to a back-matter/resource, this value will indicate the URI fragment to append to any rlink associated with the resource. This value MUST be URI encoded.
    std::optional<std::string> resource_fragment = std::nullopt;
    /// A label that indicates the nature of a resource, as a data serialization or format.
    std::optional<std::string> media_type = std::nullopt;
    /// A textual label to associate with the containing object.
    std::optional<std::string> text = std::nullopt;
};


/// A collection of resources that may be referenced from within the OSCAL document instance.
struct BackMatter {
    /// A resource associated with content in the containing document instance. A resource may be directly included in the document using base64 encoding or may point to one or more equivalent internet resources.
    std::vector<Resource> resources = {};
};


/// A resource associated with content in the containing document instance. A resource may be directly included in the document using base64 encoding or may point to one or more equivalent internet resources.
struct Resource {
    /// A unique identifier for a resource.
    std::string uuid;
    /// An optional name given to the resource, which may be used by a tool for display and navigation.
    std::optional<std::string> title = std::nullopt;
    /// An optional short summary of the resource used to indicate the purpose of the resource.
    std::optional<std::string> description = std::nullopt;
    /// A list of properties.
    std::vector<ResourceProperty> props = {};
    /// Document identifiers qualified by an identifier scheme.
    std::vector<DocumentId> document_ids = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
    /// An optional citation consisting of end note text using structured markup.
    std::optional<Citation> citation = std::nullopt;
    /// A URL-based pointer to an external resource with an optional hash for verification and change detection.
    std::vector<ResourceLink> rlinks = {};
    /// A resource encoded using the Base64 alphabet defined by RFC 2045.
    std::optional<Base64Resource> base64 = std::nullopt;
};


/// An optional citation consisting of end note text using structured markup.
struct Citation : public HasPropsAndLinks {
    /// A line of citation text.
    std::string text;
};


/// A URL-based pointer to an external resource with an optional hash for verification and change detection.
struct ResourceLink {
    /// A resolvable URL pointing to the referenced resource.
    std::string href;
    /// A label that indicates the nature of a resource, as a data serialization or format.
    std::optional<std::string> media_type = std::nullopt;
    /// A representation of a cryptographic digest generated over a resource using a specified hash algorithm.
    std::vector<Hash> hashes = {};
};


/// A resource encoded using the Base64 alphabet defined by RFC 2045.
struct Base64Resource {
    /// A label that indicates the nature of a resource, as a data serialization or format.
    std::optional<std::string> media_type = std::nullopt;
    /// The value associated with the containing object.
    std::string value;
    /// Name of the file before it was encoded as Base64 to be embedded in a resource.
    std::optional<std::string> filename = std::nullopt;
};


/// An annotated, markup-based textual element of a control's or catalog group's definition, or a child of another part.
struct Part : public HasPropsAndLinks {
    /// A unique identifier for the part.
    std::optional<std::string> id = std::nullopt;
    /// A textual label that uniquely identifies the part's semantic type, which exists in a value space qualified by the ns.
    std::string name;
    /// An optional namespace qualifying the part's name. This allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// An optional textual providing a sub-type or characterization of the part's name, or a category to which the part belongs.
    std::optional<std::string> _class = std::nullopt;
    /// An optional name given to the part, which may be used by a tool for display and navigation.
    std::optional<std::string> title = std::nullopt;
    /// Permits multiple paragraphs, lists, tables etc.
    std::optional<std::string> prose = std::nullopt;
    /// A collection of parts.
    std::vector<Part> parts = {};
};


/// Parameters provide a mechanism for the dynamic assignment of value(s) in a control.
struct Parameter : public OscalCommon {
    /// A unique identifier for the parameter.
    std::string id;
    /// A textual label that provides a characterization of the type, purpose, use or scope of the parameter.
    std::optional<std::string> _class = std::nullopt;
    /// (deprecated) Another parameter invoking this one. This construct has been deprecated and should not be used.
    std::optional<std::string> depends_on = std::nullopt;
    /// A short, placeholder name for the parameter, which can be used as a substitute for a value if no value is assigned.
    std::optional<std::string> label = std::nullopt;
    /// Describes the purpose and use of a parameter.
    std::optional<std::string> usage = std::nullopt;
    /// A formal or informal expression of a constraint or test.
    std::vector<ParameterConstraint> constraints = {};
    /// A prose statement that provides a recommendation for the use of a parameter.
    std::vector<ParameterGuideline> guidelines = {};
    /// A parameter value or set of values.
    std::vector<std::string> values = {};
    /// Presenting a choice among alternatives.
    std::optional<ParameterSelection> select = std::nullopt;
};


/// A formal or informal expression of a constraint or test.
struct ParameterConstraint {
    /// A textual summary of the constraint to be applied.
    std::optional<std::string> description = std::nullopt;
    /// A test expression which is expected to be evaluated by a tool.
    std::vector<ConstraintTest> tests = {};
};


/// A test expression which is expected to be evaluated by a tool.
struct ConstraintTest {
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
    /// A formal (executable) expression of a constraint.
    std::string expression;
};


/// A prose statement that provides a recommendation for the use of a parameter.
struct ParameterGuideline {
    /// Prose permits multiple paragraphs, lists, tables etc.
    std::string prose;
};


/// Presenting a choice among alternatives.
struct ParameterSelection {
    /// Describes the number of selections that must occur. Without this setting, only one value should be assumed to be permitted.
    std::optional<ParameterCardinalityEnum> how_many = std::nullopt;
    /// A value selection among several such options.
    std::vector<std::string> choice = {};
};


/// Include all controls from the imported catalog or profile resources.
struct IncludeAll {
};


/// Selecting a set of controls by matching their IDs with a wildcard pattern.
struct ControlMatching {
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
    /// A glob expression matching the IDs of one or more controls to be selected.
    std::optional<std::string> pattern = std::nullopt;
};


/// Select a control or controls from an imported control set.
struct SelectControlById {
    /// When a control is included, whether its child (dependent) controls are also included.
    std::optional<WithChildControlsEnum> with_child_controls = std::nullopt;
    /// Selecting a control by its ID given as a literal.
    std::vector<std::string> with_ids = {};
    /// Selecting a set of controls by matching their IDs with a wildcard pattern.
    std::vector<ControlMatching> matching = {};
};


/// Root wrapper for an OSCAL Profile document.
struct ProfileDocument : public OscalDocument {
    /// The root profile object.
    Profile profile;
};


/// An OSCAL Profile that designates a set of controls from one or more catalogs or profiles, optionally restructures and modifies them, to describe a basis for a security standard or body of practice.
struct Profile {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Designates source catalog or profile resources to be imported into the profile.
    std::vector<ProfileImport> imports = {};
    /// Structuring directives for how controls are organized after profile resolution.
    std::optional<ProfileMerge> merge = std::nullopt;
    /// Set parameters or amend controls in resolution.
    std::optional<ProfileModify> modify = std::nullopt;
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Designates a referenced source catalog or profile that provides a source of control information for use in creating a new overlay or baseline.
struct ProfileImport {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Include all selectable objects in the containing OSCAL selection context.
    std::optional<IncludeAll> include_all = std::nullopt;
    /// Control-selection entries to include in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SelectControlById> include_controls = {};
    /// Control-selection entries to exclude in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SelectControlById> exclude_controls = {};
};


/// Provides structuring directives that instruct how controls are organized after profile resolution.
struct ProfileMerge {
    /// Defines how to resolve duplicate instances of the same control.
    std::optional<CombinationRule> combine = std::nullopt;
    /// Directs that controls appear without any grouping structure.
    std::optional<MergeFlat> flat = std::nullopt;
    /// When true, retain the original grouping structure as defined in the import source.
    std::optional<bool> as_is = std::nullopt;
    /// Provides an alternate grouping structure that selected controls will be placed in.
    std::optional<MergeCustom> custom = std::nullopt;
};


/// Defines how to resolve duplicate instances of the same control (e.g., controls with the same ID) encountered in a profile merge.
struct CombinationRule {
    /// Method indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    std::optional<CombinationMethodEnum> method = std::nullopt;
};


/// Directs that controls appear without any grouping structure after profile resolution.
struct MergeFlat {
};


/// Provides an alternate grouping structure that selected controls will be placed in after profile resolution.
struct MergeCustom {
    /// A collection of control groups.
    std::vector<ProfileGroup> groups = {};
    /// Specifies which controls to use in the containing context.
    std::vector<InsertControls> insert_controls = {};
};


/// A group of (selected) controls or of groups of controls within a profile custom merge structure.
struct ProfileGroup : public OscalCommon {
    /// A unique human-oriented identifier within a particular context.
    std::optional<std::string> id = std::nullopt;
    /// A textual label that provides a sub-type or characterization.
    std::optional<std::string> _class = std::nullopt;
    /// A human-readable name or title.
    std::string title;
    /// Parameters providing a mechanism for the dynamic assignment of value(s) in a control.
    std::vector<Parameter> params = {};
    /// A collection of parts.
    std::vector<Part> parts = {};
    /// A collection of control groups.
    std::vector<ProfileGroup> groups = {};
    /// Specifies which controls to use in the containing context.
    std::vector<InsertControls> insert_controls = {};
};


/// Set parameters or amend controls in resolution.
struct ProfileModify {
    /// A parameter setting to be propagated to points of insertion.
    std::vector<ParameterSetting> set_parameters = {};
    /// Specifies changes to be made to included controls in resolution.
    std::vector<Alteration> alters = {};
};


/// A parameter setting to be propagated to points of insertion in a resolved profile.
struct ParameterSetting : public HasPropsAndLinks {
    /// The identifier for the parameter being set or referenced.
    std::string param_id;
    /// A textual label that provides a sub-type or characterization.
    std::optional<std::string> _class = std::nullopt;
    /// (deprecated) Another parameter invoking this one. This construct has been deprecated and should not be used.
    std::optional<std::string> depends_on = std::nullopt;
    /// A short, placeholder name for the parameter, which can be used as a substitute for a value if no value is assigned.
    std::optional<std::string> label = std::nullopt;
    /// Describes the purpose and use of a parameter.
    std::optional<std::string> usage = std::nullopt;
    /// A formal or informal expression of a constraint or test.
    std::vector<ParameterConstraint> constraints = {};
    /// A prose statement that provides a recommendation for the use of a parameter.
    std::vector<ParameterGuideline> guidelines = {};
    /// A parameter value or set of values.
    std::vector<std::string> values = {};
    /// Presenting a choice among alternatives.
    std::optional<ParameterSelection> select = std::nullopt;
};


/// Specifies changes to be made to an included control when a profile is resolved.
struct Alteration {
    /// A reference to a control by its identifier.
    std::string control_id;
    /// Specifies objects to be removed from a control in resolution.
    std::vector<Removal> removes = {};
    /// Specifies content to be added into a control in resolution.
    std::vector<Addition> adds = {};
};


/// Specifies objects to be removed from a control based on aspects of the object that must all match.
struct Removal {
    /// Identify items to remove by their assigned name.
    std::optional<std::string> by_name = std::nullopt;
    /// Identify items to remove by their class label.
    std::optional<std::string> by_class = std::nullopt;
    /// Identify or target items by their id value.
    std::optional<std::string> by_id = std::nullopt;
    /// Identify items to remove by the item's information object type name.
    std::optional<ByItemNameEnum> by_item_name = std::nullopt;
    /// Identify items to remove by the item's namespace.
    std::optional<std::string> by_ns = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Specifies content to be added into controls in resolution.
struct Addition {
    /// Where to add new content relative to the targeted element.
    std::optional<AdditionPositionEnum> position = std::nullopt;
    /// Identify or target items by their id value.
    std::optional<std::string> by_id = std::nullopt;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// Parameters providing a mechanism for the dynamic assignment of value(s) in a control.
    std::vector<Parameter> params = {};
    /// A list of properties.
    std::vector<ProfileAlterationProperty> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// A collection of parts.
    std::vector<Part> parts = {};
};


/// OSCAL property entries allowed in profile modify additions.
struct ProfileAlterationProperty : public Property {
};


/// Specifies which controls to use in the containing context (as part of a group or custom merge structure).
struct InsertControls {
    /// A designation of how a selection of controls is to be ordered.
    std::optional<InsertOrderEnum> order = std::nullopt;
    /// Include all selectable objects in the containing OSCAL selection context.
    std::optional<IncludeAll> include_all = std::nullopt;
    /// Control-selection entries to include in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SelectControlById> include_controls = {};
    /// Control-selection entries to exclude in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SelectControlById> exclude_controls = {};
};


/// Root wrapper for an OSCAL Assessment Plan document.
struct AssessmentPlanDocument : public OscalDocument {
    /// The root assessment plan object.
    AssessmentPlan assessment_plan;
};


/// An assessment plan, such as those provided by a FedRAMP assessor.
struct AssessmentPlan {
    /// Assessment Plan Universally Unique Identifier.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Used to import information about the system from an SSP.
    ImportSSP import_ssp;
    /// Used to define data objects that do not appear in the referenced SSP.
    std::optional<LocalDefinitions> local_definitions = std::nullopt;
    /// Terms and conditions under which an assessment can be performed.
    std::optional<TermsAndConditions> terms_and_conditions = std::nullopt;
    /// Identifies system elements being assessed.
    std::vector<AssessmentSubject> assessment_subjects = {};
    /// Identifies the assets used to perform this assessment.
    std::optional<AssessmentAssets> assessment_assets = std::nullopt;
    /// Identifies the controls being assessed and their control objectives.
    ReviewedControls reviewed_controls;
    /// A collection of tasks.
    std::vector<Task> tasks = {};
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Used by the assessment plan and POA&M to import information about the system.
struct ImportSSP {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Used to define data objects that are used in the assessment plan, that do not appear in the referenced SSP.
struct LocalDefinitions {
    /// A collection of system components.
    std::vector<SystemComponent> components = {};
    /// A collection of inventory items.
    std::vector<InventoryItem> inventory_items = {};
    /// A collection of system users.
    std::vector<SystemUser> users = {};
    /// A collection of locally-defined control objectives.
    std::vector<LocalObjective> objectives_and_methods = {};
    /// A collection of activities.
    std::vector<Activity> activities = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Used to define various terms and conditions under which an assessment can be performed.
struct TermsAndConditions {
    /// A collection of parts.
    std::vector<TermsAndConditionsPart> parts = {};
};


/// Identifies the controls being assessed and their control objectives.
struct ReviewedControls : public OscalCommon {
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Identifies the controls being assessed.
    std::vector<ControlSelection> control_selections = {};
    /// Identifies the control objectives of the assessment.
    std::vector<ControlObjectiveSelection> control_objective_selections = {};
};


/// Identifies the controls being assessed.
struct ControlSelection : public OscalCommon {
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Include all selectable objects in the containing OSCAL selection context.
    std::optional<IncludeAll> include_all = std::nullopt;
    /// Control-selection entries to include in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<AssessmentSelectControlById> include_controls = {};
    /// Control-selection entries to exclude in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<AssessmentSelectControlById> exclude_controls = {};
};


/// Identifies the control objectives of the assessment.
struct ControlObjectiveSelection : public OscalCommon {
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Include all selectable objects in the containing OSCAL selection context.
    std::optional<IncludeAll> include_all = std::nullopt;
    /// Objectives to include in the assessment.
    std::vector<SelectObjectiveById> include_objectives = {};
    /// Objectives to exclude from the assessment.
    std::vector<SelectObjectiveById> exclude_objectives = {};
};


/// Select a specific control for inclusion/exclusion in the assessment by literal control ID and optional statement IDs.
struct AssessmentSelectControlById {
    /// A reference to a control by its identifier.
    std::string control_id;
    /// Statement IDs for control selection.
    std::vector<std::string> statement_ids = {};
};


/// Used to select a control objective for inclusion/exclusion.
struct SelectObjectiveById {
    /// Reference to a control objective by its identifier.
    std::string objective_id;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies system elements being assessed, such as components, inventory items, and locations.
struct AssessmentSubject : public OscalCommon {
    /// Indicates the nature or kind of the containing object.
    std::string type;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Include all selectable objects in the containing OSCAL selection context.
    std::optional<IncludeAll> include_all = std::nullopt;
    /// Assessment subjects to include.
    std::vector<SelectSubjectById> include_subjects = {};
    /// Assessment subjects to exclude.
    std::vector<SelectSubjectById> exclude_subjects = {};
};


/// Identifies a set of assessment subjects to include/exclude by UUID.
struct SelectSubjectById : public OscalCommon {
    /// A UUID reference to the identified subject.
    std::string subject_uuid;
    /// Indicates the nature or kind of the containing object.
    std::string type;
};


/// A human-oriented identifier reference to a resource. Use type to indicate whether the identified resource is a component, inventory item, location, user, or something else.
struct SubjectReference : public OscalCommon {
    /// A UUID reference to the identified subject.
    std::string subject_uuid;
    /// Indicates the nature or kind of the containing object.
    std::string type;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
};


/// Used when the assessment subjects will be determined as part of one or more other assessment activities.
struct AssessmentSubjectPlaceholder : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Source references or source-participation entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<AssessmentSubjectSource> sources = {};
};


/// Assessment subjects will be identified while conducting the referenced activity.
struct AssessmentSubjectSource {
    /// A UUID reference to a task.
    std::string task_uuid;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies the assets used to perform this assessment.
struct AssessmentAssets {
    /// A collection of system components.
    std::vector<SystemComponent> components = {};
    /// A collection of assessment platforms.
    std::vector<AssessmentPlatform> assessment_platforms = {};
};


/// Used to represent the toolset used to perform aspects of the assessment.
struct AssessmentPlatform : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// The set of components used by the assessment platform.
    std::vector<UsesComponent> uses_components = {};
};


/// The set of components that are used by the assessment platform.
struct UsesComponent : public OscalCommon, public HasResponsibleParties {
    /// A UUID reference to a component.
    std::string component_uuid;
};


/// A local definition of a control objective for this assessment. Uses catalog syntax for control objective and assessment actions.
struct LocalObjective : public OscalCommon {
    /// A reference to a control by its identifier.
    std::string control_id;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// A collection of parts.
    std::vector<ControlPart> parts = {};
};


/// A local definition of a control objective.
struct AssessmentMethod : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// An assessment part.
    AssessmentPart part;
};


/// Identifies an assessment or related process that can be performed. In the assessment plan, this is an intended activity.
struct Activity : public OscalCommon, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// A collection of steps in an activity.
    std::vector<Step> steps = {};
    /// A reference to reviewed controls for this activity or step.
    std::optional<ReviewedControls> related_controls = std::nullopt;
};


/// Identifies an individual step in a series of steps related to an activity, such as an assessment test or examination procedure.
struct Step : public OscalCommon, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// Identifies the controls being assessed and their control objectives.
    std::optional<ReviewedControls> reviewed_controls = std::nullopt;
};


/// Represents a scheduled event or milestone, which may be associated with a series of assessment actions.
struct Task : public OscalCommon, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Indicates the nature or kind of the containing object.
    std::string type;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// The timing under which a task is intended to occur.
    std::optional<EventTiming> timing = std::nullopt;
    /// Tasks that this task depends on.
    std::vector<TaskDependency> dependencies = {};
    /// Activities associated with this task.
    std::vector<AssociatedActivity> associated_activities = {};
    /// A collection of tasks.
    std::vector<Task> tasks = {};
    /// Assessment subjects or subject references for this object.
    std::vector<AssessmentSubject> subjects = {};
};


/// The timing under which the task is intended to occur.
struct EventTiming {
    /// The task is intended to occur on the specified date.
    std::optional<OnDateCondition> on_date = std::nullopt;
    /// The task is intended to occur within the specified date range.
    std::optional<WithinDateRange> within_date_range = std::nullopt;
    /// The task is intended to occur at the specified frequency.
    std::optional<AtFrequency> at_frequency = std::nullopt;
};


/// The task is intended to occur on the specified date.
struct OnDateCondition {
    /// The date and time when the action occurred.
    std::string date;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// The task is intended to occur within the specified date range.
struct WithinDateRange {
    /// The start date/time.
    std::string start;
    /// The end date/time.
    std::string end;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// The task is intended to occur at the specified frequency.
struct AtFrequency {
    /// The task must occur every period (in the given units).
    int32_t period;
    /// The unit of time for the period.
    TimingUnitEnum unit;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Used to indicate that a task is dependent on another task.
struct TaskDependency {
    /// A UUID reference to a task.
    std::string task_uuid;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies an individual activity to be performed as part of a task.
struct AssociatedActivity : public OscalCommon, public HasResponsibleRoles {
    /// A UUID reference to an activity.
    std::string activity_uuid;
    /// Assessment subjects or subject references for this object.
    std::vector<AssessmentSubject> subjects = {};
};


/// A partition of an assessment plan or results or a child of another part.
struct AssessmentPart : public HasPropsAndLinks {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::optional<std::string> uuid = std::nullopt;
    /// A textual label that uniquely identifies an attribute or semantic type.
    std::string name;
    /// An optional namespace qualifying a name. Allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// A textual label that provides a sub-type or characterization.
    std::optional<std::string> _class = std::nullopt;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// Permits multiple paragraphs, lists, tables etc.
    std::optional<std::string> prose = std::nullopt;
    /// A collection of parts.
    std::vector<AssessmentPart> parts = {};
};


/// A terms-and-conditions scoped assessment part.
struct TermsAndConditionsPart : public AssessmentPart {
};


/// An annotated, markup-based textual element of a control's or catalog group's definition, or a child of another part.
struct ControlPart : public HasPropsAndLinks {
    /// A unique human-oriented identifier within a particular context.
    std::optional<std::string> id = std::nullopt;
    /// A textual label that uniquely identifies an attribute or semantic type.
    std::string name;
    /// An optional namespace qualifying a name. Allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// A textual label that provides a sub-type or characterization.
    std::optional<std::string> _class = std::nullopt;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// Permits multiple paragraphs, lists, tables etc.
    std::optional<std::string> prose = std::nullopt;
    /// A collection of parts.
    std::vector<ControlPart> parts = {};
};


/// Identifies the parameter that will be set by the enclosed value.
struct SetParameter {
    /// The identifier for the parameter being set or referenced.
    std::string param_id;
    /// A parameter value or set of values.
    std::vector<std::string> values = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A defined component that can be part of an implemented system.
struct SystemComponent : public OscalCommon, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Indicates the nature or kind of the containing object.
    std::string type;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// A summary of the technological or business purpose of the component.
    std::optional<std::string> purpose = std::nullopt;
    /// Information about the protocol used to provide a service.
    std::vector<Protocol> protocols = {};
    /// Status indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    ComponentStatus status;
};


/// Describes the operational status of the system component.
struct ComponentStatus {
    /// The operational status.
    ComponentStateEnum state;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Information about the protocol used to provide a service.
struct Protocol {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::optional<std::string> uuid = std::nullopt;
    /// A textual label that uniquely identifies an attribute or semantic type.
    std::optional<std::string> name = std::nullopt;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// Where applicable, the transport layer protocol port range.
    std::vector<PortRange> port_ranges = {};
};


/// Where applicable, the transport layer protocol port range.
struct PortRange {
    /// The start date/time.
    std::optional<int32_t> start = std::nullopt;
    /// The end date/time.
    std::optional<int32_t> end = std::nullopt;
    /// Indicates the transport type.
    std::optional<TransportEnum> transport = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Indicates the degree to which a given control is implemented.
struct ImplementationStatus {
    /// Identifies the implementation status of the control or control objective. Recommended values are in ImplementationStatusStateEnum; other values are permitted (OSCAL allow-other="yes").
    std::string state;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A type of user that interacts with the system based on an associated role.
struct SystemUser : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A short common name, abbreviation, or acronym.
    std::optional<std::string> short_name = std::nullopt;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Role identifiers associated with the user.
    std::vector<std::string> role_ids = {};
    /// A collection of authorized privileges.
    std::vector<AuthorizedPrivilege> authorized_privileges = {};
};


/// Identifies a specific system privilege held by the user, along with an associated description and/or rationale for the privilege.
struct AuthorizedPrivilege {
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Describes a function performed for a given authorized privilege.
    std::vector<std::string> functions_performed = {};
};


/// A single managed inventory item within the system.
struct InventoryItem : public OscalCommon, public HasResponsibleParties {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::string description;
    /// A collection of implemented components.
    std::vector<ImplementedComponent> implemented_components = {};
};


/// The set of components that are implemented in a given system inventory item.
struct ImplementedComponent : public OscalCommon, public HasResponsibleParties {
    /// A UUID reference to a component.
    std::string component_uuid;
};


/// A human-oriented, globally unique identifier for a system.
struct SystemId {
    /// A unique human-oriented identifier within a particular context.
    std::string id;
    /// A human-readable label for a specific identifier scheme. Recommended values are in SystemIdentifierTypeEnum; other URI values are permitted (OSCAL allow-other="yes").
    std::optional<std::string> identifier_type = std::nullopt;
};


/// Implementation-common scoped OSCAL property.
struct ImplementationCommonProperty : public Property {
};


/// Implementation-common scoped OSCAL link.
struct ImplementationCommonLink : public Link {
};


/// Implementation-common scoped responsible role.
struct ImplementationResponsibleRole : public ResponsibleRole {
};


/// Implementation-common scoped responsible party.
struct ImplementationResponsibleParty : public ResponsibleParty {
};


/// Identifies the source of the finding, such as a tool, interviewed person, or activity.
struct Origin {
    /// The actor that produces an observation, a finding, or a risk.
    std::vector<OriginActor> actors = {};
    /// Identifies tasks for which the containing object is a consequence.
    std::vector<RelatedTask> related_tasks = {};
};


/// The actor that produces an observation, a finding, or a risk.
struct OriginActor : public HasPropsAndLinks {
    /// Indicates the nature or kind of the containing object.
    OriginActorTypeEnum type;
    /// A machine-oriented identifier reference to the tool or person based on the associated type.
    std::string actor_uuid;
    /// A reference to a role by its identifier.
    std::optional<std::string> role_id = std::nullopt;
};


/// Identifies an individual task for which the containing object is a consequence of.
struct RelatedTask : public OscalCommon, public HasResponsibleParties {
    /// A UUID reference to a task.
    std::string task_uuid;
    /// Assessment subjects or subject references for this object.
    std::vector<AssessmentSubject> subjects = {};
    /// Used to detail assessment subjects that were identified by this task.
    std::optional<IdentifiedSubject> identified_subject = std::nullopt;
};


/// Used to detail assessment subjects that were identified by this task.
struct IdentifiedSubject {
    /// A reference to an assessment subject placeholder defined in the assessment plan.
    std::string subject_placeholder_uuid;
    /// Assessment subjects or subject references for this object.
    std::vector<AssessmentSubject> subjects = {};
};


/// Describes an individual observation.
struct Observation : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// Identifies how the observation was made. Recommended values are in ObservationMethodEnum; other values are permitted (OSCAL allow-other="yes").
    std::vector<std::string> methods = {};
    /// Identifies the nature of the observation. Recommended values are in ObservationTypeEnum; other values are permitted (OSCAL allow-other="yes").
    std::vector<std::string> types = {};
    /// Identifies the source of observations, findings, or risks.
    std::vector<Origin> origins = {};
    /// Assessment subjects or subject references for this object.
    std::vector<SubjectReference> subjects = {};
    /// Links the observation to relevant evidence.
    std::vector<RelevantEvidence> relevant_evidence = {};
    /// Date/time stamp identifying when the finding information was collected.
    std::string collected;
    /// Date/time identifying when the finding information is no longer considered valid.
    std::optional<std::string> expires = std::nullopt;
};


/// Links this observation to relevant evidence.
struct RelevantEvidence : public OscalCommon {
    /// A resolvable URL reference to a resource.
    std::optional<std::string> href = std::nullopt;
    /// A human-readable description.
    std::string description;
};


/// Describes an individual finding.
struct Finding : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// Identifies the target of a finding.
    FindingTarget target;
    /// A reference to the implementation statement in the SSP to which this finding is related.
    std::optional<std::string> implementation_statement_uuid = std::nullopt;
    /// Identifies the source of observations, findings, or risks.
    std::vector<Origin> origins = {};
    /// Relates the containing object to a set of referenced observations.
    std::vector<RelatedObservation> related_observations = {};
    /// Relates the finding to a set of referenced risks.
    std::vector<AssociatedRisk> related_risks = {};
};


/// Captures an assessor's conclusions regarding the degree to which an objective is satisfied.
struct FindingTarget : public OscalCommon {
    /// Indicates the nature or kind of the containing object.
    FindingTargetTypeEnum type;
    /// Identifies the specific target qualified by the type.
    std::string target_id;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// Identifies the implementation status of the control.
    std::optional<ImplementationStatus> implementation_status = std::nullopt;
    /// Status indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    ObjectiveStatus status;
};


/// A determination of if the objective is satisfied or not within a given system.
struct ObjectiveStatus {
    /// An indication as to whether the objective is satisfied or not.
    ObjectiveStatusStateEnum state;
    /// The reason the objective was given its status. Recommended values are in ObjectiveStatusReasonEnum; other values are permitted (OSCAL allow-other="yes").
    std::optional<std::string> reason = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Relates the identified element to a set of referenced observations.
struct RelatedObservation {
    /// A machine-oriented identifier reference to an observation defined in the list of observations.
    std::string observation_uuid;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Relates the finding to a set of referenced risks.
struct AssociatedRisk {
    /// A machine-oriented identifier reference to a risk defined in the list of risks.
    std::string risk_uuid;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// An identified risk.
struct Risk : public HasPropsAndLinks {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// An assessor's summary of the risk, in narrative form.
    std::string statement;
    /// Identifies the source of observations, findings, or risks.
    std::vector<Origin> origins = {};
    /// The referenced threat identifiers.
    std::vector<ThreatId> threat_ids = {};
    /// Supporting information about the risk and how it relates to the system.
    std::vector<Characterization> characterizations = {};
    /// Describes existing mitigating factors that may affect the overall determination of the risk.
    std::vector<MitigatingFactor> mitigating_factors = {};
    /// The date/time by which the risk must be resolved.
    std::optional<std::string> deadline = std::nullopt;
    /// Describes either recommended or actual responses to a risk.
    std::vector<Response> remediations = {};
    /// A log of all risk-related tasks taken.
    std::optional<RiskLog> risk_log = std::nullopt;
    /// Relates the containing object to a set of referenced observations.
    std::vector<RelatedObservation> related_observations = {};
    /// Status indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    std::string status;
};


/// A pointer, by ID, to an externally-defined threat.
struct ThreatId {
    /// A resolvable URL reference to a resource.
    std::optional<std::string> href = std::nullopt;
    /// Specifies the system or scheme from which the identifier originates.
    std::string system;
    /// A unique human-oriented identifier within a particular context.
    std::string id;
};


/// A collection of descriptive data about the containing object from a specific origin.
struct Characterization : public HasPropsAndLinks {
    /// The source of the finding.
    Origin origin;
    /// An individual characteristic that is part of a larger set produced by the same actor.
    std::vector<Facet> facets = {};
};


/// An individual characteristic that is part of a larger set produced by the same actor.
struct Facet : public OscalCommon {
    /// A textual label that uniquely identifies an attribute or semantic type.
    std::string name;
    /// The value associated with the containing object.
    std::string value;
    /// Specifies the system or scheme from which the identifier originates.
    std::string system;
};


/// Describes an existing mitigating factor that may affect the overall determination of the risk.
struct MitigatingFactor : public HasPropsAndLinks {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::string description;
    /// A machine-oriented, globally unique identifier with cross-instance scope that can be used to reference this implementation statement elsewhere in this or other OSCAL instances.
    std::optional<std::string> implementation_uuid = std::nullopt;
    /// Assessment subjects or subject references for this object.
    std::vector<SubjectReference> subjects = {};
};


/// Describes either recommended or an actual plan for addressing the risk.
struct Response : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// Identifies whether this is a recommendation or an actual plan. Recommended values are in ResponseLifecycleEnum; other values are permitted (OSCAL allow-other="yes").
    std::string lifecycle;
    /// Identifies the source of observations, findings, or risks.
    std::vector<Origin> origins = {};
    /// Identifies an asset required to achieve remediation.
    std::vector<RequiredAsset> required_assets = {};
    /// A collection of tasks.
    std::vector<Task> tasks = {};
};


/// Identifies an asset required to achieve remediation.
struct RequiredAsset : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// Assessment subjects or subject references for this object.
    std::vector<SubjectReference> subjects = {};
};


/// A log of all risk-related tasks taken.
struct RiskLog {
    /// Identifies an individual risk response that occurred as part of managing an identified risk.
    std::vector<RiskLogEntry> entries = {};
};


/// Identifies an individual risk response that occurred as part of managing an identified risk.
struct RiskLogEntry : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// The start date/time.
    std::string start;
    /// The end date/time.
    std::optional<std::string> end = std::nullopt;
    /// Used to indicate who created a log entry in what role.
    std::vector<LoggedBy> logged_by = {};
    /// Identifies the risk change that prompted the log entry. Recommended values are in RiskStatusEnum; other values are permitted (OSCAL allow-other="yes").
    std::optional<std::string> status_change = std::nullopt;
    /// Identifies an individual risk response that this log entry is for.
    std::vector<RiskResponseReference> related_responses = {};
};


/// Used to indicate who created a log entry in what role.
struct LoggedBy {
    /// A machine-oriented identifier reference to the party who is making the log entry.
    std::string party_uuid;
    /// A reference to a role by its identifier.
    std::optional<std::string> role_id = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies an individual risk response that this log entry is for.
struct RiskResponseReference : public OscalCommon {
    /// A machine-oriented identifier reference to a unique risk response.
    std::string response_uuid;
    /// Identifies tasks for which the containing object is a consequence.
    std::vector<RelatedTask> related_tasks = {};
};


/// Root wrapper for an OSCAL System Security Plan document.
struct SspDocument : public OscalDocument {
    /// A system security plan, such as those described in NIST SP 800-18.
    SystemSecurityPlan system_security_plan;
};


/// A system security plan, such as those described in NIST SP 800-18.
struct SystemSecurityPlan {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Used to import the OSCAL profile representing the system's control baseline.
    ImportProfile import_profile;
    /// Contains the characteristics of the system, such as its name, purpose, and security impact level.
    SystemCharacteristics system_characteristics;
    /// Provides information as to how the system is implemented.
    SystemImplementation system_implementation;
    /// Describes how the system satisfies a set of controls.
    SspControlImplementation control_implementation;
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Used to import the OSCAL profile representing the system's control baseline.
struct ImportProfile {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Contains the characteristics of the system, such as its name, purpose, and security impact level.
struct SystemCharacteristics {
    /// Unique identifiers for the system.
    std::vector<SystemId> system_ids = {};
    /// The full name of the system.
    std::string system_name;
    /// A short name for the system, such as an acronym, that is suitable for display in a data table or summary list.
    std::optional<std::string> system_name_short = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// The date the system received its authorization.
    std::optional<std::string> date_authorized = std::nullopt;
    /// The overall information system sensitivity categorization, such as defined by FIPS-199.
    std::optional<std::string> security_sensitivity_level = std::nullopt;
    /// Contains details about all information types that are stored, processed, or transmitted by the system.
    SystemInformation system_information;
    /// The overall level of expected impact resulting from unauthorized disclosure, modification, or loss of access to information.
    std::optional<SecurityImpactLevel> security_impact_level = std::nullopt;
    /// Describes the operational status of the system.
    SystemStatus system_status;
    /// A description of this system's authorization boundary, optionally supplemented with diagrams that illustrate the authorization boundary.
    AuthorizationBoundary authorization_boundary;
    /// A description of the system's network architecture, optionally supplemented with diagrams that illustrate the network architecture.
    std::optional<NetworkArchitecture> network_architecture = std::nullopt;
    /// A description of the logical flow of information within the system and across its boundaries, optionally supplemented with diagrams.
    std::optional<DataFlow> data_flow = std::nullopt;
    /// Responsible party assignments.
    std::vector<SspSystemCharacteristicsResponsibleParty> responsible_parties = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Contains details about all information types that are stored, processed, or transmitted by the system, such as privacy information.
struct SystemInformation {
    /// A list of properties.
    std::vector<SspSystemInformationProp> props = {};
    /// A list of links.
    std::vector<SspSystemInformationLink> links = {};
    /// Contains details about one information type that is stored, processed, or transmitted by the system.
    std::vector<InformationType> information_types = {};
};


/// Contains details about one information type that is stored, processed, or transmitted by the system, such as privacy information, and its impact level.
struct InformationType {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::optional<std::string> uuid = std::nullopt;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// A set of information type identifiers qualified by the given identification system used.
    std::vector<InformationTypeCategorization> categorizations = {};
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// The expected level of impact resulting from the unauthorized disclosure of the described information.
    std::optional<ImpactLevel> confidentiality_impact = std::nullopt;
    /// The expected level of impact resulting from the unauthorized modification of the described information.
    std::optional<ImpactLevel> integrity_impact = std::nullopt;
    /// The expected level of impact resulting from the disruption of access to or use of the described information or the information system.
    std::optional<ImpactLevel> availability_impact = std::nullopt;
};


/// A set of information type identifiers qualified by the given identification system used.
struct InformationTypeCategorization {
    /// Specifies the information type identification system used. Recommended values are in InformationTypeCategorizationSystemEnum; other values are permitted (OSCAL allow-other="yes").
    std::string system;
    /// An identifier qualified by the given identification system used, such as NIST SP 800-60.
    std::vector<std::string> information_type_ids = {};
};


/// The expected level of impact resulting from the described information's confidentiality, integrity, or availability affect.
struct ImpactLevel {
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// The prescribed base (Confidentiality, Integrity, or Availability) security impact level.
    std::string base;
    /// The selected (Confidentiality, Integrity, or Availability) security impact level.
    std::optional<std::string> selected = std::nullopt;
    /// If the selected security level is different from the base security level, this contains the justification for the change.
    std::optional<std::string> adjustment_justification = std::nullopt;
};


/// The overall level of expected impact resulting from unauthorized disclosure, modification, or loss of access to information.
struct SecurityImpactLevel {
    /// A target-level of confidentiality for the system, based on the sensitivity of information within the system.
    std::string security_objective_confidentiality;
    /// A target-level of integrity for the system, based on the sensitivity of information within the system.
    std::string security_objective_integrity;
    /// A target-level of availability for the system, based on the sensitivity of information within the system.
    std::string security_objective_availability;
};


/// Describes the operational status of the system.
struct SystemStatus {
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
    /// The current operating status of the system.
    SystemOperatingStatusEnum state;
};


/// A description of this system's authorization boundary, optionally supplemented with diagrams that illustrate the authorization boundary.
struct AuthorizationBoundary {
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// A collection of diagrams that visually depict the subject.
    std::vector<Diagram> diagrams = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A graphic that provides a visual representation the system, or some aspect of it.
struct Diagram {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<SspDiagramLink> links = {};
    /// A brief caption to annotate the diagram.
    std::optional<std::string> caption = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A description of the system's network architecture, optionally supplemented with diagrams that illustrate the network architecture.
struct NetworkArchitecture {
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// A collection of diagrams that visually depict the subject.
    std::vector<Diagram> diagrams = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A description of the logical flow of information within the system and across its boundaries, optionally supplemented with diagrams.
struct DataFlow {
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// A collection of diagrams that visually depict the subject.
    std::vector<Diagram> diagrams = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Provides information as to how the system is implemented.
struct SystemImplementation {
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// A description of another authorized system from which this system inherits capabilities that satisfy security requirements.
    std::vector<LeveragedAuthorization> leveraged_authorizations = {};
    /// A collection of system users.
    std::vector<SystemUser> users = {};
    /// A collection of system components.
    std::vector<SspSystemComponent> components = {};
    /// A collection of inventory items.
    std::vector<SspInventoryItem> inventory_items = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A description of another authorized system from which this system inherits capabilities that satisfy security requirements. Another term for this concept is a common control provider.
struct LeveragedAuthorization {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::string title;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<SspLeveragedAuthorizationLink> links = {};
    /// A machine-oriented identifier reference to the party who is making the log entry.
    std::string party_uuid;
    /// The date the system received its authorization.
    std::string date_authorized;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Describes how the system satisfies a set of controls.
struct SspControlImplementation {
    /// A human-readable description.
    std::string description;
    /// Identifies the parameter that will be set by the enclosed value.
    std::vector<SetParameter> set_parameters = {};
    /// Control implementation requirement entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SspImplementedRequirement> implemented_requirements = {};
};


/// Describes how the system satisfies an individual control.
struct SspImplementedRequirement {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A reference to a control by its identifier.
    std::string control_id;
    /// A list of properties.
    std::vector<SspControlOriginationProp> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Identifies the parameter that will be set by the enclosed value.
    std::vector<SetParameter> set_parameters = {};
    /// Responsible role assignments.
    std::vector<SspImplementedRequirementResponsibleRole> responsible_roles = {};
    /// Control statement implementation entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<SspStatement> statements = {};
    /// Defines how the referenced component implements a set of controls.
    std::vector<ByComponent> by_components = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies which statements within a control are addressed.
struct SspStatement {
    /// A reference to a control statement identifier.
    std::string statement_id;
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A list of properties.
    std::vector<SspControlOriginationProp> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Responsible role assignments.
    std::vector<SspImplementedRequirementResponsibleRole> responsible_roles = {};
    /// Defines how the referenced component implements a set of controls.
    std::vector<ByComponent> by_components = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Defines how the referenced component implements a set of controls.
struct ByComponent {
    /// A UUID reference to a component.
    std::string component_uuid;
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<SspControlOriginationProp> props = {};
    /// A list of links.
    std::vector<SspByComponentLink> links = {};
    /// Identifies the parameter that will be set by the enclosed value.
    std::vector<SetParameter> set_parameters = {};
    /// Identifies the implementation status of the control.
    std::optional<ImplementationStatus> implementation_status = std::nullopt;
    /// Defines a set of control implementations that are provided as reference implementations for use by organizations implementing the leveraged system.
    std::optional<Export> export = std::nullopt;
    /// Describes a control implementation inherited by a leveraging system.
    std::vector<InheritedControlImplementation> inherited = {};
    /// Describes how this system satisfies a responsibility imposed by a leveraged system.
    std::vector<SatisfiedControlImplementation> satisfied = {};
    /// Responsible role assignments.
    std::vector<SspByComponentResponsibleRole> responsible_roles = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Defines a set of control implementations that are provided as reference implementations for use by organizations implementing the leveraged system.
struct Export {
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Describes a capability which may be inherited by a leveraging system.
    std::vector<ProvidedControlImplementation> provided = {};
    /// Describes a control implementation responsibility imposed on a leveraging system.
    std::vector<ControlResponsibility> responsibilities = {};
};


/// Describes a capability which may be inherited by a leveraging system.
struct ProvidedControlImplementation {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Responsible role assignments.
    std::vector<SspByComponentResponsibleRole> responsible_roles = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Describes a control implementation responsibility imposed on a leveraging system.
struct ControlResponsibility {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Machine-oriented identifier reference to an inherited control implementation that a leveraging system is implementing.
    std::optional<std::string> provided_uuid = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Responsible role assignments.
    std::vector<SspByComponentResponsibleRole> responsible_roles = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Describes a control implementation inherited by a leveraging system.
struct InheritedControlImplementation {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Machine-oriented identifier reference to an inherited control implementation that a leveraging system is implementing.
    std::optional<std::string> provided_uuid = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Responsible role assignments.
    std::vector<SspByComponentResponsibleRole> responsible_roles = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Describes how this system satisfies a responsibility imposed by a leveraged system.
struct SatisfiedControlImplementation {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Machine-oriented identifier reference to a control implementation responsibility imposed by a leveraged system.
    std::optional<std::string> responsibility_uuid = std::nullopt;
    /// A human-readable description.
    std::string description;
    /// A list of properties.
    std::vector<Property> props = {};
    /// A list of links.
    std::vector<Link> links = {};
    /// Responsible role assignments.
    std::vector<SspByComponentResponsibleRole> responsible_roles = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// SSP-scoped property used in system characteristics.
struct SspSystemCharacteristicsProp : public Property {
};


/// SSP-scoped property used in system information.
struct SspSystemInformationProp : public Property {
};


/// SSP-scoped property used in implemented requirement and by-component contexts.
struct SspControlOriginationProp : public Property {
};


/// SSP-scoped property used for component and inventory allows-authenticated-scan.
struct SspAllowsAuthenticatedScanProp : public Property {
};


/// SSP-scoped link used in system information.
struct SspSystemInformationLink : public Link {
};


/// SSP-scoped link used in diagram objects.
struct SspDiagramLink : public Link {
};


/// SSP-scoped link used in leveraged authorization objects.
struct SspLeveragedAuthorizationLink : public Link {
};


/// SSP-scoped link used in by-component contexts.
struct SspByComponentLink : public Link {
};


/// SSP-scoped responsible party for system characteristics.
struct SspSystemCharacteristicsResponsibleParty : public ResponsibleParty {
};


/// SSP-scoped responsible role used by implemented requirement and statement contexts.
struct SspImplementedRequirementResponsibleRole : public ResponsibleRole {
};


/// SSP-scoped responsible role used by by-component contexts.
struct SspByComponentResponsibleRole : public ResponsibleRole {
};


/// SSP-scoped system component with allows-authenticated-scan property typing.
struct SspSystemComponent : public SystemComponent {
};


/// SSP-scoped inventory item with allows-authenticated-scan property typing.
struct SspInventoryItem : public InventoryItem {
};


/// Root wrapper for an OSCAL Assessment Results document.
struct AssessmentResultsDocument : public OscalDocument {
    /// The root assessment results object.
    AssessmentResults assessment_results;
};


/// Security assessment results, such as those provided by a FedRAMP assessor in a security assessment report.
struct AssessmentResults {
    /// Assessment Results Universally Unique Identifier.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Used to import information about the governing assessment plan.
    ImportAssessmentPlan import_ap;
    /// Used to define data objects that do not appear in the referenced SSP.
    std::optional<AssessmentResultsLocalDefinitions> local_definitions = std::nullopt;
    /// A collection of assessment results.
    std::vector<Result> results = {};
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Used by assessment-results to import information about the original plan for assessing the system.
struct ImportAssessmentPlan {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Used to define data objects that are referenced by the assessment results but do not appear in the imported assessment plan.
struct AssessmentResultsLocalDefinitions {
    /// A collection of locally-defined control objectives.
    std::vector<LocalObjective> objectives_and_methods = {};
    /// A collection of activities.
    std::vector<Activity> activities = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies all of the assessment observations and findings, initial and residual risks, deviations, and disposition for a particular execution of the assessment.
struct Result : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// The start date/time.
    std::string start;
    /// The end date/time.
    std::optional<std::string> end = std::nullopt;
    /// Used to define data objects that do not appear in the referenced SSP.
    std::optional<ResultLocalDefinitions> local_definitions = std::nullopt;
    /// Identifies the controls being assessed and their control objectives.
    ReviewedControls reviewed_controls;
    /// A set of attestation statements for the result.
    std::vector<Attestation> attestations = {};
    /// A log of assessment-related actions taken.
    std::optional<AssessmentLog> assessment_log = std::nullopt;
    /// A collection of observations captured in the containing context.
    std::vector<Observation> observations = {};
    /// A collection of risks captured in the containing context.
    std::vector<Risk> risks = {};
    /// A collection of findings captured in the containing context.
    std::vector<Finding> findings = {};
};


/// Used to define local implementation and assessment assets referenced by a result that do not appear in the imported system security plan.
struct ResultLocalDefinitions {
    /// A collection of system components.
    std::vector<SystemComponent> components = {};
    /// A collection of inventory items.
    std::vector<InventoryItem> inventory_items = {};
    /// A collection of system users.
    std::vector<SystemUser> users = {};
    /// Identifies the assets used to perform this assessment.
    std::optional<AssessmentAssets> assessment_assets = std::nullopt;
    /// A collection of tasks.
    std::vector<Task> tasks = {};
};


/// A set of textual attestation statements, typically written by the assessor.
struct Attestation : public HasResponsibleParties {
    /// A collection of parts.
    std::vector<AssessmentPart> parts = {};
};


/// A log of all assessment-related actions taken.
struct AssessmentLog {
    /// Identifies an individual risk response that occurred as part of managing an identified risk.
    std::vector<AssessmentLogEntry> entries = {};
};


/// Identifies the result of an action and/or task that occurred as part of executing an assessment plan or assessment event.
struct AssessmentLogEntry : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable name or title.
    std::optional<std::string> title = std::nullopt;
    /// A human-readable description.
    std::optional<std::string> description = std::nullopt;
    /// The start date/time.
    std::string start;
    /// The end date/time.
    std::optional<std::string> end = std::nullopt;
    /// Used to indicate who created a log entry in what role.
    std::vector<LoggedBy> logged_by = {};
    /// Identifies tasks for which the containing object is a consequence.
    std::vector<RelatedTask> related_tasks = {};
};


/// Root wrapper for an OSCAL Component Definition document.
struct ComponentDefinitionDocument : public OscalDocument {
    /// The root component-definition object.
    ComponentDefinition component_definition;
};


/// A collection of component descriptions, which may optionally be grouped by capability.
struct ComponentDefinition {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Component-definition resources imported into this document.
    std::vector<ImportComponentDefinition> import_component_definitions = {};
    /// A collection of system components.
    std::vector<DefinedComponent> components = {};
    /// Capability groupings for the defined components.
    std::vector<Capability> capabilities = {};
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Loads a component definition from another resource.
struct ImportComponentDefinition {
    /// A resolvable URL reference to a resource.
    std::string href;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A defined component that can be part of an implemented system.
struct DefinedComponent : public OscalCommon, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Indicates the nature or kind of the containing object.
    std::string type;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// A summary of the technological or business purpose of the component.
    std::optional<std::string> purpose = std::nullopt;
    /// Information about the protocol used to provide a service.
    std::vector<Protocol> protocols = {};
    /// Control implementation sets for a component or capability.
    std::vector<ControlImplementationSet> control_implementations = {};
};


/// A grouping of other components and/or capabilities.
struct Capability : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A textual label that uniquely identifies an attribute or semantic type.
    std::string name;
    /// A human-readable description.
    std::string description;
    /// Component references incorporated by a capability.
    std::vector<IncorporatesComponent> incorporates_components = {};
    /// Control implementation sets for a component or capability.
    std::vector<ControlImplementationSet> control_implementations = {};
};


/// The collection of components comprising a capability.
struct IncorporatesComponent {
    /// A UUID reference to a component.
    std::string component_uuid;
    /// A human-readable description.
    std::string description;
};


/// Defines how the component or capability supports a set of controls.
struct ControlImplementationSet : public HasPropsAndLinks {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Reference to an external catalog or profile resource.
    std::string source;
    /// A human-readable description.
    std::string description;
    /// Parameter values applied in the containing implementation context.
    std::vector<SetParameter> set_parameters = {};
    /// Control implementation requirement entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<ImplementedRequirement> implemented_requirements = {};
};


/// Describes how the containing component or capability implements an individual control.
struct ImplementedRequirement : public HasPropsAndLinks, public HasResponsibleRoles {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A reference to a control by its identifier.
    std::string control_id;
    /// A human-readable description.
    std::string description;
    /// Parameter values applied in the containing implementation context.
    std::vector<SetParameter> set_parameters = {};
    /// Control statement implementation entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<ImplementedControlStatement> statements = {};
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Identifies which statements within a control are addressed.
struct ImplementedControlStatement : public HasPropsAndLinks, public HasResponsibleRoles {
    /// A reference to a control statement identifier.
    std::string statement_id;
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// A human-readable description.
    std::string description;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Root wrapper for an OSCAL Mapping Collection document.
struct MappingCollectionDocument : public OscalDocument {
    /// The root mapping collection object.
    MappingCollection mapping_collection;
};


/// A collection of control mappings between source and target resources.
struct MappingCollection {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Global provenance and mapping method metadata.
    MappingProvenance provenance;
    /// A collection of control mappings.
    std::vector<Mapping> mappings = {};
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Mapping-level provenance details and mapping defaults.
struct MappingProvenance : public OscalCommon, public HasResponsibleParties {
    /// Method indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    MappingMethodEnum method;
    /// The rationale method used to relate mapped items.
    MatchingRationaleEnum matching_rationale;
    /// Status indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    MappingStatusEnum status;
    /// Confidence descriptor for a mapping.
    std::optional<ConfidenceScore> confidence_score = std::nullopt;
    /// Coverage metadata for a mapping.
    std::optional<Coverage> coverage = std::nullopt;
    /// Description of the context and intended use of the mapping.
    std::string mapping_description;
};


/// A mapping between two mapped resources.
struct Mapping : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Method indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    std::optional<MappingMethodEnum> method = std::nullopt;
    /// The rationale method used to relate mapped items.
    std::optional<MatchingRationaleEnum> matching_rationale = std::nullopt;
    /// Status indicator used by the containing OSCAL context. Allowed values are constrained by class-level slot_usage.
    std::optional<MappingStatusEnum> status = std::nullopt;
    /// Reference to the mapping source resource.
    MappingResourceReference source_resource;
    /// Reference to the mapping target resource.
    MappingResourceReference target_resource;
    /// Mapping entries relating source items to target items.
    std::vector<Map> maps = {};
    /// Description of the context and intended use of the mapping.
    std::optional<std::string> mapping_description = std::nullopt;
    /// Summary of unmapped source controls.
    std::optional<GapSummary> source_gap_summary = std::nullopt;
    /// Summary of unmapped target controls.
    std::optional<GapSummary> target_gap_summary = std::nullopt;
    /// Confidence descriptor for a mapping.
    std::optional<ConfidenceScore> confidence_score = std::nullopt;
    /// Coverage metadata for a mapping.
    std::optional<Coverage> coverage = std::nullopt;
};


/// A relationship-based mapping entry between source and target sets.
struct Map : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// An optional namespace qualifying a name. Allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// The rationale method used to relate mapped items.
    std::optional<MatchingRationaleEnum> matching_rationale = std::nullopt;
    /// Relationship type for a mapping entry. OSCAL namespace values are defined by RelationshipEnum.
    std::string relationship;
    /// Source references or source-participation entries in the containing OSCAL context. The concrete entry class is selected by class-level slot_usage.
    std::vector<MappingItem> sources = {};
    /// Target subjects participating in a mapping entry.
    std::vector<MappingItem> targets = {};
    /// Qualifier statements for a mapping entry.
    std::vector<QualifierItem> qualifiers = {};
    /// Confidence descriptor for a mapping.
    std::optional<ConfidenceScore> confidence_score = std::nullopt;
    /// Coverage metadata for a mapping.
    std::optional<Coverage> coverage = std::nullopt;
};


/// A source or target item participating in a mapping entry.
struct MappingItem : public OscalCommon {
    /// Indicates the nature or kind of the containing object.
    MappingSubjectTypeEnum type;
    /// Identifier reference of a source/target subject.
    std::string id_ref;
};


/// A reference to the source or target resource for a mapping.
struct MappingResourceReference : public OscalCommon {
    /// An optional namespace qualifying a name. Allows different organizations to associate distinct semantics with the same name.
    std::optional<std::string> ns = std::nullopt;
    /// The semantic type of the referenced resource. OSCAL defines catalog and profile, while locally defined values are also permitted.
    std::string type;
    /// A resolvable URL reference to a resource.
    std::string href;
};


/// A qualifier describing requirements or incompatibilities.
struct QualifierItem {
    /// Subject to which the qualifier applies.
    QualifierSubjectEnum subject;
    /// Predicate describing qualifier semantics.
    QualifierPredicateEnum predicate;
    /// Confidence category label or qualifier category value.
    QualifierCategoryEnum category;
    /// A human-readable description.
    std::string description;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// A summary of controls that were not mapped.
struct GapSummary {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Controls that remain unmapped.
    std::vector<SelectControlById> unmapped_controls = {};
};


/// Confidence represented as a category and/or percentage value.
struct ConfidenceScore {
    /// Confidence category label or qualifier category value.
    std::optional<std::string> category = std::nullopt;
    /// A decimal percentage value in the range 0 to 1.
    std::optional<float> percentage = std::nullopt;
};


/// A percentage representing target coverage by source mappings.
struct Coverage {
    /// Method used to determine the coverage value. Recommended values are in CoverageGenerationMethodEnum; other values are permitted.
    std::optional<std::string> generation_method = std::nullopt;
    /// Percentage coverage of targets by sources.
    float target_coverage;
};


/// Root wrapper for an OSCAL Plan of Action and Milestones document.
struct PoamDocument : public OscalDocument {
    /// The root plan of action and milestones object.
    PlanOfActionAndMilestones plan_of_action_and_milestones;
};


/// A plan of action and milestones that identifies initial and residual risks, deviations, and disposition.
struct PlanOfActionAndMilestones {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::string uuid;
    /// Provides information about the containing document, and defines concepts shared across the document.
    Metadata metadata;
    /// Used to import information about the system from an SSP.
    std::optional<ImportSSP> import_ssp = std::nullopt;
    /// A human-oriented, globally unique identifier for a system.
    std::optional<SystemId> system_id = std::nullopt;
    /// Used to define data objects that do not appear in the referenced SSP.
    std::optional<PoamLocalDefinitions> local_definitions = std::nullopt;
    /// A collection of observations captured in the containing context.
    std::vector<Observation> observations = {};
    /// A collection of risks captured in the containing context.
    std::vector<Risk> risks = {};
    /// A collection of findings captured in the containing context.
    std::vector<Finding> findings = {};
    /// A collection of POA&M items.
    std::vector<PoamItem> poam_items = {};
    /// A collection of resources that may be referenced from within the OSCAL document instance.
    std::optional<BackMatter> back_matter = std::nullopt;
};


/// Allows components and inventory items to be defined within the POA&M for cases where no OSCAL SSP is available with the POA&M.
struct PoamLocalDefinitions {
    /// A collection of system components.
    std::vector<SystemComponent> components = {};
    /// A collection of inventory items.
    std::vector<InventoryItem> inventory_items = {};
    /// Identifies the assets used to perform this assessment.
    std::optional<AssessmentAssets> assessment_assets = std::nullopt;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


/// Describes an individual POA&M item.
struct PoamItem : public OscalCommon {
    /// A machine-oriented, globally unique identifier with a cross-instance scope.
    std::optional<std::string> uuid = std::nullopt;
    /// A human-readable name or title.
    std::string title;
    /// A human-readable description.
    std::string description;
    /// Identifies the source of observations, findings, or risks.
    std::vector<Origin> origins = {};
    /// Relates a POA&M item to one or more findings.
    std::vector<RelatedFinding> related_findings = {};
    /// Relates the containing object to a set of referenced observations.
    std::vector<RelatedObservation> related_observations = {};
    /// Relates the finding to a set of referenced risks.
    std::vector<AssociatedRisk> related_risks = {};
};


/// Relates a POA&M item to a referenced finding.
struct RelatedFinding {
    /// A UUID reference to a finding.
    std::string finding_uuid;
    /// Additional commentary about the containing object.
    std::optional<std::string> remarks = std::nullopt;
};


} // namespace oscal
